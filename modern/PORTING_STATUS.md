# Monopoly 1999 -> modern porting status

Cette matrice est la carte unique du port moderne. La source originale reste
l'autorite semantique; les statuts ci-dessous decrivent uniquement le code
effectivement present sous `modern/`.

## Campagne du 20 septembre 2026 - GIS-8 / GIS-9 / GIS-10

Les compilations et tests ont ete explicitement reautorises. Sept sous-agents
Astra couvrent repereur, expert legacy, deux workers, tests, reviewer et
validator; le pilote conserve le plan et l integration. Le validator est
l unique executant des builds/tests.

**GIS-8** : l admission TCP attend maintenant que MESS soit vide avant de
remettre `ResyncClient` a RULE. La resynchronisation complete dispose ainsi
de la capacite necessaire. Pendant un lock d animation, DATN/STOP des pairs
etablis restent traites sans consommer cette admission; une regression a ete
reproduite puis corrigee sur ce cas. Les tests loopback couvrent
identites, admission, framing, saturation, Unicode/blobs et deconnexion.

**GIS-9** : `TextureCatalog::boardTextureRelativePath` traduit le contexte
edition/langue/ville/devise en chemins stock. `BoardTextureRuntime` utilise
`ResourcePaths` et le decodeur BMP existant, avec limites de taille et overlays
Europe par index de palette 0. L anomalie `CT07/08/09_256` du preset High/128
conserve ses dimensions physiques. Racines custom et overlays non palettises
restent explicitement non supportes.

`MeshXRuntime::withTextureImages` remplace les images apres construction : les
UV normalises et poses restent identiques, meme avec un BMP 256 remplacant
une image HMD 128. Page/raw X/Y doivent correspondre au mesh; tous les groupes
materiau modernes partageant l image changent ensemble. `MeshRuntimeCache`
publie transactionnellement une nouvelle identite pour ce seul DataId. Les
caches World3D/GPU existants detectent cette identite. `SequencePlayback`
memorise le contexte complet, actualise par `Engine` avant son retour "meme
DataId" : USA ville 1 -> 2 rafraichit donc les textures. Europe stock utilise
le mesh classique medium. Le runtime utilise 128 px; l API couvre aussi 256 px.

Un BMP/overlay requis absent ou invalide fait echouer le lot et conserve le
cache precedent. Le legacy ignorait ces entrees avec diagnostic debug; ce
port expose explicitement l erreur au caller. La voie HMD embarquee reste
preservee, notamment pour le plateau USA classique 128 px.

**GIS-10** : aucun decodeur supplementaire n est justifie par les callers
actifs sous `USE_OLD_FRAME`. `Source/PC3D/hmdload.cpp` active les triangles
`0x8/0x9/0xC/0xD`, l image `0x02000001` et les MIMe vertex/normal
`0x04010020/0x04010021`, deja couverts par `LegacyMeshData` et `MeshRuntime`.
Quad, UIMG0, ground/envmap et reset/joint MIMe sont commentes dans ce dispatch;
les types non prouves restent explicitement non supportes. Cet audit des
callers ne prouve pas un contenu retail absent.

La compilation a revele une inclusion manquante du lot texte d enchere :
`AuctionTextPlayback.cpp` inclut maintenant `AuctionPlayback.hpp` pour utiliser
les constantes existantes. Aucune valeur n a ete dupliquee ou inventee.
La cible `UserInterfaceRoutingTests` retrouve aussi ses dependances reelles
OptionsSaveRuntime/FontRuntime/SDL et ses doubles de services Engine/IBar/
LocalPlayers manquants; ses assertions existantes restent preservees.
`IBarActionInputTests` retrouve SDL, OptionsUI et ses doubles de services
timer/son/etat Options. Les deux cibles compilees passent leurs tests cibles.
`StatsUITests` lie maintenant son module IBarLayout manquant.

La premiere suite complete a donne 105/109 : quatre tests attendaient encore
des commandes Move distinctes apres le Start, alors que `ba81764` a rendu la
transformation locale au Start, conformement a `L_Seqncr`. Le test de mouvement
utilisait en plus Main:1, ecrase dans la fixture par les barres Stats. Il utilise
desormais la sequence finie synthetique ThreeD:0x0157 de meme contenu. Les tests
conservent leurs assertions de poses, fin, boucles, GO et atomicite; le seuil
de saturation RuntimeBitmapSurface est ajuste a la vraie capacite requise.

Qualification finale Windows/MSVC Debug : build global reussi, application
recompilee avec le correctif reseau, puis confirmation Ninja sans travail
restant. **109/109 suites CTest passent en 20,69 s**, dont TCP (8 groupes),
audio SDL dummy (3 groupes), textures/mesh et rendu Direct3D12 avec readback.
Logs locaux : `build-phase-c/gis8-final-build.log`, `gis8-final-ctest.log`
et `gis8-final-LastTest.log` dans ce meme repertoire.

Aucun test CTest ni GPU saute. Deux cas internes ResourcePaths restent non
executes sur cet hote : filesystem sensible a la casse et creation de symlink
sans privilege. Les warnings de conversion/signedness, getenv et dependances
externes ne sont pas presentes comme corriges. Les controles Git, perimetre
Source/SVG et fins de ligne sont distincts de cette qualification executable.

Les DAT/HMD retail ne sont pas disponibles ici. Ces preuves ne remplacent
donc pas une partie complete, le changement de ville visible en jeu, ni une
conversation entre deux machines avec microphone et sortie audio physiques.
Les branches POSIX ne sont pas compilees. Aucun commit ni push de ce lot.

## Checkpoint du 20 septembre 2026 - GIS-8 avant reautorisation des tests

Base locale remise a `c360a04` par avance rapide depuis `origin/master`.
Le rightpanel Future/Immunity est acquis et ne fait plus partie des travaux
restants. Les anciens identifiants `c3e32fb` (audio/GSM) et `3edb287` (Trade/IBar)
correspondent respectivement a `acaf985` et `cc9db8c` apres reecriture : leurs
contenus modernes ont ete compares avant reprise.

`Messaging` possede maintenant un `Transport` optionnel; `TcpMessageTransport`
fournit une session TCP IPv4 reelle, avec hote et jusqu a neuf spectateurs
vocaux. `Application` la raccorde par `--voice-host IPv4:port` ou
`--voice-connect IPv4:port`. Le mode reseau depend d un handshake reussi;
une simple ecoute ne declenche pas la capture. Le client ne devient jamais
serveur RULE en cas d echec ou de deconnexion.

Les connexions attribuent les identites machines transportees par
`Message::sourceId` puis `NotifyVoiceChat.numberD`. L admission declenche la
resynchronisation RULE existante; les options precedent le redemarrage micro,
qui reannonce CHAT meme si la capture etait deja active. Le sentinel
`NobodyPlayer` du pre-game est accepte dans cette resync. Une file pleine
differe le redemarrage jusqu a liberation de place. Les compteurs sortants
alimentent le seuil vocal >50, les sockets sont non bloquants, les tailles et
files bornees, les pairs lents deconnectes sans interrompre le jeu local.
La perte d un pair produit STOP avec son identite; la perte de la derniere
connexion ferme capture, recepteurs et sessions puis purge la voix obsolete.

Le perimetre est celui de GIS-8 : voix distante et notifications publiques pour
clients spectateurs, sans inventer DirectPlay ou l attribution de joueurs.
Les commandes gameplay distantes sont refusees avant RULE. Lobby, admission
de joueurs, migration/reconnexion et spectateur visuel complet restent des
contrats distincts. Le codec GSM/WAV49 et le pipeline PCM/DAT1/DATN/playback
existants ne sont pas modifies. Voir [NETWORK_VOICE.md](NETWORK_VOICE.md)
pour les commandes, le protocole et les limites.

**Etat : raccordement implemente, qualification GIS-8 encore ouverte.**
Relecture source/diff et controles Git uniquement : aucun test, build,
lancement reseau ou essai microphone. `Source/` et SVG inchanges; aucun commit
ni push de ce lot. L ordre reste **GIS-8 -> GIS-9 -> GIS-10**; FontRuntime est
transverse et ne remplace pas ces priorites.

## Lot du 20 septembre 2026 - Trade Future/Immunity

Reprise depuis `cc9db8c` (`Port trade and IBar runtime text`), apres la
reecriture de l historique. Le code du panneau droit est maintenant raccorde
par `TradeContractTextPlayback` : surface transparente 200x225 a (600,0),
police slot 7 (Arial 8/700), titres et consignes LANG des modes 0..6,
confirmation avec compteur et destinataire, nom joueur, liste de cinq lignes,
selection bleu (0,0,180)/texte blanc et bouton Okay 80x22 avec biseau retail.
Les listes des contrats existants gardent le prefixe `%02d`; le compteur de
creation utilise `%2i`. Les noms des proprietes suivent la ville et le fallback
`*` du source `Rule.cpp::InitialisePredefinedData`, avec le choix de base Europe
`language-2` pour `city == -1`; les noms personnalises des custom boards restent
tributaires du futur owner de ces donnees.

Correction de l audit precedent : **le bitmap `rightpanel` est affiche a la
priorite 148**, d apres les vrais `LE_SEQNCR_StartXY` de
`UDTrade_ProcessEverything`. Le champ `FutureTradeDlg/ImmunityTradeDlg.priority`
vaut 524 et sert aux fleches a **525**; il ne determine pas la priorite du bitmap.
Le libelle Okay utilise **TMN_BUTTON_OK = 933**, issu du fichier genere
`Dat_Mon/dat_lang.h` effectivement inclus, et non 802 dans l ancien `Langids.h`.

`TradeUI` maintient maintenant la liste des proprietes choisies en modes 1..3,
la positionne sur la propriete ajoutee et accepte les fleches avant les retours
anticipes du traitement modal. Le bornage conserve le dernier element comme
derniere position de depart, comme le retail. Le mode 6 reste en lecture seule.
Le rendu reconstruit la surface seulement quand le contenu visible change,
restaure la police et retire le bitmap a la fermeture ou a la sortie de Trade.

**Validation de ce lot : relecture source/diff et controles Git statiques
uniquement. Aucun test, aucune compilation ni validation visuelle executes.**
La parite visuelle et les metriques GDI/FreeType restent a verifier sur le jeu.
`Source/` reste inchange; le SVG demeure exclusivement gere par GitHub Actions.
Aucun nouvel indice fonctionnel global n est estime pour ce lot.

## Recapitulatif du 16 septembre 2026

Le cycle du 16 septembre couvre principalement `UDStats`, `UDChat`, les voix
Europe, le voice-chat et maintenant le demarrage du backend fonts portable. Le nombre
de commits continue d evoluer pendant ce cycle et n est donc plus utilise comme indicateur.
Ce recapitulatif est le snapshot du 16 septembre; le lot du 20 septembre
ci-dessus le complete. Les bilans dates du 14 et du 15 ci-dessous restent
egalement des snapshots historiques.

| Domaine | Commits principaux du 16/09 | Livraison constatee |
|---|---|---|
| `UDStats` / Portfolio | `fed05bf` -> `71c9632`, `a235507` | Projection Player/Deed/Bank, playback sequence, bank/player overlays, grille Deed, floater/bar Owner, routage BSSM, calculatrice complete cote logique/workflow, picker Deed, Future/Immunity et cartes Liabilities autonomes. `a1a249b` rend en plus les transformations `StartXY` instance-locales pour les doublons DataId/priorite. |
| `UDChat` / Fluff | `d594079` -> `a369993` | Destinataires, controles/drag/resize, Options, fenetre Fluff, scroll, categories/phrases canned, historique de saisie 100 entrees et scrollbar Chat. Le checkpoint `c0e5b3a` a ete nettoye par `17ea4e6`; le script one-shot temporaire n'est plus suivi. |
| Voix / audio | `17cb877`, `64fd9ae`, `283a3a3`, `f29fda4`, `1137dc0` | Transport voice-chat, validation des paquets, mappings complets des 32 cartes Europe et handoff musique Credits `WAV_credit_a -> WAV_m1`. |
| File MESS / sessions voice-chat | `3609b32`, `4ffbc1c` | Dequeue prioritaire du voice-chat pendant un game-queue lock, parseur ArtLib `CHAT/fmt/dims/volm/DATN/DAT1/POSN/STOP` et jusqu'a 10 sessions de reception par source. Capture, codec GSM 6.10 et lecture audio restent hors de ce lot. |

Indicateurs structurels recalcules sur la matrice actuelle : **41/41 familles
engagees = 100 %** (les 6 familles `LEGACY_UNUSED` sont exclues) et **63 %
d'indice automatique** sur 68 entrees actives : 18 completes/remplacees,
50 partielles et 0 non demarree. L'ancienne estimation globale de 75 % du
14 septembre est conservee uniquement comme snapshot historique; elle n'est
pas reutilisee comme estimation courante sans nouvel audit fonctionnel global.
Le premier indicateur du SVG est donc explicitement le **dernier audit
fonctionnel connu (75 %)**, pas une estimation courante. Le generateur accepte
maintenant les snapshots dates et echoue si aucun audit n'est trouve, au lieu
de fabriquer un 0 % lors d'un changement de libelle. Le SVG reste genere par
GitHub Actions et n'est pas edite manuellement.

## Recapitulatif du 15 septembre 2026

Ce recapitulatif reprend les 26 commits Git dates du 15 septembre 2026. Il
complete le bilan consolide du 14 sans recalculer son estimation globale et
n ajoute aucune affirmation de validation qui ne soit pas deja documentee.

| Domaine | Commits du 15/09 | Livraison constatee |
|---|---|---|
| Projection UI / etat | `01577fb`, `8043982`, `0b88223`, `b90cedd`, `1a16915`, `0295274`, `52ea700`, `bd20dda`, `8635b45`, `3431e5e`, `26b71b6`, `8aafa0d` | Cash, mortgage/free parking, lock BSSM, jail cards, futures/immunities, resync client, turn UI, reset trade, lifecycle load, cash animation, free-unmortgage, focus UI et decomposition hotel sont projetes dans le miroir moderne. |
| BSSM / IBar / ready / chat | `d211cd9`, `8788a99`, `39ce375`, `8864cdc`, `a783a71`, `e7776fc` | SFX et camera BSSM, handoffs IBar et sons UI, roll-call generique, filtrage du chat prive vers le slot local et actions GameOver IBar sont portes. |
| Trade | `6f58356`, `83c5155`, `7ec53da`, `be50a7f`, `ae7b8a8`, `5be592b` | Warning d edition rejetee, teardown modal/proposition, abandon si participant bankrupt, routage du joueur UI courant et tracking IBar de fin de trade sont alignes sur le retail. |
| Synchronisation finale UI | `5041976`, `e5eeef7` | La synchronisation ready reste possedee par le client local et le toggle global IBar Status/Main est restaure. |

## Bilan consolide du 14 septembre 2026

Etat consolide apres les ports AI, Chat et audio SDL3 de septembre sur `master`.
La source originale reste l'autorite semantique et aucun changement de ce cycle
de portage ne touche `Source/`.

**Progression fonctionnelle estimee au 14 septembre : environ 75 %**. Cette
valeur est conservee comme snapshot historique et ne decrit plus l'etat courant
du 16 septembre. Elle n'etait pas derivee du nombre de lignes ou de familles.

Les trois indicateurs ci-dessous sont donc ceux de ce snapshot du 14 septembre :

- **Snapshot 14/09 - 75 % fonctionnels estimes** : appreciation du port global; la hausse n'est
  pas forcee tant que les nouveaux chemins audio/UI n'ont pas une qualification
  runtime plus large.
- **Snapshot 14/09 - 36/41 familles engagees = 87,8 %** : matrices Jeu Monopoly et ArtLib
  (hors detail PC3D et DATA), apres exclusion documentee des familles legacy
  hors cible livree et engagement reel de `UDChat` puis `UDSound`.
- **Snapshot 14/09 - 59 % d'indice automatique** : 68 entrees actives, dont 18 completes ou
  remplacees, 45 partielles et 5 non demarrees; les entrees bloquees et
  l'outillage manquant sont exclus. Cet indice n'est pas l'estimation globale.


| Checkpoint (heure Europe/Paris) | Progres significatif livre | Validation retrouvee / limite |
|---|---|---|
| `5748c42` (09:42) | Constructeur transactionnel `AI_Trade_SemiImportant_Property` : partenaires, groupes, exclusions retail et compensation cash. | Tests cibles, clean build Ninja complet et CTest 92/92 PASS. |
| `06282b4` (10:09) + `c9f8aba` (11:28) | Declarations puis implementation du tri Darzinskis/random et de `AI_Propose_Trade`; priorite give-monopoly-for-cash, acquisition, fallback semi-important, envoi/cooldown et declenchement autonome tick/debut de tour. | Clean build Debug et CTest 92/92 PASS rapportes dans le run. Le commit de declarations n'est pas un progres fonctionnel autonome. Re-tirage semi-important par partenaire encore incomplet. |
| `ea41862` (13:17) | `AI_Spend_Excess_Assets` hors trade : planification construction/unmortgage, financement, demande/conservation/restitution du controle BSSM et reprise apres action. | Tests purs et runtime BSSM, build complet et CTest 92/92 PASS rapportes dans le run. |
| `18eecae` (15:10) | Decisions taxe/prison/free-unmortgage, gardes anti-doublon; `AI_Get_Most_Liquid_Assets`, `AI_Cash_Available_After_Housing` et `AI_Have_Monopoly_Trade`. | Clean build Debug et CTest 92/92 PASS; journal local du 13/09 a 15:08 confirme 92 succes et zero echec. |
| `0779b79` (16:41) | Importance strategique des proprietes : monopole direct, trade possible, cash cow, joueur exclu et achat en cours; propagation des dettes au calcul des liquidites. | Suites `AIUtility` et `AITradeUtility` PASS rapportees; nouveau test de classification present. Le declencheur push du workflow de progression est aussi livre, sans gain fonctionnel de jeu associe. |
| `c20ee4b` (21:20) | Achat/enchere, valorisation des mises et rotation des IA locales; roll/end-turn differes apres trade et depenses; penurie de logements et placement du batiment gagne. | Build Debug complet PASS rapporte. Tests achat pur/runtime ajoutes et executes lors d'un run intermediaire; pas de tests dedies retrouves pour les nouveaux chemins enchere, tour et penurie, ni de CTest global apres ce lot. |
| `62d4a84` (22:30) | `NotifyPleasePay` : dette persistante, hypotheque progressive, trades cash, vente de batiments/liquidation de monopoles, continuation `DecomposeHotel`, trade desperate et faillite arbitree par RULE. | Compilation de `MonopolyAIDecisionUtilityTests`, `MonopolyAITradeIngressTests` et `MonopolyModern.exe` PASS rapportee. Aucun nouveau test dans ce commit; execution du parcours de dette non demontree. |
| `b59b9a7` | `CardSeen` automatique, acceptation de configuration AI, reset transitoire resync et nettoyage des IA off-board. | `MonopolyAITradeIngressTests` et `MonopolyModern` compilent PASS; CTest global non relance. |
| `fd7a0d1` | Sauvegarde/restauration AI `AISV` portable : profil complet et 20 compteurs `time_last_trade`. | Cibles AI et `MonopolyModern` compilees PASS. |
| `e681c09` | Fermeture des ecarts runtime AI : contexte achat en cours et re-tirage semi-important par partenaire. | Cibles AI et `MonopolyModern` compilees PASS. |
| `4ef89fd` + `b68efc0` | Noyau Chat puis saisie SDL : historique 512, UTF-16LE, public/prive, Tab, Backspace, Enter et lots transactionnels. | `MonopolyModern` compile PASS. |
| `21e5455` | Backend audio SDL3 : WAV DAT, streams, gain/boucle et execution des feuilles Sound du sequenceur. | `MonopolyModern` compile PASS. |

Le dernier cycle apporte un backend audio SDL3 reel pour les WAV DAT et les
feuilles Sound du sequenceur, puis le noyau `UDSound` : Warning, Click,
CashUp/CashDown et musique `m1..m5` en boucle a volume retail. Le toggle Music
de `UDOpts` a maintenant un owner runtime reel. Token Voices possede desormais son catalogue USA/Europe exact de 73 categories, son toggle et ses premiers callers `UDPieces`. `UDPennyVoice` porte maintenant les reactions deterministes et les hooks achat/enchere/GoToJail; Host Comments/Pennybags et le raccord complet de fin de mouvement restent a poursuivre.

## Legende

- `PORTED_COMPLETE` : contrat original identifie, integre et verifie sans
  omission connue dans le perimetre indique.
- `PORTED_PARTIAL` : une partie reelle du comportement existe, mais le module
  conserve des omissions connues.
- `NOT_STARTED` : aucun equivalent fonctionnel significatif n'existe encore.
- `REPLACED_PORTABLE` : le service legacy a un equivalent portable couvrant
  le contrat consomme par Monopoly.
- `BLOCKED_MISSING_DATA` : le code ou les identifiants existent, mais les
  payloads necessaires ne sont pas disponibles et ne doivent pas etre inventes.
- `MISSING_TOOLING` : un outil historique necessaire a une reproduction
  bit-a-bit n'est pas present dans le depot inspecte.
- `LEGACY_UNUSED` : le callgraph a prouve que Monopoly ne consomme pas ce
  composant. Ne pas utiliser ce statut sans preuve.

## Jeu Monopoly

| Original | Equivalent moderne | Statut | Symboles originaux importants | Dependances | Travail restant / blocage |
|---|---|---|---|---|---|
| `Source/monopoly/Main.cpp` | `Application`, `Engine`, `Game`, `RenderSlots`, `Timers`, `GPUFrame` | `PORTED_PARTIAL` | `GameInitialise`, `InitRenderSlots`, `ProcessUIMessage`, `GameShutdown`, `GameUpdateCycle` | SDL3, SDL_GPU, DISPLAY, UI messages | Lifecycle DATA/LANG et timers raccorde. World3D 1 et Overlay2D alimentent maintenant des renderers SDL_GPU reels jusqu au swapchain/offscreen, avec readbacks D3D12; le renderer UI generique et le parcours interactif retail restent incomplets. |
| `Source/monopoly/GameInc.cpp/.h` | `RuleTypes`, `DataBanks`, `LegacyDataArchive`, includes modernes explicites | `PORTED_PARTIAL` | constantes `DAT_*`, `MAIN_GAME_TIMER`, configuration de build | Tous les sous-systemes | Groupes, chemins de banques et triplets de langue sont portes; continuer a remplacer le precompiled-header implicite par des interfaces explicites. |
| `Source/monopoly/Mdef.cpp` | `RuleTypes`, `RuntimeState`, `BoardRules` | `PORTED_PARTIAL` | constantes de jeu, langues, plateaux, joueurs | RULE, LANG, DISPLAY | Comparaison systematique des constantes encore a terminer. |
| `Source/monopoly/Mess.cpp` | `Messaging` | `PORTED_PARTIAL` | `MESS_InitializeSystem`, `MESS_SendAction`, `MESS_ReceiveActionMessage`, modes serveur/reseau | RULE, UI, Transport/TcpMessageTransport | File locale fonctionnelle avec la capacite retail exacte; owner optionnel TCP IPv4 moderne pour GIS-8, sans DirectPlay. `receiveVoiceChatOnly` reproduit maintenant `MESS_ReceiveVoiceChatMessageOnly` en extrayant ACTION/NOTIFY_VOICE_CHAT depuis le milieu de la file sans consommer les messages de jeu precedents. Le transport distant vocal est implemente dans TcpMessageTransport (hote et clients spectateurs); l attribution des joueurs et le lobby restent a porter. Qualification reseau/audio non executee. |
| `Source/monopoly/Rule.cpp` | `RulesEngine`, `Rule*`, `BoardRules`, `PhaseStack`, `CardDeck*` | `PORTED_PARTIAL` | creation, phases, tours, economie, cartes, prison, enchere, trade, save/resync | Messaging, RNG, donnees plateau | Couverture importante mais inventaire symbole-par-symbole et scenarios complets encore requis; ne pas reecrire RULE. `GF_WAIT_FOR_EVERYBODY_READY` respecte maintenant la frontiere retail : RULE diffuse `NotifyAreYouThere` et attend de vrais `IAmHere`; l ancien fallback moderne qui acquittait lui-meme tous les slots, y compris distants, a ete retire maintenant que `UserInterface` repond pour les seuls slots locaux et que l enchere differe sa reponse jusqu a Pennybags Begin. |
| `Source/monopoly/trade.cpp` | `RuleTrade`, `TradeUI` | `PORTED_PARTIAL` | editions, acceptations, `Trade_AddItem`, `Trade_SendItems`, `AddUiImmunity`, immunites, loyers futurs | RULE, Messaging, UI trade | La logique RULE reste largement portee. `TradeUI` porte maintenant le stockage editeur du source : capacite exacte 105 (`28+15+60+2`), remplacement Cash global / Square+receiver / JailCard+receiver / Immunity-Future+receiver+property-set, normalisation `ACTION_TRADE_ITEM` vers la Banque, suppression des contrats a zero et plafond combine 99 en ignorant volontairement `fromPlayer`; `AddUiImmunity` reproduit recherche existing/free, fallback collection et transient traded. `Trade_SendItems` et la chaine complete d emission editeur restent a raccorder avec les controles `UDTrade` suivants. |
| `Source/monopoly/Ai.cpp` | `AIDecisionUtility`, `AIUtility`, `AIMessageIngress`, `AITradeIngress`, `BoardRules` | `PORTED_PARTIAL` | reserve de cash, achat hypothetique, decisions economiques, boucle/message AI | `GameState`, `AIProfile`, Messaging, RULE actions | Le noyau decisionnel pur couvre maintenant reserve de cash, construction/unmortgage hypothetiques, achat de maisons, valeur/chances et plusieurs decisions de trade. `AIMessageIngress` est raccorde apres l UI comme dans le retail et delegue les notifications trade a `AITradeIngress`. Les profils `*.AI` sont parses, copies en `assets/ai`, charges par token/niveau pour les IA locales et convertis vers les configs modernes. `NotifyTradeEditor` declenche maintenant une contre-proposition profile-driven reelle. NotifyTradeAcceptanceDecision traite les IA locales chargees : acceptation TRUE/3 hors trade, TRUE/1 selon seuil normal ou fed-up, refus futures/immunites, counter ou refus, garde anti-doublon liberee par NotifyActionCompleted. Les reponses RULE et changements d editeur ajustent maintenant les attitudes des observateurs locaux charges et du proposant, avec seuils et signes retail. NotifyStartTurn entretient la memoire des proprietes, les 20 delais de trade et la derive vers l attitude neutre (egalite/overshoot retail preserves). La branche proactive de AI_Process_AIs/AI_Spend_Excess_Assets propose maintenant des trades sur ACTION_TICK, et aussi sur NotifyStartTurn quand AITakesTimeToThink est desactive, avec garde trade/auction/BSSM/placement/shortage/paiement et grace period retail de 10 s. Les etapes non-trade de AI_Spend_Excess_Assets sont maintenant raccordees : planification retail de construction/unmortgage, acquisition et conservation du controle BuySellMortgage, actions BuyHouse/Mortgaging, relance apres NotifyActionCompleted et restitution PlayerDoneBuySellMort. Les decisions runtime taxe, sortie de prison et free-unmortgage sont maintenant raccordees et testees avec gardes anti-doublon. AI_Should_Buy_Property, BuyOrAuction, la valorisation/rotation des encheres, roll/end-turn differe et la penurie de logements/placement sont maintenant raccordes avec gardes anti-doublon. NotifyPleasePay est maintenant raccorde: dette persistante dans les evaluations, hypotheque progressive, trade cash somewhat-important, liquidation maisons/monopoles, trade desperate, poursuite DecomposeHotel puis GoBankrupt arbitre par RULE. NotifyPickedUpCard -> CardSeen, acceptation de configuration, resync des transitoires sans purge des profils et nettoyage des IA eliminees/off-board sont maintenant raccordes. La sauvegarde/restauration AI est maintenant raccordee via un chunk AISV portable et versionne : tout le Profile runtime et les 20 delais time_last_trade sont serialises/restaures transactionnellement, tandis que les transitoires AI_State restent volontairement hors sauvegarde comme dans le retail. Le contexte retail d achat en cours est maintenant memorise entre NotifyBuyOrAuctionDecision et NotifySquareOwnership et injecte dans les evaluations de trade. Le re-tirage semi-important est aussi aligne sur le retail : choix initial du partenaire puis nouveaux groupes/limites pour chaque partenaire reellement tente, sans pre-consommer les rand des tentatives jamais atteintes. Les derniers case de production du switch AI sont ainsi raccordes; NotifyGameOver/NotifyNumberOfPlayers restants appartiennent exclusivement au mode drone/test historique et les autres notifications terminales sont des no-op retail. Les chemins enchere/tour/penurie/dette/save restent a qualifier par des tests dedies. |
| `Source/monopoly/Ai_load.cpp` | `AIProfile` | `PORTED_PARTIAL` | chargement `Boot.ai`, `Normal.ai`, profils jetons, parametres globaux/niveaux | fichiers `*.AI`, filesystem | Parser portable implemente pour GLOBAL + niveaux 1/2/3, 12 fichiers retail valides sur les 3 niveaux, mapping des 11 jetons, `WHAT TO TRADE`, enums/clamps et erreurs typees. Les builders alimentent `TradeEvaluationConfig`, preflight, balance et `MonopolyProposalConfig` sans valeurs inventees. `AIProfileRuntime` charge aussi `Normal.ai` niveau 3 comme expert, cache les profils joueurs par token/niveau, purge les slots invalides et resout les assets pres de l executable. |
| `Source/monopoly/Ai_trade.cpp` | `AITradeUtility`, `AIDecisionUtility`, `AICounterTradeRuntime`, `AITradeSendRuntime`, `AITradeIngress` | `PORTED_PARTIAL` | evaluation, fairness, counter-proposals, envoi et suivi de trade | noyau AI pur, `AIProfile`, RuleTrade, Messaging | Le port couvre maintenant evaluation de trade/chances/importance, preparation des partenaires, memoire des proprietes, fairness/properness, preflight et balancing des contre-propositions, plan d envoi retail ordonne, cycle `StartTradeEditing -> TradeItem -> TradeEditingDone` et reprise via notifications. Le chemin `NotifyTradeStarted -> TradeItem -> NotifyTradeEditor -> counter proposal -> RULE actions` est actif avec le vrai profil du pion. Les quirks/bugs retail documentes restent testes. Le cycle acceptance/counter est verrouille en multi-IA public/prive, sans rejet ajoute apres counter reussi, avec reprise differee RestartPhase et liberation de garde meme sur echec StartTradeEditing. Les attitudes et la maintenance memoire/delais de debut de tour sont raccordees aux notifications RULE. Les constructeurs purs AI_Trade_Monopoly, AI_Give_Monopoly_For_Cash et AI_Trade_SemiImportant_Property sont portes : acquisition/offre de lots, selection ponderee et circulaire du partenaire, exclusions retail, limites aleatoires injectees, compensation selon attitude et equilibrage cash transactionnel. `AI_Propose_Trade` est maintenant porte sous forme transactionnelle : ordre Darzinskis ascendant/descendant ou random injecte, liste de partenaires et exclusions retail, priorite give-monopoly-for-cash avec early-return historique, limite `smallest_trade == 2`, seuil d evaluation selon importance, properness puis fallback semi-important. `AIMessageIngress` declenche ce chemin sur `ACTION_TICK` et sur `NotifyStartTurn` en mode sans pause; `sendProactiveTrade` utilise le moteur d envoi existant et consomme un delai d historique seulement apres mise en file reussie, la proposition survivant a la synchronisation NotifyTradeStarted. La generation semi-important est retardee jusqu au vrai fallback pour ne pas consommer les rand avant les branches monopole; le re-tirage des limites/groupes est maintenant refait pour chaque partenaire reellement tente, sans pre-consommer les rand des tentatives jamais atteintes. `AI_Have_Monopoly_Trade` est aussi porte/teste avec la contrainte retail ou chaque participant doit pouvoir sortir avec un monopole; AI_Give_List_Players_Important_Monopoly / AI_Give_Players_Important_Monopoly classent maintenant NotImportant, GivesDirectMonopoly, AllowsTrade et CashCow en tenant compte des sous-groupes, du joueur exclu et de l achat en cours. |
| `Source/monopoly/Ai_util.cpp` | `AIUtility`, `BoardRules` | `PORTED_PARTIAL` | groupes/sets de proprietes, monopoles, lots, loyers, proximite, revenus/depenses, highest-rent/best-current-rent, stade de partie, maisons/hotels, actifs liquides, valeur totale | `GameState`, `BoardRules`, `AIProfileRuntime`, contexte AI explicite | `AIUtility` porte maintenant le noyau pur de base de `Ai_util.cpp` sans etat global AI : plages `AI_Group_Start_End`, sets/comptage, detection/proprietaire de monopoles, lots, maisons/hotels et shortage, railroads/utilities, `AI_Rent_If_Stepped_On`, table retail des frequences d atterrissage, proximite joueur/monopole, cout d unmortgage a 110 %, cash cows, revenu potentiel, rente moyenne recue/payee, depenses minimales, highest-rent/best-current-rent, `AI_Get_Monopolies`, comptage des 22 lots couleur encore a acheter et `AI_Get_Monopoly_Stage`, actifs liquides de proprietes et total worth. Les quirks retail documentes (railroads dans `AI_Is_Monopoly`, exclusion dans `AI_Own_Monopoly`, semantique mortgage, inversion historique `count_baltic`, calcul fiscal utilise par `AI_Average_Rent_Payed`) sont preserves/testes. Trois bugs sources explicites sont corriges/testes : affectation inversee de `AI_Highest_Rent_Square`, mauvais property-set employe pour le loyer adverse, et `AI_Best_Current_Rent` classe sur le loyer recu plutot que `AI_Average_Rent_Payed`. Le port couvre aussi `AI_Test_Sell_Houses`, le choix de mortgage a impact de rente minimal et le meilleur unmortgage sous plafond, y compris la decomposition d hotel sous penurie et le bord flottant retail de `mortgageCost * 1.1`. `AI_Square_Move_Reaction` est aussi porte : seuils Happy/Sad/Angry, cartes Chance/Community speciales, couts de reparation, mouvements recursifs et reaction prison. Deux bugs sources supplementaires sont corriges/testes : loyer adverse calcule avec le property-set du proprietaire (pas du payeur) et comparaison de l exposition prison au seuil dollar 500 (pas a l enum `AI_REACTION_ANGRY`=3). Les helpers de preparation au trading `AI_Num_Of_Monopolies_In_Set`, `AI_Num_Of_Monopolies_Between_Players`, `AI_Remove_Players_From_List` et `AI_XOR_Property_Set` sont aussi portes/testes. `AI_Get_Liquid_Assets` est maintenant porte sous forme pure avec cash + actifs hypothecables - dette explicitement injectee, ce qui preserve la semantique du `money_owed` local AI sans global. `AI_Get_Most_Liquid_Assets` et `AI_Cash_Available_After_Housing` sont aussi portes/testes, avec exclusion des joueurs off-board, protection des monopoles et simulation build/unmortgage retail. Le parser `AIProfile` fournit les heuristiques `*.AI` et `AIProfileRuntime` les branche maintenant aux joueurs locaux actifs par token/niveau. Le calcul des liquidites adverses accepte maintenant les dettes par joueur; l achat en cours est injecte dans la classification strategique. La boucle hors trade consomme desormais ces helpers pour construction/unmortgage, taxe/prison, achat/enchere et liquidation. L inventaire complet des utilitaires et la qualification des derniers raccordements runtime restent a terminer. |
| `Source/monopoly/Lang.cpp` | `LanguageCatalog`, `LanguageService`, `ResourceRuntime`, `DataBanks`, `LegacyTextIds` | `PORTED_PARTIAL` | `LANG_InitializeSystem`, `StartupExternalLanguage`, `GetLanguageString`, fallback, clean | lecteur DAT, index packed, UTF-16LE | Contrats existants conserves; startup publie les trois banques LANG partagees avec le registre DATA avant MESS. Les snapshots anciens conservent des chargements non caches apres remplacement/arret. Restent la migration des callers UI, fonts/mesures/impression et le formatage locale; textes retail bloques separement. |
| `Source/monopoly/TexInfo.cpp` | `TextureCatalog` | `PORTED_COMPLETE` | tableaux USA/Europe, recettes 8/16/14/22, overlays, 39 vues 2D, tags HMD | ResourcePaths, BMP, MeshRuntime | Catalogue CPU, ordre, coordonnees, noms et anomalie `CityHigh128 -> *_256.BMP` sont portes et testes. Les meshes/resolutions hors contrat produisent une erreur typee avant toute indexation. BoardTextureRuntime charge les BMP stock et overlays puis MeshRuntimeCache publie les substitutions; les UV et la voie embarquee sont preserves. |
| `Source/monopoly/Tickler.cpp` | `TimeStep`, `Messaging`, `UserInterface`, `GameQueueGate`, `AIMessageIngress` | `PORTED_PARTIAL` | `AdvanceTimeStep`, action unique par cycle, `ACTION_TICK`, `gameQueueLock/gameQueueUnLock`, frontiere `AI_ProcessMessage` | Timers, RULE, DISPLAY, AI | Routage local/broadcast, reset et compteur de verrou animation sont portes; le gate conserve le failsafe historique 15 s / 900 ticks. L ordre retail RULE -> UI -> AI est maintenant raccorde, y compris `ACTION_TICK`, avec filtrage local, ingress trade et proposition proactive autonome. Les consommateurs AI de depenses, taxe/prison, achat/enchere, tour, penurie et dette sont maintenant raccordes. Les notifications carte/configuration/resync, sauvegarde/restauration AISV et contexte d achat en cours sont maintenant raccordes. Les derniers handlers absents du switch AI retail sont soit drone/test-only, soit des no-op; le voice-chat reste hors AI. |
| `Source/monopoly/L_voice.cpp` | `VoiceChatLegacyContract`, `VoiceChatAudioRuntime`, `Gsm610Codec`, `VoiceChatPacket`, `Messaging`, `Engine` | `PORTED_PARTIAL` | contrat capture/lecture, catalogue compresseurs, format retail 11025 Hz / 8-bit / mono, PCM et GSM 6.10 WAV49 | SDL3 audio, libgsm, MESS | La famille est maintenant engagee explicitement : `VoiceChatLegacyContract` porte les globals/choix consommes de `L_voice` sans recreer DirectSound/ACM, expose les deux compresseurs modernes `No Compression` et `GSM 6.10`, verrouille le format capture retail 11025 Hz / 8-bit / mono et empeche `Engine` d ignorer silencieusement des options incompatibles. `VoiceChatAudioRuntime` assure deja capture/lecture SDL3, silence/post-silence, multi-sources, volume et lifecycle CHAT/DAT1/DATN/STOP; `Gsm610Codec` porte WAV49. Restent les capacites device explicites et les variantes ACM historiques non necessaires au chemin moderne courant. |
| `Source/monopoly/display.cpp` | `Display`, `GPUFrame`, `RenderSlots`, `LogicalViewport`, `World2DRenderer` | `PORTED_PARTIAL` | `DISPLAY_initialize`, `DISPLAY_tickActions`, `DISPLAY_showAll2`, `DISPLAY_destroy` | modules UD, renderer, assets | Ordre relatif, cycle desired/current et repere logique letterbox conserves. Le slot Overlay2D rend maintenant les feuilles bitmap du sequenceur via SDL_GPU dans le canvas logique 800x600; sprites/UI generiques, fonts et nombreux modules UD restent a porter. |
| `Source/monopoly/Userifce.cpp` | `UserInterface`, `LocalPlayers`, `RuntimeState`, `TimeStep`, `ExtendedInitialization`, `ResourceRuntime`, `IBarRuleState` | `PORTED_PARTIAL` | `MainExtendedInitialization`, `ProcessLibraryMessage`, `ProcessMessageToPlayer`, `ProcessPlayersUI`, `GameInProgress`, projection des etats UDIBar | RULE, DISPLAY, LANG, CHAT | Cinq banques core puis LANG avant MESS raccordes; absence/corruption bloque le startup avec erreur typee. Frontiere locale, `NOTIFY_GAME_STARTING`, pause et `GameInProgress` sont routes. Le split retail de `NOTIFY_ERROR_MESSAGE` 71 est maintenant exact : `numberC==6` conserve le warning generique, les autres variantes jouent `PB_HumanReplacedByComputerPlayer` en `WaitForAnyOldSoundThenPlay`. Le premier `NOTIFY_GAME_OVER` recu pendant `GameInProgress` joue `PB_PlayAgain` avec la meme politique et force le backdrop Main; les duplicats suivants restent silencieux une fois `GameInProgress` efface. `IBarRuleState` conserve les valeurs numeriques `IBAR_STATES` et projette les notifications de tour, paiement, achat/enchere, prison, cartes, unmortgage gratuit, taxe, placement/decomposition, controle BuySellMortgage et GameOver, avec reset exact apres les actions acceptees actuellement couvertes. `NotifyPlayerBuySellMort` reproduit maintenant l acquisition retail en `OtherPlayer` et la liberation vers `Nothing` sans perdre le joueur affiche. Le miroir UI projette maintenant aussi `NotifyCashAmount`, `NotifySquareMortgage`, `NotifyFreeParkingPot`, `NotifyJailCardOwnership`, `NotifyImmunityCount` et `NotifyFutureRentCount`; le cycle de partie restaure aussi le tracking IBar, purge les `CountHits` a `NotifyGameStarting`, selectionne un humain local a `NotifyGameOver` et de-pause sur `NotifyStartTurn`, puis synchronise `CurrentPlayer`, le joueur UI local et `FlashCurrentToken` sur `NotifyEndTurn`/`NotifyStartTurn` dans `GameState`, en plus de ownership/houses. Les deux decks mettent donc a jour leur `jailOwner` jusque l IBar, et les contrats hors trade reutilisent la semantique retail `AddUiImmunity` pour `countHits`. `NotifyClientResyncInfo` decode maintenant atomiquement le blob v1 de `RuleResync` (cash, ownership, mortgages, buildings, positions, jail cards, first-move et current player). Le cause `LoadGame` reproduit le coeur de `SetUpLoadedGame`: `GameInProgress`, retour Main, tracking IBar, purge des `CountHits` temporaires, baseline IBar Nothing/player 0 et reconstruction des resting idles depuis les positions chargees. `NotifyFreeUnmortgaging` rearme aussi le tracking IBar uniquement lorsqu un set non nul est fourni, comme le retail.  Les notifications retail qui appellent SetCurrentUIPlayerFromPlayerNumber synchronisent maintenant aussi le joueur UI local pour roll, buy/auction, pay, jail, card, free-unmortgage, tax, placement, decomposition et BSSM; NotifyPleaseRollDice choisit sa camera depuis le joueur du message plutot qu un CurrentPlayer potentiellement stale. Le focus trade est maintenant aligne sur `UDTrade.cpp`: `NotifyTradeEditor` selectionne exactement son editeur via `SetCurrentUIPlayerFromPlayerNumber`, et `NotifyTradeAcceptanceDecision` selectionne uniquement le slot `TradeB` reconstruit via `SetCurrentUIPlayerFromPlayerSet`; un spectateur garde donc `CurrentUIPlayer=Nobody` meme lorsque l IBar observe un autre trader distant encore present dans le player-set d acceptation. `NotifyTradeFinished` reproduit aussi le branchement historique `CurrentPlayer == numberB`: tracking RULE seulement dans ce cas, sinon affichage force du `CurrentPlayer` en `OtherPlayer` ou `OtherPlayerRemote`. Le roll-call generique `NotifyAreYouThere` emet maintenant les `IAmHere` uniquement pour les slots locaux demandes; le hint enchere reste differe vers `AuctionPennyBagsPlayback`, ce qui permet a RULE d attendre reellement les clients au lieu de s auto-acquitter. Restent notamment enchere detaillee et autres etats interactifs. |
| `Source/monopoly/UDAuct.cpp` | `AuctionUI`, `AuctionPlayback`, `AuctionPennyBagsPlayback`, `UserInterface`, `RuleAuction` | `PORTED_PARTIAL` | projection notifications, input billets, layout, playback statique et state machine graphique Pennybags | DISPLAY, LocalPlayers, SequencePlayback, RULE auction, fonts/audio | `AuctionUI` projette `NotifyNewHighBid`/`NotifyAuctionGoing`, conserve former view, third-going et roll-call, calcule les panneaux retail 201/134 et les sept billets 500/100/50/20/10/5/1, puis route `ACTION_BID` pour les humains locaux avant UDBoard comme `Userifce.cpp`. `AuctionPlayback` publie transactionnellement le bottom bar `TAB_anbg00`, deed `index+28*city` ou house/hotel, panneaux couleur, trays, bid plates et tokens aux priorites/positions retail; 4 joueurs donnent 18 objets statiques et 5 joueurs 22. `AuctionTextPlayback` porte les quatre surfaces GRAFIX runtime restantes : libelle localise `TMN_AUCTION_CURRENT_BID` + montant a (20,280), noms centres en noir, mises centrees en blanc et cash restant centre en noir, aux dimensions/offsets/priorites retail. Les bitmaps RGBA8 sont alimentes par `FontRuntime`, rafraichis sans redemarrer les sequences lorsque seul le contenu change, puis montes/demontes transactionnellement avec les panneaux actifs. `MoneyFormat` reproduit en commun `UDPENNY_ReturnMoneyValueBasedOnMonatarySystem` : dollars forces pour l edition USA, 14 systemes, facteurs x1/x10/x20/x100 et prefixes/suffixes historiques. `AuctionPennyBagsPlayback` porte les CNK `an01..17` et l automate Intro/Instructions/StartBidding/Begin/Idle/NameHighestBidder/GoingOnce/GoingTwice/Sold/Congrats/EndAuction, detecte `endTime <= sequenceClock`, preserve le skip des explications apres la premiere enchere, repond au roll-call par `ACTION_I_AM_HERE` pour tous les joueurs locaux attendus avec serial exact et preflight FIFO, puis revient a `formerView`; ressource/FIFO/sender invalides sont transactionnels. Les voix Pennybags sont maintenant synchronisees avec chaque changement d animation : Intro/Instructions/OpeningBid/GoingOnce/GoingTwice/Sold/Congrats utilisent le catalogue general, tandis que NameHighestBidder, zero-bid Sold et building Congrats utilisent les offsets specifiques retail; le garde anti-relecture conserve `AuctionPennyBagsSound != AuctionPennyBagsDesiredSound`. Les surfaces texte auto-contenues de `UDAuct` sont donc raccordees; restent surtout les dependances partagees fonts/word-wrap hors enchere et la qualification runtime differee. |
| `Source/monopoly/UDBoard.cpp` | `Display`, `BoardCameraController`, `BoardGeometry`, `BoardBackdropPlayback`, `BoardOwnershipHighlight`, `BoardLightingController`, `World3DRenderer` | `PORTED_PARTIAL` | `UDBOARD_SetBackdrop`, `DISPLAY_UDBOARD_Show`, backdrop 2D runtime, ownership/mortgage bars, presets/camera waiting et interpolation `TickActions` | assets plateau, renderer, PC3D | Valeurs des huit ecrans, etat initial invalide, commit differe et mapping Main/Portfolio/Trade portes. `game3DOn` est maintenant distinct de `board3DOn`, rendant le chemin Main 2D reellement atteignable. `BoardBackdropPlayback` reproduit le cache legacy : quatre surfaces DataNative 800x450 pour Main, une surface partagee 400x225 pour Portfolio/Trade, `BMP_mybs*`/`BMP_mybss*` par camera, priorite 10, `StartCXYSlot`, offsets viewport et remplacement Stop/Start/Move transactionnel. Pour USA, les deux familles DataBMP utilisent maintenant l index retail `camera + 39*city` pour les villes 0..10; le cache Main identifie explicitement `(city,camera)` et une ville invalide est rejetee avant allocation ou publication. Les 39 `CameraAngles2D`, le slot waiting unique, l interpolation accel/decel sur 75 ticks, force-interrupt hors 3D et revalidation apres override sont portes/testes. `UDBOARD_ProcessMessage` couvre le controle manuel souris du viewport 3D : deltas logiques C/D, bouton gauche, bouton droit/Ctrl pour l orbite verticale, centre (243,10,243), mouvement initial lineaire 75 ticks, zoom 100..1200 et lock libere strictement apres 1200 ticks avant revalidation du preset. `TickActions` couvre aussi le DemoMode historique : seuil strict >90 s, cycle sequentiel des 39 cameras, durees 51..118 ticks, sortie sur activite et retour a la camera originale. Le floating idle est porte comme un Bezier interruptible de 100 ticks, arme une fois apres un mouvement standard, supprime sur TopDownSquare et module son amplitude selon TokenAnimStack. Les ownership/mortgage bars sont planifiees depuis `GameState` puis jouees transactionnellement : Main 2D utilise les DataUAP `DAT_BOARD/DAT_BOARD2` camera/propriete/couleur avec origine `StartCXYSlot`, Main 3D/Portfolio/Trade utilisent les HMD ownership/mortgage, priorite `12+square` et transformations exactes des quatre cotes. Pour `USA_VERSION`, `UDBOARD_CompileBackdrop` retourne avant les overlays ville/systeme; le chemin custom `city<0` depend encore des DataID externes `g_aid2DBoards` et reste differe. La selection 3D USA suit maintenant exactement `city == 0 ? HMD_boardmed : HMD_board_citymed`, y compris le futur sentinel custom non nul. Le bloc `LIGHTING` est maintenant porte sans simulation : `BoardLightingController` reproduit ambient 0.53/0.84, les deux directionnelles, spotlight active seulement avec board 3D, position source Y+200, range/attenuation/falloff/theta/phi, focus accelerate-in-out sur 210 ticks, suivi du token anime au ratio 0.3 via `TokenPoseTracker`, idle strict >25 s avec pas de 3 unites/tick et convergence couleur joueur de 0.003/tick vers 75 % des six RGB retail. `World3DRenderer` transporte world position/normale, applique ces lumieres dans HLSL/MSL et utilise des DXIL regeneres; des readbacks D3D12 verrouillent directionnelle, spotlight dans le cone et rejet hors cone. Le contrat `viewportBackgroundFillOn`/`LE_REND3D_ClearBeforeRender` est aussi raccorde : le flag suit `game3DOn` lors de `DISPLAY_UDBOARD_Show` et `GPUFrame` ne reblitte `BackGround.bmp` dans le viewport que lorsque ce fill est actif, preservant le fond 2D en mode plateau 2D. Les backdrops statiques `BMP_sybkgrnd`, `BMP_rnbacknd` et `BMP_auctiona` ne sont pas presents dans les ressources modernes et restent donc explicitement differes; `g_bOptionsButtonsOn` est un contrat UDOpts/menus hors IBar sans consommateur moderne actuel, il n est pas materialise comme etat mort. `UDBOARD_SelectAppropriateView` est maintenant porte pour ses deux callers retail actifs : `VIEW_ROLLDICE` reproduit la formule 15-tuiles et le fallback `square>39 -> 10`, tandis que `VIEW_JAILCHOICE` force `VIEW2D17_CORNER_JAIL`; `UserInterface` applique ces choix sur `NOTIFY_PLEASE_ROLL_DICE` et `NOTIFY_JAIL_EXIT_CHOICE`. `UDBOARD_PreLoadBackdrop` n est pas porte car son seul caller de `UDPieces.cpp` est compile sous `#if 0`. Les allocations de surfaces/lumieres et leur destruction ont desormais des owners modernes (`BoardBackdropPlayback`, renderer/controller lighting), donc aucun handle DirectDraw/Direct3D legacy n est recree pour le lifecycle. Les manques UDBoard auto-contenus actifs sont ainsi fermes; restent les dependances externes deja identifiees (`g_aid2DBoards`, DAT statiques, UDOpts et effets camera specifiques portes avec UDPieces). |
| `Source/monopoly/UDIBar.cpp` | `IBar`, `IBarLayout`, `IBarBackdropPlayback`, `IBarPropertyPlayback`, `IBarCardPlayback`, `IBarJailCardPlayback`, `IBarBankPlayback`, `IBarCameraButtonPlayback`, `IBarCurrentPlayerPlayback`, `IBarRuleState` | `PORTED_PARTIAL` | initialize/destroy/tick/show/process, filtrage joueurs, backdrop, boutons globaux et d'etat, pion courant, titres/deeds, cartes Chance/Community Chest | LocalPlayers, DISPLAY, UserInterface, SequencePlayback, Overlay2D, assets/fonts | Overlay2D couvre backdrop joueur/banque, icone Banque, pion courant, boutons globaux, decisions tour/prison/taxe/achat, Bankrupt, BSSM Build/Sell/Mortgage/Unmort, AucHouse/AucHotel, PlaceHouse/PlaceHotel et TradeAcc/TradeCnt/TradeRej. Les acknowledgements RULE alimentent maintenant le visuel transitoire `Pressed`, consomme une seule fois par le playback. Les 28 titres DAT_MAIN et leur mouseover deed sont raccordes : delai strict `>36`, priorite 1003, `StartXY(540,130)`, variantes normale/hypothequee DAT_LANG2. Le popup Buy/Auction reutilise le deed normal USA, conserve `PropertyBuyAuctionDesired` comme etat separe, priorite 1002 et placements source `StartXY(20,110)`, `(560,110)` ou `(594,110)` selon Main gauche/droite ou Trade/Portfolio; le gate par identite conserve aussi l absence de repositionnement quand la vue change sans changer de deed. Les cartes USA suivent `Off -> DeckOut -> CardIn -> FaceIn -> Idle -> Out`, priorite 1005, DropDropFrames, `StayAtEnd`/`Stop`, 39 offsets camera DAT_MAIN et les quatre familles DAT_LANG2 par deck; Main reste a y=0 et Portfolio/Trade a y=136. `NotifyPickedUpCard` mappe Chance/Community en index 0..31 et `CardSeen`/`NotifyPutAwayCard` declenchent la sortie. Les score boxes bitmap sont raccordees via `IBarScoreStripPlayback` : tokens `TAB_inpsa`, barres couleur large/petite `TAB_inpsl0`/`TAB_inpss0`, barreaux `TAB_inpbj`, priorites historiques, hover +1 et positions `UDIBAR_Calc_ScoreX`; le bank hover pilote maintenant `TAB_bank` entre y=560/561. Le snapshot cash/nom conserve aussi `LastPlayerScoresPrinted`, le gate de 20 ticks et la direction Up/Down. `IBarScoreTextPlayback` consomme maintenant ce snapshot dans de vraies surfaces runtime : noms a (57,5), cash monetaire aligne a droite a y=18, tailles/poids 11/600 ou 7/400, reduction large-currency du cas `system==2`, largeur large/petite derivee du strip, priorite 305 et hover vertical +1 comme `UDIBar.cpp`. Le contenu bitmap se rafraichit sans redemarrer la sequence quand seul nom/cash change. Les deux cartes prison joueur `TAB_indsgoojc01/02` sont maintenant des feuilles DAT_LANG2 transactionnelles aux priorites 256/257 et positions (740,506)/(750,525), visibles sur Main/Trade selon `cards[deck].jailOwner`; le cas BankPlayer est maintenant porte par `IBarRuntimeTextPlayback`, avec bases `TAB_inhouse`/`TAB_inhotel`, decompte retail des maisons/hotels restants, nombres `%*i` largeur 3 centres et priorites/positions 256/257 a (740,506)/(750,525). Les entrees BSSM Build/Sell/Mortgage/Unmortgage jouent maintenant leurs Host Comments retail respectifs (`PB_PlayerClicks_BuyHouseHotel`, `PB_PlayerClicks_SellHouseHotel`, `PB_PlayerClicks_Mortgage`, `PB_PlayerClicks_UnMortgage`) en `SkipIfOldSoundPlaying`, exactement au changement de sous-etat IBar. La selection score bar est maintenant fidele a `IBarStateTrackOn` : clic joueur RULE restaure le tracking, humain local/Banque entre en `OtherPlayer`, distant/IA en `OtherPlayerRemote`, avec joueur inspecte persistant a travers les changements RULE et retour Done exact depuis les sous-etats BSSM/DeedActive. Le meme mecanisme est expose par `inspectPlayer` pour les transitions retail non issues d un clic, notamment la fin de trade qui force `OtherPlayer`/`OtherPlayerRemote` lorsque le tracking RULE ne doit pas etre rearme. Le bouton Camera est maintenant aussi un controle reel, pas seulement un playback : hit global independant des masques RULE, joueur IBar inspecte, cycle source Top -> 3 tiles -> 15 tiles -> Top, trois variantes Top, fallback prison, avance droite/Ctrl modulo 39, feedback Pressed et handoff one-shot d un preset quand le lock souris manuel est actif. Le bouton global Status/Main est maintenant raccorde au meme niveau : son hotspot reste independant des masques RULE pendant une partie, `Main -> Portfolio` demande le feedback Status, tandis que `Portfolio/Trade -> Main` demande le feedback Main, exactement comme le `IBAR_Status` retail. Les atlas boutons CNK_iyaaf/CNK_iycaf, priorites 999/1000/1001/1002, IBarIsStable, IBAR_JustChanged, layout/hit masks et sous-etats locaux restent testes. Le mode GameOver n est plus seulement visuel : General2 envoie ACTION_NEW_GAME depuis AnyLocalPlayer comme le retail, et Main/Exit pose maintenant le flag Exit puis ouvre la confirmation Escape retail au lieu de quitter immediatement. `NotifyCashAnimation` projette maintenant aussi le montant temporaire, son tick, son serial same-tick et le force-update de la barre, avec suppression exacte pendant RaiseMoney/HotelDecomposition. `IBarRuntimeTextPlayback` rend desormais les quatre surfaces GRAFIX dynamiques retail : stocks Banque maison/hotel, nom du joueur courant et `ButtonBarMessage`; CashAnimation garde le TTL strict de 4*60 ticks, RaiseMoney/HotelDecomposition restent epingles tant que leur etat RULE est actif, et les couleurs/alignements/coordonnees/priorites suivent `UDIBar.cpp`. Les SFX cash sont consommes par `UDSoundRuntime`. `NotifyHousingShortage` reproduit aussi le commentaire Pennybags actif de `UDIBar.cpp`: uniquement lorsqu un humain local peut participer, sur le premier countdown `numberD==0`, avec le `static lastCount=2` historique qui elimine le doublon initial et persiste au niveau process. `NotifyPleasePay` et la maintenance periodique de `RaiseMoney` reproduisent maintenant `PB_RaisingMoneySuggestion`: seuil strict `tick > last+40*60`, IBar visible et uniquement le slot retail `SoundToPlayPostLock` vide (une voix deja active n interdit donc pas de poster le rappel), puis rearmement du timestamp. `NotifyJailExitChoice` joue aussi le commentaire roll-or-pay uniquement pour un humain local pouvant encore tenter les doubles: USA via `PB_JailPayMoneyOrTryForDoubles`, Europe via le WAV specifique `pb235`, resolu comme l alternate 2 de la range `PB_JailPayMoneyOrTryForDoubles` (`pb233..pb235`, `DAT_LANGDIALOG`, tag `0x0A26`), tous deux en `WaitForAnyOldSoundThenPlay`. `NotifySquareOwnership` et `NotifySquareHouses` alimentent maintenant aussi la projection de case requise par BSSM. Les notifications BSSM jouent maintenant aussi leurs SFX retail autonomes: WAV_build (DAT_MAIN 0x084B) sur hausse des maisons, WAV_unbuild (0x085B) sur baisse/egalite comme le source, et Click sur mortgage/unmortgage, uniquement pour un owner joueur valide comme UDSOUND_HandleBSSMSoundNCam; la camera BSSM conditionnelle est maintenant portee dans Display/BoardCameraController: Build/Sell peuvent ouvrir la chaine quand la camera est idle/floating et Camera option active, Mortgage/Unmortgage la prolongent seulement si une chaine BSSM existe deja; le prior view est preserve, les etats 0->1->2 suivent le demarrage reel des interpolations, et le retour n arrive qu apres le seuil retail strict >5 s si aucune autre camera n a repris desiredBoardCamera. NotifyDecomposeSale publie en plus le nombre de maisons restant a vendre dans le contrat de barre IBar; `IBarRuntimeTextPlayback` le consomme avec le suffixe USA ` houses`, le rouge retail et l alignement a droite. Une hausse du nombre de maisons reproduit le commentaire retail `PB_PlayerBuiltFirstHouse`: timer process-level par joueur, seuil strict `tick > last+30*60`, commentaire uniquement tant que le timestamp Buy vaut encore zero, puis rearmement sans second commentaire, en `SkipIfOldSoundPlaying`; une vente reste silencieuse. |
| `Source/monopoly/UDOpts.cpp` | `OptionsUI`, `OptionsFilePlayback`, `OptionsNavigationPlayback`, `OptionsOptionPlayback`, `OptionsTogglePlayback`, `OptionsHelpPlayback`, `OptionsSaveRuntime`, `OptionsSavePlayback`, `UserInterface`, `IBar`, `IBarBackdropPlayback` | `PORTED_PARTIAL` | entree Options, ecran File, navigation, toggles, New/Exit, confirmation Escape, cinq slots Load/Save, persistance `.msv/.sgd`, saisie description | DISPLAY, IBar, SequencePlayback, FontRuntime, RuleSave, filesystem | Le bouton global Options et les ecrans File/Option/Help restent raccordes comme precedemment. New/Exit utilisent le flux Escape retail avec Yes/No, timeout 10 s et Credits. Load/Save est maintenant fonctionnel : `OptionsSaveRuntime` reproduit les cinq slots fixes `game1..game5`, le repertoire `savegame`, le blob `.msv` et le sidecar `.sgd` de 476 octets (`GameFileDesc[100]` UTF-16, city/system, 42 `game_earnings`, custom-board name), selection de slot, edition Unicode, Backspace/Enter et limite de largeur 446 px. Save envoie `ACTION_GET_GAME_STATE_FOR_SAVE`, attend `NOTIFY_GAME_STATE_FOR_SAVE` puis persiste; un echec d action purge le pending save. Load relit le blob et envoie `ACTION_SET_GAME_STATE` avec les marqueurs retail version 1, puis restaure city/system/earnings avant le resync RULE. `OptionsSavePlayback` rend les titres USA/Europe, les cinq backgrounds `CNK_snfileb0/snfilegl`, descriptions via `FontRuntime` sur runtime bitmaps et OK/Cancel localises aux positions retail. Le chemin custom-board externe a maintenant un owner portable complet : inventaire `custbrds`, verification du DWORD de securite de l editeur, `city=-1`, `customRoot` 2D/3D et dialogue runtime. Une sauvegarde custom dont le repertoire externe n est plus resolvable conserve le fallback historique vers le plateau standard. Credits est rendu/scrolle par `OptionsVisualPlayback`; Quick Help charge, wrappe et pagine le fichier retail, et Full Help conserve le chemin systeme Windows quand le fichier existe. Les metriques exactes restent dependantes des differences GDI/FreeType et certains libelles Option restent a qualifier visuellement. |
| `Source/monopoly/UDPieces.cpp` | `PiecePlacement`, `PieceRuntime`, `PieceCamera`, `PieceMovePlan`, `PieceMovePlayback`, `PieceMoveIngress`, `PieceInterpolation`, `PieceJailPlan`, `PieceJailPlayback`, `PieceIdleTransition`, `PieceIdlePlayback`, `PieceIdleDisplay`, `PieceBuildingDisplay`, `PieceShadowDisplay`, `DiceIngress`, `DiceDisplay`, `BoardGeometry` | `PORTED_PARTIAL` | orientation tokens/repos, maisons/hotels, pose runtime, cameras, TokenAnimStack, game-queue movement ingress, GoToJail 1..14, transitions idle centre<->repos, idles persistants, batiments et cycle des | `SequenceRuntime`, `SequencePlayback`, GameQueueGate, BoardCameraController, PC3D moderne, BoardGeometry, RULE | Placement/orientation, pose runtime, cameras 3/5/15, `PlanMoveAnim`, faillite/victoire, interpolation Bezier, paddywagon GoToJail 1..14, transitions centre/repos et `Player3DTokenShown` sont portes/testes. Maisons/hotels utilisent HMD 4/5 et priorites historiques. Le lancer de des 3D/idle 3D utilise les tables 6x6, priorite 100, seuils stricts +36/+91/+121, queue lock et override camera. Le cycle 2D DAT_MAIN 0x0096..0x009C est raccorde au moteur : `CurrentDiceID`, `CurrentBobDice`, Stop/Start, notification consommee apres le de gauche, bobbing -35/-11, DropDropFrames a droite et LoopToBeginning. Un readback D3D12 prouve les pixels reels aux deux positions. `PieceShadowDisplay` porte aussi `CNK_shadowa..CNK_shadowk` a la priorite `Generic3DPriority+player`, suit la pose runtime via `TokenPoseTracker`, force le plan d ombre a Y=1, respecte board visible + lighting + game3DOn, masque exactement le passager au paddywagon state 8 et conserve l exception off-board tant que la sequence token courante reste active; start/move/stop utilisent DropFrames + StayAtEnd et un token hors plage est rejete transactionnellement. Les boucles victoire et la faillite non-locale declenchent maintenant les voix de pion retail via `UDSoundRuntime`; la faillite humaine locale joue desormais `PB_Bankrupt`, tandis que toute victoire joue `PB_GameOver` au demarrage du plan, avec la politique Wait retail. Restent quelques reactions/commentaires vocaux encore non raccordes et des aspects lifecycle/options externes. |
| `Source/monopoly/UDPsel.cpp` | `PlayerSelection`, `PlayerSetupFlow`, `LocalPlayers`, `IBar` | `PORTED_PARTIAL` | phases, noms, tokens, humains/IA, add/remove/start, SelectCity | UserInterface, Messaging, DISPLAY | Commit de phase limite a `DISPLAY_UDPSEL_Show`, refresh one-shot, reset zero-joueur et initialisation de la premiere notification non nulle portes. Les notifications NamePlayer/AddLocalPlayer/PlayerDeleted retablissent maintenant explicitement le suivi RULE de l IBar comme `IBarStateTrackOn = TRUE` dans le retail, en abandonnant toute inspection locale devenue stale. `SelectCity` couvre maintenant USA et Europe. USA conserve Classic/Left/Right/Next, wrap 0..10 et monnaie US. Europe porte les 12 pays retail, le pays par defaut derive de `LanguageId`, les hotspots pays/monnaie exacts, les choix `[pays, langue, euro]`, le skip des doublons monetaire, le wrap 0..11 et le commit atomique `Display::State.city` + `Display::State.system` avant `StandardOrCustomRules`. Le choix Standard/Custom est maintenant porte : hotspots retail exacts, Custom -> CustomizeRules, et Standard applique le preset source complet (dont houseShortageLevel=6) puis emet ACTION_ACCEPT_CONFIGURATION depuis le premier humain local avec protocole v1 et blob GameOptions. Le moteur decode/valide ce blob transactionnellement comme Rule.cpp, conserve les options courantes sur payload invalide, gere la compatibilite pre-v1, reset les acceptations lors d un changement final, memorise le proposer et rediffuse NotifyProposedConfiguration avec le blob courant. La projection UI recopie ce blob sans corruption en cas d erreur et SetCurrentUIPlayerFromPlayerSet selectionne le premier humain local du bitmask. CustomizeRules porte maintenant la semantique des 21 controles retail : 2 choix houses-per-hotel, 15 groupes numeriques a quatre choix et 5 toggles, avec valeurs source exactes, Restore Standard, preset Short Game (Standard + housesPerHotel=4 + deux proprietes gratuites), rejet transactionnel des choix invalides et bouton Okay fixe [337,450]-[464,486]. Chaque modification hote emet ACTION_ACCEPT_CONFIGURATION v1 avec le blob GameOptions et numberD=1; Okay emet l acceptation finale numberD=0 depuis le premier humain local. Les hotspots CustomizeRules dependant de `LANG_GetStringWidth` sont maintenant derives des metriques `FontRuntime` avec les colonnes/largeurs/pas retail; le rendu utilise en plus un wrapper UDPsel dedie qui coupe uniquement sur les espaces, preserve `_` et reproduit le hard-break du premier mot au lieu des quirks `CHAT_WordWrap`. SelectPlayer porte maintenant la liste historique pure : maximum 100 noms, filtrage des joueurs deja presents, troncature a 10 caracteres, pagination par 8 avec wrap, hotspots retail exacts New/More/Card1..8, cartes absentes inactives, New -> EnterName et carte -> SelectToken humain. `PlayerSelectionHistory` porte maintenant la lecture/ecriture atomique de l INI historique, les `Wins`/`GreatestNetWorth`, l enregistrement des nouveaux joueurs, le garde anti-double-credit par partie et le tri HiScore; `PlayerSelectionPlayback` rend les huit lignes HiScore via `FontRuntime`. `udpsel_StartSoundForPhase` est maintenant porte pour toutes les phases actives : Welcome one-shot process-lifetime en `ClipOldSoundIfPlayingWithLock`, HiScore/LocalOrNetwork en `WaitForAnyOldSoundThenPlay`, puis commentaires SelectPlayer, EnterName, token humain/IA, les trois Summary, AI strength, SelectCity et Standard/Custom en `ClipOldSoundIfPlaying`; RemovePlayer/CustomizeRules restent silencieux et le couple playing/desired evite les replays. Les anciens blocages UDPsel lies aux fonts sont donc fermes pour EnterName, SelectPlayer, HiScore, SelectCity et CustomizeRules; restent les phases/branches suivantes a auditer dans l ordre source. |
| `Source/monopoly/UDSound.cpp` | `UDSoundRuntime`, `TokenVoiceCatalog`, `PennybagsCatalog`, `AudioRuntime`, `Engine`, `OptionsUI`, `PieceMovePlan` | `PORTED_PARTIAL` | SFX UI/cash, musique `WAV_m1..m5`, `UDSOUND_TokenGenericPlaySound`, politiques clip/skip/wait | DATA WAV, DAT_LANGDIALOG, SDL3 audio, DISPLAY/Options, UDPieces | SFX Warning/Click/Cash et musique m1..m5 sont portes. Le catalogue Token Voices est derive automatiquement des tables retail USA/Europe : 73 categories x 11 pions x 2 editions, avec base WAV + nombre d alternates exacts. Les cinq politiques `Clip`, `Clip+Lock`, `Skip`, `Wait`, `Wait+Lock` et le slot poste unique sont reproduits, y compris le quirk retail ou une voix postee apres release n est pas reverrouillee. Token Voices est un toggle reel; `UDPieces` raccorde la voix de victoire et GiveUp/Bankrupt hors humain local. Pennybags/Host Comments utilise maintenant le meme arbitre parlant que Token Voices, avec catalogue USA/Europe genere depuis le retail, alternates exacts, gain 70 %, cinq politiques clip/skip/wait et slot poste partage. Les annonces specifiques de proprietes USA/Europe sont raccordees avec leurs banques DATA historiques et le watch/queue-lock retail. La lecture vocale des cartes USA est raccordee au demarrage DeckOut via WAV_pb186 + index. Les 32 cartes Europe sont maintenant resolues par les tables retail de `UDPENNY_GetCorrectCardWav`: Chance conserve monnaie/pays, variantes Euro/default et cas irreguliers 1/7; Community Chest conserve les 15 variantes monetaire par systeme et la carte pays partagee `cycoal05`. Le fallback custom-board derive de `LanguageId` comme `iLangId-2`, et le caller fournit directement `DISPLAY_state.city/system`. La sirene Paddywagon est deja raccordee via `UDSoundRuntime::siren` aux deux variantes DAT_3D et au gain retail 74 %. La musique Credits est maintenant portee aussi : `WAV_credit_a` joue une seule fois a 20 %, puis `WAV_m1` prend le relais en boucle, independamment du toggle Music comme dans `UDSOUND_BackgroundMusicUpdate`, et la sortie de Credits restaure la selection musicale normale. Restent les commentaires Pennybags des autres ecrans et le rendu des noms de morceaux dependant de `L_Fonts`. |
| Source/monopoly/UDStats.cpp | StatsUI, StatsPlayback, StatsBankPlayback, StatsCalculatorPlayback, StatsCalculatorDeedPickerPlayback, StatsCalculatorLogic, StatsCalculatorUI, StatsPlayerPlayback, StatsPlayerCashPlayback, StatsPlayerAuxPlayback, StatsFutureImmunityUI, StatsFutureImmunityPlayback, StatsFutureImmunityTextPlayback, StatsDeedPlayback, StatsDeedFloaterPlayback, StatsDeedFloaterTextPlayback, StatsDeedBarPlayback, StatsDeedValueTextPlayback, UserInterface, Engine, AIUtility, BoardRules | PORTED_PARTIAL | categories Player/Deed/Bank, hotspots category/sort, tris Player/Deed, Bank Houses/Properties, playback backgrounds/barres/boutons | miroir UI, DISPLAY, SequencePlayback, fonts/assets, compteurs liabilities, journal account history | Le noyau interactif Portfolio est maintenant porte sans simuler le rendu legacy : les trois categories conservent chacune leur dernier tri comme last_psort_result/last_dsort_result/last_bsort_result, avec hotspots retail exacts (categories 54+93*i, sorts 495+70*i, bords Win32 exclusifs). Player porte Order of Turn, Net Worth via AI_Get_Total_Worth/ai::totalWorth, High Future Value via AI_Potential_Income/ai::potentialIncome et Cash, avec ordre descendant et ties source. Deed porte Purchase Value descendant, Owner stable ascendant comme la bubble sort retail, Estimated Current Value via AI_Rent_If_Stepped_On et Most Valuable via game_earnings. Bank porte les deux datasets autonomes : Houses/Hotels reproduit les comptes par joueur et l inventaire restant a partir de maximumHouses/maximumHotels/housesPerHotel; Properties projette chaque deed ownable en Available/Sold/Mortgaged avec priorite mortgage identique au source. StatsPlayback porte maintenant la couche sequence autonome : background Portfolio CNK_dndsbg00 priorite 10, background Deed TAB_dndsdpbg priorite 20 a StartXY(5,229), les quatre backgrounds Bank CNK_byhhbr00/CNK_byprbr00/CNK_bylibr00/CNK_byahbr00 priorite 50, doubles barres Category/Sort aux priorites 54/154, et les 3+4 boutons Idle/Return/Press avec tags et priorites retail exacts. Les changements de categorie conservent le Return precedent, pressent la nouvelle categorie et remplacent atomiquement les quatre familles de sorts; les changements de sort font le meme handoff sans commandes redondantes. Liabilities reste explicitement indisponible cote compteurs car g_Card50Counter/g_Card200Counter est une statistique externe non possedee par GameState; son habillage autonome USA porte toutefois maintenant les deux cartes TAB_cycad05/TAB_cyyo07 a priorite 51, positions (365,275)/(365,358) et scale 0.3. Account History reste differe car le retail lit/ecrit acchist.txt. UserInterface reset la projection avec le miroir RULE, initialise le default Player/Turn des l entree Portfolio et refresh les donnees live pendant cette vue; Engine synchronise le playback et le reset avec le reste de la scene. StatsBankPlayback rend maintenant aussi les deux datasets Bank internes : Houses/Hotels publie les barres CNK_bnhh00+colour et toutes les instances maison/hotel aux priorites retail 55/56 avec positions calculees par couleur, tandis que Properties publie les 28 deeds PAT aux coordonnees historiques plus les panneaux SOLD/MORTGAGED centres a priorite 60. Le playback reconstruit transactionnellement les doublons a meme DataId/priorite grace au StartXY atomique du sequenceur. StatsCalculatorPlayback porte aussi la couche autonome toujours visible du Portfolio : CNK_sncalbg et CNK_sntextbg a priorite 50, dix touches numeriques, Enter et huit fonctions a priorite 100, avec fallback des tags Enter des deux banques LANG2 historiques; aucun rendu texte ou resultat n est simule. StatsCalculatorLogic porte maintenant les huit calculs actifs appeles par UDStats_GetFunctionResult : Odds, Net Worth, Future Value To You/Other, Maximum Expense, Current Income (branche retail ayant remplace Minimum Expense), Maximum Revenue et Potential Income (branche ayant remplace Minimum Revenue), en reutilisant les contrats AI deja portes et les quirks de parcours du source. Le formatage monetaire/texte et le workflow visuel multi-etapes restent separes. StatsCalculatorUI porte maintenant le workflow actif : hotspots exacts des 8 fonctions et des 11 touches, hover ROLL_OVER/FIRST_STEP, choix deed/token selon la fonction, popup-deed hit grid 7 colonnes a (200,230)+(10,10), liste tokens 42x20 depuis (514,79), Future Value To Other en trois etapes, branches 4-7 forcees a turns=1 comme le code June 25, Clear index 10 et reset automatique en quittant Portfolio. StatsCalculatorPlayback suit cet etat : fonction active en Press, touche numerique retail pressee (1 pour les branches 4-7, index joueur pour Future Value To Other), touches masquees pendant le picker Player et tokens TAB_inpsa affiches a scale 0.8 via le contrat StartCXYSR retail. StatsCalculatorDeedPickerPlayback publie les 28 cartes normales DAT_PAT du picker deed a priorite POPUP 610, aux positions absolues equivalentes au composite retail (210+57*col, 240+45*row), et suit maintenant le MouseMoved retail pour afficher uniquement la grande carte ville courante survolee a StartXY(600,-2), priorite 610, sans redemarrer les 28 cartes lors d un changement de hover; seul le fond noir GRAFIX du popup reste differe. Le resultat numerique est conserve sans inventer le rendu GRAFIX/FONTS. StatsPlayerCashPlayback restitue aussi TAB_snpscsh1 comme couche autonome priorite 501, a l offset local (5,30) dans chaque boite Player visible, avec le meme tri et filtrage BSSM que le composite OthersID retail. StatsPlayerPlayback porte maintenant les boites couleur Player et les deeds possedes : layouts retail 1-4/5-6 joueurs, tri Player conserve, compression des colonnes par groupes, positions/priorites STATUS_DEED_Priority+IBARPropertyBarOrder, faces normales/mortgagees et filtres Build/Sell/Mortgage/UnMortgage limites au joueur humain local comme dans UDStats. StatsFutureImmunityUI porte la projection interactive du popup Future/Immunity : rectangles reels publies par le playback, priorite Immunity avant Future, lignes CountHits bornees comme les tableaux retail, scroll g_fniIndex et regles historiques d activation Up/Down. StatsFutureImmunityPlayback porte le chrome autonome du popup : CNK_tydisplr priorite 610, TAB_tndisbrd a (601,3) priorite 611 et fleches Down/Up DAT_MAIN priorite 620 avec les offsets StartXY retail (25,-214)/(25,-220). `StatsFutureImmunityTextPlayback` ajoute maintenant le panneau texte runtime 197x223 a (601,3), titres Futures/Immunities avec nom joueur, colonnes Property/Hits Remaining, noms de cases localises, comptes `%2i`, clipping du nom avant la colonne hits et rerasterisation live lors du scroll sans redemarrer la racine sequence. StatsPlayerAuxPlayback porte les deux cartes Get Out of Jail ainsi que les indicateurs Future/Immunity derives de CountHits, avec tags USA/Europe, dimensions bitmap reelles, positions retail et le meme filtrage local BSSM. StatsDeedPlayback porte la grille Deed autonome : 28 deeds PAT ordonnes selon deedOrder, grille retail 7 colonnes a (22,234) avec pas 110x54, faces normales/mortgagees, compaction Most Valuable quand gameEarnings vaut zero et filtrage local Build/Sell/Mortgage/UnMortgage identique au source. StatsDeedFloaterPlayback porte maintenant la partie sequence autonome du survol Deed : hit-test 36x42 sur la grille reellement affichee, cadre TAB_deedinfo priorite 600 et grande carte ville courante priorite 602, positionnes a gauche ou a droite comme UDStats_DisplayDeedInfo. `StatsDeedFloaterTextPlayback` porte la couche GRAFIX/FONTS priorite 601 : surface runtime 400x235, Owner/Current Rent/Game Earnings/Future Value To You localises, nom Banque/joueur avec reduction de taille retail, montants via `MoneyFormat`, word-wrap du libelle Future Value et mise a jour live sans redemarrer la racine. StatsDeedBarPlayback porte la couche de fond des value bars : TAB_dndsdp00+couleur pour Owner et TAB_dvaldisp pour Purchase Value / Estimated Current Value / Most Valuable, a deed.x+42/deed.y+13. `StatsDeedValueTextPlayback` complete les trois tris numeriques avec des surfaces runtime 52x13, police retail 8/500, gris (200,200,200), montants `MoneyFormat` sans symbole, alignement a droite, refresh live des valeurs et compaction Most Valuable quand gameEarnings vaut zero; l overlay texte se place juste au-dessus du fond sequence afin de reproduire le composite GRAFIX sans muter les assets DAT. Le routage BSSM du Portfolio est maintenant restitue : Engine publie dans StatsUI le meme contexte Build/Sell/Mortgage/UnMortgage qui a servi au rendu, les grilles Player et Deed refont les hit-tests retail sur les seules proprietes affichees (priorite maximale pour les deeds Player superposes), puis UserInterface redirige le square vers le chemin IBar existant comme UDStats_ProcessMessage. Restent surtout l habillage GRAFIX du popup calculateur, les autres surfaces texte/word-wrap/fonts et les deux datasets Bank externes. |
| `Source/monopoly/UDTrade.cpp` | `TradeUI`, `TradePropertyPlayback`, `TradeOfferIconPlayback`, `TradeCashDialogPlayback`, `TradeContractDialogPlayback`, `TradeBackdropPlayback`, `TradeTokenPlayback`, `TradeActionButtonPlayback`, `UserInterface`, `IBar`, `LocalPlayers::tradeSourcePlayer`, `RuleTrade` | `PORTED_PARTIAL` | `UDTrade_GetPlayerToTradeFrom`, entree ecran Trade, selection partenaire, liste editeur, cash, deeds, jail cards, futures/immunites, notifications started/editor/item/acceptance/finished, counter-offer | miroir UI, RuleTrade, LocalPlayers, DISPLAY, IBar, SequencePlayback | Le chemin interactif est maintenant largement porte : selection partenaire, logique cash retail, cartes sortie de prison, Propose/Cancel, `Trade_SendItems`, deeds/proprietes avec hit-test source exact, projection before/offered/after, hypotheques, futures/immunites et nettoyage des contrats devenus invalides. `InitializeTradeSequence` est maintenant reproduit dans le miroir UI: nouveau trade et resync d un proposant distant purgent offres de cases, cash, cartes prison et contrats temporaires, tandis qu une resync du proposant humain local preserve la proposition; `NotifyTradeFinished` retire uniquement les `CountHits` traded comme `EndTradeSequence`, sans toucher aux contrats actifs. `TradePropertyPlayback` rend les quatre ensembles statiques reellement affiches par le retail (before A/B + offered A/B) avec PAT `TAB_trprfm0000..27` / `TAB_trprfp0000..27`, priorites 324/374/424/474, positions/ordre historiques et transitions Stop/Start/Move transactionnelles. Son `movingcard` reproduit `MOVESTEP=4`, cadence 25 ms, depart immediat a 1/4, priorites 626/676/726/776, blocage des clics pendant le mouvement et apparition statique seulement au quatrieme pas; les changements distants restent statiques. `TradeOfferIconPlayback` publie maintenant les cartes prison Chance/Community et les icones Future/Immunity avec les DataID retail, priorites 275..278, quatre positions sources et transitions atomiques, puis les retire en quittant Trade. `TradeCashDialogPlayback` porte le popup cash autonome : fond `TAB_tnmtry00`, Clear/Okay/Cancel idle et pressed, priorites 1975/1976/1977, positions A/B retail, `EndingActionStop` et `CashTradeDying` retardant le retrait jusqu a la fin du CNK Okay/Cancel; Clear reste ouvert et les transitions sont transactionnelles. `TradeContractDialogPlayback` porte les deux seules sequences autonomes communes aux dialogues Future/Immunity : `CNK_byahaupi`/`CNK_byahadni` DAT_MAIN 0x0009/0x0007, priorite 525 et positions (22,-168)/(22,-247), sans redemarrage lors des changements de mode. `TradeBackdropPlayback` rend maintenant les six CNK de decor autonomes, les rails couleur gauche/droite et les boutons Create/Future/Immunity conditionnels aux priorites retail 80/95/1000/130/140/145/150/155/160; les trois boutons animes utilisent `LoopToBeginning`, avec remplacement de rail et preflight FIFO transactionnels. `TradeTokenPlayback` rend les jetons A/B via `TAB_inpsa+token`, priorites 274/275 et positions retail; le jeton B reprend exactement `790 - largeur_bitmap` en inspectant le bitmap reel de la sequence, avec remplacement et preflight transactionnels. `TradeActionButtonPlayback` rend Propose/Cancel idle `CNK_iytprpf`/`CNK_iytcxlf` puis leurs animations de sortie `CNK_iytprpo`/`CNK_iytcxlo` aux priorites 202/201, avec `EndingActionStop` et transition transactionnelle. Le lot GRAFIX/FONTS est maintenant engage dans l ecran lui-meme : `TradeCashTextPlayback` reproduit les quatre surfaces cash 50x11, les positions/priorites retail et `TAB_snpscsh1`, avec le font-slot 8; `TradeNamePlayback` rend NameA/NameB 135x35 a (56,229)/(604,229), taille 12 poids 700; `TradePanelTextPlayback` reconstruit `leftpanel` 200x225 a partir de `TMN_TRADING`/`TMN_TRADING2`, tailles 12/9, gras/soulignement et wrap 176 px. `TradeContractTextPlayback` porte maintenant `rightpanel` Future/Immunity (200x225, position 600/0, priorite reelle 148), titres/consignes des modes 0..6, compteur, nom joueur, noms de proprietes localises par ville, cinq lignes scrollables avec selection et bouton Okay. La liste de creation est raccordee dans `TradeUI`, avec auto-scroll vers la propriete ajoutee. Le lot GRAFIX/FONTS du 20 septembre a depuis ete compile et integre aux validations globales; le 23 septembre, `TradeContractTextPlayback` recoit en plus une suite runtime dediee couvrant pixels, revisions live et rollback. La qualification visuelle avec les assets retail reste distincte. Les structures after A/B restent une projection logique : leur repositionnement 4/5 est commente dans le source retail et n est donc pas invente. Les Host Comments Trade cross-edition sont maintenant actifs : nouveau trade a 2 joueurs -> PB_TradeScreen, 3+ -> PB_TradeScreen_PickTradePartner, proposition incomplete -> PB_TradeScreen_ProposeClickedButOneSideHasNotOfferedAnything, fin acceptee/annulee -> PB_TradeScreen_TradeComplete/TradeCancelled, tous en ClipOldSoundIfPlaying. La reouverture d un editeur stocke reste silencieuse. Un refus `NotifyActionCompleted(StartTradeEditing)` joue maintenant le warning uniquement sur la machine possedant le demandeur humain local, comme `UDTrade.cpp`; les autres machines restent silencieuses. L acceptation de StartTradeEditing ferme aussi immediatement selecteur joueur, popup cash et dialogue Future/Immunity; `NotifyTradeAcceptanceDecision` ferme le cash et les dialogues contrat editables mais preserve exactement le viewer mode 6 retail. Le garde-fou periodique retail qui abandonne l ecran Trade si A ou B devient bankrupt/off-board est raccorde au changement de projection des mouvements : etat trade nettoye et retour Main immediat. `NotifyTradeStarted` applique aussi le teardown `proposed` de `UDTrade_ProcessEverything`: selecteur/cash et contrats editables ferment, tandis que les viewers contrat mode 6 restent ouverts. Le routage `CurrentUIPlayer` du trade est maintenant fidele: `NotifyTradeEditor` selectionne l editeur local s il existe, et l acceptation selectionne exclusivement `TradeB`; le fallback distant de `tradeAcceptanceIBarPlayer` ne contamine plus le joueur UI local du spectateur. La sortie `NotifyTradeFinished` respecte aussi le quirk retail `IBarLastRulePlayer = numberB`: si `CurrentPlayer != numberB`, le tracking reste coupe et l IBar inspecte explicitement le joueur courant en mode local ou distant au lieu de revenir systematiquement a `Nothing`. Les annonces USA-only `PB_HumanInitiatesTrade` / `PB_AIInitiatesTrade` sont maintenant raccordees sur `NotifyTradeAcceptanceDecision` avec le `AcceptSndTick` retail transpose en 60 Hz : seuil strict `>5*60`, gate global process-level rearme meme quand aucune ligne n est selectionnee, Human uniquement pour un proposant humain non-local vers un B humain local, AI pour un proposant AI vers B local ou pour un trade AI-vs-AI observe, le tout en `ClipOldSoundIfPlaying`; Europe reste silencieuse et ne consomme pas le gate USA. Restent surtout les surfaces/dialogues/panels et la presentation texte/fonts. |
| `Source/monopoly/UDCGE.cpp` | Aucun requis | `LEGACY_UNUSED` | hors cible livree | `TheGame.dsp` | Absent de la liste des sources de `TheGame.dsp`; ne fait pas partie de Monopoly.exe livre. Le flux joueur/regles actif est dans `UDPsel.cpp`. |
| `Source/monopoly/UDChat.cpp` | `ChatRuntime`, `ChatRecipientPlayback`, `ChatOptionPlayback`, `ChatFluffPlayback`, `Messaging`, `UserInterface`, `Engine` | `PORTED_PARTIAL` | historique texte, saisie SDL, public/prive, destinataires, Close/Shade/Options, `ACTION_TEXT_CHAT` / `NOTIFY_TEXT_CHAT` | RULE echo, LocalPlayers, SequencePlayback, futur renderer/fonts | Noyau runtime porte : historique circulaire 512 messages, texte 255 caracteres, UTF-8/UTF-16LE, ouverture/reception, Tab reseau, Backspace Unicode, Enter/envoi, masque destinataires `CHAT_ToPlayer`, diffusion ou lots prives transactionnels et echo local retail. Les destinataires prives sont cliquables avec la geometrie retail fixe initiale du chat : humains distants en ordre decroissant, priorite du joueur sur le chevauchement avec All et bascule All exacte avant intersection avec les eligibles. `ChatRecipientPlayback` rend `TAB_cnclrall`, `TAB_cnclr00+couleur` et `TAB_cntlkfc0+couleur` aux coordonnees/priorite de `CHAT_BarArea`. `ChatRuntime` porte maintenant aussi les hotspots et transitions Close/Shade/Options, y compris conservation de Shade/Options a travers `CHAT_Toggle`, ouverture Options qui deshade, et Shade qui ferme Options. `ChatOptionPlayback` rend `TAB_cnoption` et le panneau `TAB_cntrspy` aux offsets retail de la fenetre, priorites 5230/5232, sans redemarrer le bouton lors du toggle. Le deplacement de fenetre est maintenant porte : drag du chatbar, conservation exacte de l offset souris, mise a jour de `CHAT_winloc` sans clamp comme le source, hotspots destinataires/All/Options/Shade/Close et playbacks Recipient/Options tous recalcules depuis la position courante. Le redimensionnement est aussi porte : hotspot bas-droite retail, offsets souris, bornes 246..1601 / 99..1201 et quantification par pas de 5 en conservant la barre de 18 px; `ChatTextPlayback` recalcule maintenant le word-wrap historique depuis les metriques `FontRuntime`. Le bouton Messages/Fluff est maintenant actif avec le hotspot `CHAT_fluffbtn_rel`; `FLUFF_BoxActive` reste memorise quand Chat est ferme comme le source, tandis que son rendu et ses entrees restent caches jusqu a la reouverture de Chat. `ChatFluffPlayback` rend `TAB_cnmsgs` a l offset courant de la barre. La fenetre Fluff porte maintenant aussi son runtime autonome : geometrie initiale (265,10) 246x99, Close/Shade, drag sans clamp, resize 246..1601 / 99..1201 avec quantification retail, six hotspots de categories, conservation de `FLUFF_BoxActive` quand Chat est ferme, avec controles alors non interactifs, et effacement du draft uniquement lors d un vrai changement de categorie comme `GetCategory`. `ChatFluffPlayback` rend le bouton Messages puis, lorsque Chat est ouvert, le chrome autonome utile de Fluff : `TAB_cntoplft`, `TAB_cndropdn`, `TAB_cnarup`, `TAB_cnardwn` et les six icones `TAB_cngreetn/cncomans/cngmply/cntrade/cnposcom/cnnegcom` a `FLUFF_boxlevel=5225`, avec positions retail, suivi du move/resize et retrait des fleches quand la fenetre est shaded. Le routage souris respecte aussi la priorite retail "Chat toujours au-dessus de Fluff" et absorbe les evenements de souris dans les surfaces Chat/Fluff pour eviter le fall-through vers le plateau. Le scroll Fluff est aussi actif : comptes retail 19/21/18/19/14/8 par categorie, fleches Up/Down bornees, drag du rail `FLUFF_scrollbar` avec clamp et projection fractionnelle exacte vers `FluffLinesOffset`, arret sur LMB Up et reset a zero lors d un changement de categorie. La semantique des phrases Fluff est maintenant portee independamment du renderer : plages de messages 1..19, 21..41, 43..60, 62..80, 82..95 et 97..104 derivees exactement de `CategoryStart`, premier clic = selection, second clic = `ACTION_TEXT_CHAT` avec `numberC` retail, diffusion All ou lot prive selon le masque destinataire, echo local prive et envoi de la selection sur la prochaine touche comme le source. Le hit-test des lignes Fluff utilise maintenant la hauteur de police mesuree par `FontRuntime`, donc la selection suit la geometrie rendue au lieu d une hauteur fictive. L historique de saisie texte est maintenant porte sur les 100 entrees retail : chaque message tape non vide est memorise avant emission, Up remonte jusqu au plus ancien, Down redescend sans effacer la derniere ligne comme le source, et toute reception remet l offset d historique a zero. Le scrollbar du Chat principal porte aussi ses hotspots retail, fleches Up/Down, drag borne et PageUp/PageDown; son cardinal est maintenant celui des lignes effectivement wrappees par `ChatTextPlayback`. La logique du panneau Options est maintenant portee : les six hotspots actifs reprennent les offsets retail, la taille de police est bornee 7..14, l alpha de fond 0..16 et l alpha texte 1..16, avec les valeurs initiales 7/10/10 et consommation du clic dans tout le panneau comme le source. `ChatTextPlayback` applique maintenant ces indices aux surfaces runtime : taille de police, alpha fond/texte, historique wrappe, prefixes public/prive/spectateur, titre/lignes Fluff, draft selectionne et scroll. Le transport voice-chat est maintenant raccorde sans simuler le codec : `VoiceChatRuntime` reproduit le callback d emission legacy (`ACTION_VOICE_CHAT`, Spectator -> Bank -> All), le seuil retail qui jette les paquets non critiques quand la file depasse 50, recoit `NOTIFY_VOICE_CHAT` apres le meme filtrage broadcast/cible locale que `Userifce.cpp`, conserve `numberD` comme identifiant de source pour le futur backend et expose des sinks raw ou evenementiels. Le framing ArtLib est valide en deux passes avant tout backend : suite de chunks little-endian type+taille sans padding, sous-chunks `CHAT` avec `fmt `/`dims`/`volm`, puis `DATN`, `DAT1`, `POSN` et `STOP`. `VoiceChatPacket` decode le WAVEFORMATEX 18 octets, preserve l ordre des chunks et `VoiceChatRuntime` maintient jusqu aux 10 sessions receveuses de `CE_ARTLIB_SoundMaxChatsReceivable`, remplace une session sur un nouveau CHAT, ignore les donnees sans session et libere la source sur STOP. `RuleCoreActions` fait l echo VoiceChat -> NotifyVoiceChat avec sourceId assigne par la connexion; 0 reste l hote local. Le lot audio portable est maintenant engage plus loin : `VoiceChatAudioRuntime` raccorde le sink evenementiel, la reception PCM SDL3, le cut immediat de `DAT1`, le volume, la capture micro SDL3 convertie directement en PCM 11025 Hz/8 bits/mono, la detection de silence, le herald, l etat Collecting/Streaming et les paquets CHAT/DAT1/DATN/STOP limites a 64K. Le demarrage respecte le mode reseau maintenant possede par Messaging et rendu actif apres handshake TCP. Le codec GSM 6.10 est maintenant raccorde via la revision epinglee de `libgsm` en mode WAV49 : blocs Microsoft 65 octets / 320 echantillons, WAVEFORMATEX 0x31 exact a 11025 Hz (`2239 B/s`, `blockAlign=65`, `cbSize=2`, `wSamplesPerBlock=320`), encodage capture et decodage reception vers PCM 8 bits. Le runtime conserve le mode `No Compression` separe et ne change jamais silencieusement le format demande. Le demarrage respecte toujours le mode reseau, avec redemarrage apres resync et arret lors de la deconnexion. Les anciens manques Chat lies aux fonts sont maintenant raccordes : word-wrap, historique rendu, Fluff localise, selection/hit-test des lignes et indices d alpha/taille; restent les differences fines de rendu et les branches actives a auditer. |
| `Source/monopoly/UDPlrCfg.cpp` | Aucun requis | `LEGACY_UNUSED` | hors cible livree | `TheGame.dsp` | Absent de la liste des sources de `TheGame.dsp`; ne fait pas partie de Monopoly.exe livre. Le flux joueur/regles actif est dans `UDPsel.cpp`. |
| `Source/monopoly/UDPlrSum.cpp` | Aucun requis | `LEGACY_UNUSED` | hors cible livree | `TheGame.dsp` | Absent de la liste des sources de `TheGame.dsp`; ne fait pas partie de Monopoly.exe livre. Le flux joueur/regles actif est dans `UDPsel.cpp`. |
| `Source/monopoly/UDRules.cpp` | Aucun requis | `LEGACY_UNUSED` | hors cible livree | `TheGame.dsp` | Absent de la liste des sources de `TheGame.dsp`; ne fait pas partie de Monopoly.exe livre. Le flux joueur/regles actif est dans `UDPsel.cpp`. |
| `Source/monopoly/UDTitle.cpp` | Aucun requis | `LEGACY_UNUSED` | hors cible livree | `TheGame.dsp` | Absent de la liste des sources de `TheGame.dsp`; ne fait pas partie de Monopoly.exe livre. Le flux joueur/regles actif est dans `UDPsel.cpp`. |
| `Source/monopoly/UDPenny.cpp` | `UDPennyVoice`, `TokenVoiceCatalog`, `PennybagsCatalog`, `UserInterface`, `Engine`, `UDSoundRuntime` | `PORTED_PARTIAL` | reactions vocales achat/enchere/GoToJail et selecteur pur des reactions de case | GameState, BoardRules, Token Voices, Host Comments/Pennybags | `UDPennyVoice` porte les branches retail deterministes : achat, enchere, GoToJail, GO, Free Parking, taxes, prison, proprietes libres/propres/hypothequees, railroads/utilities, maisons/hotel et seuils loyer/fortune. Les hooks runtime exacts sont actifs pour achat accepte, enchere acceptee et GoToJail; les reactions Token Voice de fin de mouvement sont maintenant raccordees au terme reel de `PieceMovePlayback`, avec `JustReadACardHack`, `AI_Get_Total_Worth`/`AI_Rent_If_Stepped_On` equivalents modernes et reset du garde apres landing. Host Comments/Pennybags est maintenant raccorde pour achat humain/monopole, prompt Buy-or-Auction, GoToJail humain et reactions de landing Chance/Community Chest/Free Parking/GO/taxes/Just Visiting. UDPENNY_PassedGo est aussi reproduit au moment exact ou un segment termine sur GO avant de poursuivre, sans doublonner le cas ou GO est la destination finale. Les annonces vocales des proprietes sont maintenant raccordees avant la reaction de landing : USA utilise DAT_LANGDIALOG avec WAV_s_010000 + square + city*41; Europe utilise DAT_BOARD avec WAV_s_010201 + propconv[square] + city*28. Le son est lance en Skip puis watch/queue-lock comme le retail, afin que la reaction suivante attende sa fin. Les custom boards Europe city=-1 restent volontairement muets tant que iLangId n a pas de proprietaire moderne. La lecture vocale des cartes est maintenant raccordee au demarrage reel de l animation DeckOut : le build USA utilise le mapping retail contigu WAV_pb186 + index 0..31 dans DAT_LANGDIALOG. Le mapping Europe 0..31 est maintenant porte depuis `UDPENNY_GetCorrectCardWav`: Chance conserve tables monnaie/pays, variantes Euro/default et cas irreguliers 1/7; Community Chest conserve les variantes monetaire 01..04/06..16 et la carte pays partagee 05. `Display::State.system` est l owner monetaire moderne et les custom boards city=-1 retombent sur la langue active comme le `iLangId-2` retail. `UDPENNY_NextPlayer` est maintenant raccorde sur `NotifyStartTurn` : prompt Pennybags generique a 7 % sinon ligne du pion, voix `Intro` au premier mouvement, puis pour une IA tirage 1/8 avec `StartSide4Only` sur les cases 31..39 ou `StartTurnGeneric`; la consommation de `rand()` conserve le short-circuit retail et le prompt hote est watch/queue-locke avant la voix du pion. `UDPENNY_AnnounceRoll` est aussi raccorde au signal existant `DicePlayback::announceRoll` a elapsed 91, mappe les totaux 2..12 vers `PB_SayDiceRoll_2..12` et reste muet quand Token Animations est OFF. Restent les commentaires Pennybags des autres ecrans et les chemins audio/custom-board qui dependent encore d owners ou de donnees non portes. |
| `Source/monopoly/UDUtils.cpp` | `UDUtils`, `ResourcePaths`, `ResourceContext`, `TextureCatalog` | `PORTED_PARTIAL` | chemins, INI, choix HMD, substitutions de textures | filesystem, DATA, assets | Resolver portable raccorde au startup DATA selon edition/langue, racines explicites et casse ASCII des chemins legacy. Recettes stock ville/langue/plateau/devise raccordees aux chemins BMP et au mesh via BoardTextureRuntime; custom externe reste differe. INI/CD et recherche par basename remplaces. |
| `Source/monopoly/Unility.cpp` | Aucun requis dans la cible livree | `LEGACY_UNUSED` | conversions Unicode/locale historiques | aucun | `TheGame.dsp` lie le fichier, mais ses deux seuls callers sont inactifs : `Userifce.cpp` sous `#if 0`, `Main.cpp` sous `#if FOREMAILVERSION` avec `FOREMAILVERSION 0` dans `GameInc.h`. Les conversions modernes utilisent SDL/std::wstring/UTF-16 sans ces wrappers C locale. |
| `Source/monopoly/Debugart.cpp` | `DebugDialogs` | `PORTED_PARTIAL` | `DisplayMessage`, `RetryMessage`, `OKCancelMessage`, `YesNoMessage` | SDL3 native message boxes | La famille est engagee avec un service portable synchrone base sur `SDL_ShowSimpleMessageBox`/`SDL_ShowMessageBox`. Les labels, retours booleens, bouton par defaut et Escape reproduisent les quatre contrats Win32 actifs de `Debugart.cpp` sans dependance `MessageBox`. `ErrorExit` n est pas branche dans le runtime moderne : aucun caller actif n a ete trouve dans le source Monopoly, et l arret brutal reste volontairement hors de ce premier lot. |

## Services ArtLib consommes

| Original | Equivalent moderne | Statut | Contrat consomme | Dependances | Travail restant / blocage |
|---|---|---|---|---|---|
| `Source/artlib/L_Main.*` | `Application`, `Engine`, `Game` | `REPLACED_PORTABLE` | boucle evenementielle, init/shutdown | SDL3 | Fenetre redimensionnable/HiDPI, presentation, erreurs de startup, creation/device/swapchain et lifecycle GPU sont raccordes et testes sur D3D12. Vulkan/Metal restent architecturaux mais non compiles ici; un smoke test interactif retail reste requis. |
| `Source/artlib/L_Timers.*` | `Timers` + `UIMessages` | `REPLACED_PORTABLE` | horloge 60 Hz, 4 timers, speed/restart, evenement index+tick | steady_clock, file UI | Contrat actuellement raccorde teste de facon deterministe; les appels historiques `LE_TIMER_Delay` identifies dans `Main`, `UDUtils` et les diagnostics DISPLAY appartiennent a des chemins remplaces, inactifs ou encore differes. |
| `Source/artlib/L_UIMsg.*` | `UIMessages` | `PORTED_PARTIAL` | FIFO 100, evenements timer/input, delestage | Application, Timers | Ajouter davantage de tests de pression/coalescence et les types requis par les futurs modules. |
| `Source/artlib/L_Data.*` | `DataBanks`, `LegacyDataArchive`, `DataBankRegistry`, `LegacyDataArchiveBuilder` | `PORTED_PARTIAL` | DataId 16:16, groupes, header/index DAT, zlib, chargement paresseux, refs | zlib, filesystem | Lecture LE explicite, validation, metadata, cache, leases partagees, mount/unmount, index packed et writer de fixtures sont testes. Restent les sources runtime/user-created/external-file, le budget/LRU automatique et une validation sur banque retail. |
| `Source/artlib/L_Chunk.*` | `LegacyChunkReader`, `openLegacyChunkReader` | `PORTED_COMPLETE` | lecteur consomme : header 24-bit + ID 8-bit, descend/ascend/seek/map/read, limite 8 niveaux | `LegacyDataArchive`, futur `L_Seqncr` | Contrat read-only et ownership `ReadFromDataID` portes sans bitfield ABI et testes sur fixtures, y compris le franchissement historique des siblings ID 0/128 lors d'une recherche precise. La validation d'un CNK retail reste bloquee separement; l'editeur/writer non consomme n'est pas dans ce perimetre. |
| `Source/artlib/L_Grafix.*`, `L_Rend2D.*`, `L_Sprite.*` | `Display`, `SequenceBitmapRenderData`, `SequenceWorld2DSlot`, `World2DRenderer` | `PORTED_PARTIAL` | composition 2D, clipping, priorites, surfaces | SDL_GPU, assets, transformation 800x600 | Les feuilles bitmap de sequence ont un chemin actif jusqu'au quad SDL_GPU : ordre depth-first/priorite, cache RGBA8, alpha source, viewport/scissor letterbox et readback D3D12 sont testes. Les sprites UI generiques, fonts, surfaces/blits hors sequence et clipping fin restent a construire. |
| `Source/artlib/L_Rend3D.*` | `GPUFrame`, `SequenceWorld3DSlot`, `World3DGPUScene`, `World3DProjection`, `World3DRenderer` | `PORTED_PARTIAL` | slot World3D 1, viewport, camera/projection, bounds/culling, draw indexed et meshes animes | SDL_GPU, `SequenceRenderData`, PC3D moderne | Le chemin sequence -> slot 1 -> scene GPU -> renderer -> GPUFrame est actif. Bounds, projection ecran, culling, textures HMD et vertex buffers MIMe par node sont testes sur D3D12 reel. Camera 3D/FOV/SetCamera sont raccordes. La visibility 3D historique est maintenant auditee : `SequenceMoved()` retourne toujours TRUE pour un mesh (commentaire source inclus), donc le culling moderne reste strictement renderer-only et ne pilote pas `scrollingWorld`. Restent certains contrats de scene et primitives HMD non consommees. |
| `Source/artlib/L_Seqncr.*` | `LegacySequence`, `SequenceClock`, `SequenceChildSchedule`, `SequenceProgram`, `SequenceRuntime`, `SequenceCommandQueue`, `SequenceTransforms`, `SequenceRenderData`, `SequenceBitmapRenderData` | `PORTED_PARTIAL` | records, arbre runtime, lifecycle, commandes actives, transformations/tweekers, mesh choice, feuilles 2D/3D/audio | `LegacyChunkReader`, DATA, `MeshRuntime`, `SequenceWorld3DSlot`, `SequenceWorld2DSlot` | Grouping/indirect/tweeker, feuilles mesh 3D, feuilles bitmap 2D, records Sound WAV et records camera 3D sont executes; Start/Stop/SetEndingAction, MoveTheWorks/MoveXY/MoveRySTxz et SetCamera sont raccordes. `GetInfo` expose le sous-ensemble effectivement lu par Monopoly (clock/endTime/matrice monde 3D) avec la recherche `FindNextSequence`, et `GetChildMeshWorldMatrix` parcourt uniquement le sous-arbre du premier root selectionne. ForceRedraw est porte avec son cycle transitoire de redraw et la reevaluation cible/ancetres. StartXY/StartMoved transportent maintenant leur transform initial dans la commande Start elle-meme : plusieurs racines partageant le meme DataId et la meme priorite conservent donc des positions independantes comme LE_SEQNCR_StartXY, au lieu qu un MoveMatching ulterieur deplace tous les doublons. Restent surtout le raccord video/preloader vers `VideoRuntime`, puis labels generiques, model, callbacks et chains selon callers reels. |
| `Source/artlib/L_Fonts.*`, `L_Print.*` | `FontRuntime`, SDL3_ttf | `PORTED_PARTIAL` | Arial, mesure largeur/hauteur, rendu texte, taille, poids/styles et 10 slots de reglages | SDL3_ttf, font systeme/fichier explicite, LANG, futurs overlays 2D | `FontRuntime` porte maintenant le sous-contrat source utile : resolution explicite de `Arial.ttf`, mesure UTF-8 avec le `+1` de largeur retail, rasterisation non antialiasée `TTF_RenderText_Solid` vers RGBA8, taille, poids traduit en regular/bold, italic/underline/strikeout et 10 snapshots de reglages. `Engine::fontPlayback` recherche Arial d abord pres de l executable puis dans `%WINDIR%/Fonts` sous Windows sans imposer ce fallback aux autres plateformes. `AuctionTextPlayback` est maintenant le premier ecran complet hors dialogue Load/Save a consommer ce backend : current-bid, noms, mises et cash passent par des surfaces runtime. `MoneyFormat` fournit en plus le formateur monetaire source-backed reutilisable par les prochains ecrans. `FontRuntime::wrap` reproduit maintenant le coeur de `UDChat.cpp::CHAT_WordWrap` avec mesure du font courant, preference des espaces, hard-break UTF-8 quand un mot ne tient pas et marqueur `_` non cassable; `TradePanelTextPlayback` en est le premier consommateur. Les slots partages 0/7/8/9 sont derives du baseline retail (Arial 12/700) dans `Engine::fontPlayback`. `UDTrade` consomme maintenant le backend pour leftpanel, NameA/B, les quatre cash boxes et `rightpanel` Future/Immunity via le slot 7, et l IBar pour noms/cash des score boxes. Le panneau contrat ajoute le texte localise, la liste selectionnable et Okay; `TradeContractTextPlaybackTests` valide maintenant la surface 200x225 a (600,0), la priorite 148, le highlight bleu retail, les revisions bitmap sans Stop/Start, le basculement Future/Immunity, l expansion mode 3, le retrait et les echecs transactionnels. `FontRuntime` est maintenant aussi consomme par les surfaces WIP `UDStats`, `UDChat`, `UDPsel`, `UDOpts` et `IBarRuntimeTextPlayback`; les tests cibles valident le rendu glyphes/metriques et les wrappers Chat/UDPsel separes. Restent les surfaces UI encore non portees et les differences fines de metriques GDI/FreeType. |
| `Source/artlib/L_Keybrd.*`, `L_Mouse.*` | traduction SDL dans `Application`, `MousePointer`, `MousePointerPlayback` | `REPLACED_PORTABLE` | input clavier/souris, pointeur ArtLib, curseur texte Chat | SDL3, `LogicalViewport`, `SequencePlayback` | Souris reconvertie vers 800x600 et bandes noires rejetees. `TAB_pointer` DAT_MAIN est maintenant rendu dans Overlay2D a la priorite retail `0xFFFF`, suit les coordonnees logiques par Move sans redemarrage, disparait quand la souris est desactivee et laisse un fallback natif tant que la ressource n'est pas prete. Le champ d'edition Chat bascule sur un I-beam natif et restaure le pointeur de jeu selon le drag/clic retail. Restent seulement les types d'evenements legacy non encore requis par les callers modernes. |
| `Source/artlib/L_Sound.*`, `L_Midi.*` | `AudioRuntime`, `UDSoundRuntime`, `VoiceChatRuntime`, `VoiceChatPacket`, feuilles Sound de `SequenceRuntime`, `Engine` | `PORTED_PARTIAL` | WAV DAT, playback SDL3, gain, stop, boucle, lifecycle des sons de sequence/SFX Monopoly et protocole de sessions voice-chat | `LegacyDataArchive`, `LegacySequence`, SDL3 audio, Messaging | `AudioRuntime` lit directement les RIFF/WAVE DAT via `SDL_LoadWAV_IO`; les records Sound de `L_Seqncr` sont executes. `UDSoundRuntime` reutilise ce backend pour Warning/Click/Cash, musique m1..m5, Credits et voix de pions/Pennybags. Le sous-contrat ArtLib voice-chat est maintenant engage : framing `CHAT/fmt/dims/volm/DATN/DAT1/POSN/STOP`, validation en deux passes, limite retail de 10 sessions receveuses, sourceId et dequeue prioritaire pendant les locks de jeu. `VoiceChatAudioRuntime` ajoute maintenant le chemin PCM portable : capture par `SDL_AUDIO_DEVICE_DEFAULT_RECORDING` avec conversion SDL vers 11025 Hz/8 bits/mono, seuil de silence retail 0.05, silence max 6 s, herald 20 ticks, collecte post-silence 120 ticks, emission CHAT/DAT1/DATN/STOP, reception PCM par source et cut/flush sur DAT1. `VoiceChatPacket` encode aussi les chunks sortants et impose la limite UNS16/64K du callback retail. Le codec GSM 6.10 est maintenant branche sur `libgsm` avec `GSM_OPT_WAV49`; les valeurs Microsoft du format 11025 Hz sont reproduites (`nAvgBytesPerSec=2239`, `nBlockAlign=65`, 320 samples/block), les deux frames WAV49 32+33 octets sont encodees et les blocs entrants 65 octets sont decodes en PCM 8 bits avant SDL3. La dependance est epinglee au commit `98f1708fb5e06a0dfebd58a3b40d610823db9715` et conserve sa notice Degener/Bormann. L owner reseau vocal TCP est maintenant implemente et peut activer la capture; sa qualification reste ouverte. Restent MIDI, attributs audio avances et quelques commentaires Pennybags. |
| `Source/artlib/L_Video.*` | `VideoRuntime` | `PORTED_PARTIAL` | contrat AVI consomme par le sequenceur : metadata `avih`, status current/desired/total frames, loop/end sentinel, cut-to-frame et alternatives de saut | futur decoder video portable, SequencePlayback | La famille est engagee sans porter VFW/ICM/DirectDraw : `VideoRuntime` parse le conteneur RIFF AVI de facon borne/LE, valide `avih` (frame duration/count/dimensions), porte le status historique, le calcul frame<-temps, `StopVideo`, `CutVideoToFrame`, loop-at-end et les alternatives DecisionFrame/JumpToFrame. Le decodage pixels/audio et le raccordement des opcodes video de `L_Seqncr` restent a faire avant lecture effective des opening movies. |

## PC3D consomme

| Original | Equivalent moderne | Statut | Contrat consomme | Dependances | Travail restant / blocage |
|---|---|---|---|---|---|
| cameras, viewports, background (`camera.*`, `D3DDevice.*`, view code) | `GPUFrame`, `World3DProjection`, `World3DRenderer`, `Display::Viewport3D`, `LogicalViewport` | `PORTED_PARTIAL` | rectangles Main/Status/Trade, view/projection, depth, bounds ecran et clear | SDL_GPU | Camera/view/projection portable et viewport letterbox sont raccordes au renderer et testes sur D3D12. Le record camera 7, FOV 144, SetCamera direct et selection par label camera sont portes; l angle est confirme comme FOV complet par `PC3D/Matrix.inl`. Le source 3D ne remonte pas son culling au sequenceur (`SequenceMoved` retourne TRUE); restent surtout les transitions UDBoard/UDPieces au fil du port UI. |
| meshes/scenes/materials (`mesh*`, `NewMesh*`, `Scene.h`, `l_material.h`) | `MeshXRuntime`, `MeshRuntimeCache`, `MeshRenderData`, `MeshGPUCache`, `World3DGPUScene` | `PORTED_PARTIAL` | postload HMD, groupes materiau/texture, poses MIMe, bounds, donnees indexees et buffers GPU | `LegacyMeshData`, SDL_GPU | Pour Monopoly, `USE_OLD_FRAME` rend `hmdload.cpp`/`meshx` autoritatif; `NewMesh.cpp` est le chemin alternatif inactif. Triangles actuellement decodes, textures HMD embarquees, pose 0 + diff MIMe, interpolation/extrapolation mesh choice et buffers vertex GPU par node sont portes. Substitutions BMP stock TextureCatalog raccordees transactionnellement, UV conserves. L audit GIS-10 ne trouve aucun type actif supplementaire a porter; les categories sans caller restent exclues. |
| decodeur HMD / postload MESHX (`HMDData.h`, `NewMesh.cpp`, `hmdload.*`) | `LegacyMeshData`, `openLegacyMeshData`, `MeshXRuntime` | `PORTED_PARTIAL` | HMD disque LE, offsets DWORD, triangles, `GsUIMG1`, `GsVtxMIMe`/`GsNrmMIMe`, puis construction mesh CPU | `DAT_3D`, textures, GPU | Structure bornee, cycles/budgets, triangles categorie 0, image 8-bit CLUT -> RGBA8 et diff blocks vertex/normal MIMe sont testes sur fixtures synthetiques. Reset/joint MIMe et autres categories/primitives restent explicites/non portees. MESHX demeure un resultat memoire, jamais un second format disque. |
| vieux DirectDraw/Direct3D drivers (`DDraw*`, `D3DDevice*`) | SDL3 / SDL_GPU | `REPLACED_PORTABLE` | creation device, swapchain, soumission | SDL3 | Les implementations legacy ne seront pas portees; completer seulement les comportements de rendu consommes. |

## Donnees et verification

| Sous-composant DATA | Equivalent moderne / preuve | Statut | Limite exacte |
|---|---|---|---|
| `DataId` / `DataTag` / groupes | `DataBanks`, y compris `IdWithFileFromParent` | `PORTED_COMPLETE` | Contrat 16 bits groupe + 16 bits tag; groupe zero reserve, tag zero valide. |
| Header et index physique DAT | `LegacyDataArchive` | `PORTED_COMPLETE` | Header Win32 28 octets et records 16 octets lus champ par champ en LE; signature/version/types/ranges/troncatures testes. |
| Codec DAT | zlib via `uncompress2` avec consommation exacte | `PORTED_COMPLETE` | `ZImplode.c` prouve un stream zlib enveloppe (`windowBits=15`), pas un codec PKWARE opaque. |
| Lifecycle, lookup, metadata et ownership | `LegacyDataArchive`, `DataBankRegistry`, `ResourceRuntime` | `PORTED_PARTIAL` | Montage des huit banques raccorde au jeu; publication transactionnelle et conservation des snapshots lecteurs testees sur succes/echec/reprise. Sources runtime, external-file et LRU automatique restent a porter. |
| Resolution des chemins DATA | `ResourcePaths`, `UDUtils`, `ResourceContext` | `REPLACED_PORTABLE` | Racines absolues ordonnees, separateurs legacy, casse ASCII, erreurs de collision/acces et choix USA/Europe + langues 1..10. Raccordement reel SDL/variable de processus teste; adaptation contextuelle des textures encore distincte. |
| CRC global DAT | option `ChecksumPolicy::Verify` | `PORTED_PARTIAL` | Le runtime original ne le verifiait pas et aucune banque retail ne confirme la convention du writer; politique runtime par defaut `Ignore`, audit explicite disponible. |
| Writer DAT portable | `LegacyDataArchiveBuilder` | `PORTED_COMPLETE` | Reconstruit deterministement un conteneur valide depuis des payloads fournis; ne pretend pas reproduire les payloads ou octets retail. |
| Index logique packed 6 octets | `DataIndexTable`, `lookupIndexedDataId` | `PORTED_COMPLETE` | Tri strict, cles dupliquees rejetees, plusieurs cles vers le meme tag permises, groupe parent reapplique. |
| Lecteur CNK | `LegacyChunkReader`, `openLegacyChunkReader` | `PORTED_COMPLETE` | Lecture hierarchique source-compatible sur fixtures, y compris la semantique des sentinelles nulles selectionnees ou sautees; aucune validation retail faute de CNK. |
| Parseurs semantiques CNK / sequence | `LegacySequence` | `PORTED_PARTIAL` | En-tete 12 octets, temps signes 24 bits, records grouping/indirect/bitmap/model/sound/camera/mesh/tweeker et attributs prives dimensionality/offset/matrix/OSRT sont bornes. Le record camera packe fait 21 octets (near/far/label); les chunks 139 `3D_MESH_CHOICE` et 144 `CAMERA_FIELD_OF_VIEW` sont decodes explicitement. Video/preloader et attributs encore inconnus sont refuses tant que leurs effets ne sont pas portes. |
| `SequenceClock` | `SequenceClock` | `PORTED_COMPLETE` | Cadence, premiere evaluation, drop/catch-up enfant, pause/reprise, stop, seek, hold cadence 255 et boucle naturelle a zero sont testes pour les types actuellement executables. |
| `SequenceChildSchedule` | `SequenceChildSchedule` | `PORTED_COMPLETE` | Selection ouverte a gauche/fermee a droite, ordre disque, refs DATA relatives et rewind; description partagee immutable et curseur runtime independant. |
| Arbre runtime et execution | `SequenceProgram`, `SequenceRuntime` | `PORTED_PARTIAL` | DAG descriptif borne puis foret mutable a ownership unique; lifecycle, loops, stop/hold/seek, cycles/profondeur/budgets et snapshots testes. Grouping/indirect/tweeker, feuilles mesh 3D, feuilles bitmap 2D et cameras 3D sont executes. Les bitmaps publient `contentsDataId`, priorite, clock et matrice monde 2D; les cameras exposent matrice monde/FOV/near/far et les meshes `contentsDataId`, world transform et `meshChoice`. Les queries immuables `GetInfo` et `GetChildMeshWorldMatrix` reproduisent le premier match et les bornes de sous-arbre du source. |
| Commandes `L_Seqncr` | `SequenceCommandQueue` | `PORTED_PARTIAL` | FIFO proprietaire 500 commandes, nesting Collect/Execute, Start, Stop, SetEndingAction, MoveTheWorks, MoveXY, MoveRySTxz, SetCamera et ForceRedraw, priorite 16 bits, doublons et recherche top-level/whole-tree portes. ForceRedraw marque tous les matches, force la reevaluation cible/ancetres et expose `needsRedraw` pour le frame courant avant remise a zero au cycle suivant. MoveXY atteint maintenant visuellement les feuilles bitmap via Overlay2D; les callers Monopoly actifs ne donnent aucun chain ID et chains restent differees. |
| Transformations / tweekers | `SequenceTransforms`, etat `SequenceRuntime` | `PORTED_PARTIAL` | Matrices row-vector 2D/3D, offset/matrix/OSRT, composition parentale, commandes Move*, identity/constant/linear et ordre tweeker-avant-position portes. `3D_MESH_CHOICE` interpole la proportion sans clamp; `CAMERA_FIELD_OF_VIEW` constant/linear modifie le FOV brut du parent camera avec defaut 3D pi/4. Restent son et callbacks; `scrollingWorld` demeure un contrat d horloge distinct et non simule. |
| MESHX runtime | `MeshXRuntime`, `MeshRuntimeCache` | `PORTED_PARTIAL` | Le chemin Monopoly `USE_OLD_FRAME` est reproduit pour le sous-ensemble HMD porte : inversion Y, normales de base /4096, deduplication, groupes materiau/texture, pose 0 de base puis poses MIMe `base + delta`, interpolation position/normale non clampee, UV de A et bounds interpoles. Le comportement historique ajoute les diff normals SVECTOR bruts apres conversion de la normale de base; ce contrat est teste. Autres primitives et donnees retail restent bloquees. |
| Render data de sequence | `MeshRenderData`, `SequenceRenderData`, `SequenceBitmapRenderData` | `PORTED_PARTIAL` | Les feuilles mesh publient DataID/node/priority/clock/world matrix, `meshChoice`, asset partage et pose evaluee. Les feuilles bitmap publient leur DataID de contenu, payload immutable et matrice monde 2D jusqu'au slot Overlay2D. Une ressource invalide produit une erreur explicite; les objets SDL_GPU restent crees uniquement dans la couche GPU. |
| Raccordement sequence -> render slots | `SequenceWorld3DSlot`, `World3DGPUScene`, `SequenceWorld2DSlot`, `World2DRenderer` | `PORTED_PARTIAL` | Le slot historique 1 gere startup/moved/shutdown, bounds, camera et culling 3D; la scene GPU utilise buffers index/textures statiques et un vertex buffer MIMe par node. Overlay2D synchronise transactionnellement les feuilles bitmap, conserve l'ordre depth-first/priorite, resout les assets RGBA8 et les dessine en coordonnees logiques 800x600 avec alpha source. Le readback D3D12 2x2 prouve les deplacements -35/-11 des des. Le culling 3D n est volontairement pas renvoye au sequenceur, conforme a `SequenceMoved()` qui retourne toujours TRUE. |
| LANG core | `LanguageCatalog`, `LanguageService`, `ResourceRuntime` | `PORTED_PARTIAL` | Contrats existants conserves et raccordes au startup via les memes archives que DATA; index valide avant publication. Adaptateurs de rendu et migration des callers UI non raccordes. |
| Chaines/audio/dialogues LANG retail | aucun payload | `BLOCKED_MISSING_DATA` | Les neuf fichiers texte bruts sont vides et les `dat_ln/lm/lkNN.dat` sont absents; aucune chaine ne sera inventee. |
| Catalogue `TexInfo` | `TextureCatalog` | `PORTED_COMPLETE` | Huit atlas, recettes USA/Europe, overlays, provenances et tags HMD portes et testes; meshes/resolutions invalides rejetes par erreurs typees. |
| Corpus BMP `TexInfo` | `LegacyBitmap`, manifeste de 1 001 assets | `PORTED_COMPLETE` | 1 001/1 001 fichiers trouves : 497 en 128x128, 504 en 256x256, 881 en 8-bit, 120 en 24-bit, tous `BI_RGB`; offsets, tailles declarees, stride DWORD, raster complet et overflows sont verifies. |
| Loader BMP runtime | `LegacyBitmap`, `BitmapRuntimeCache`, `World2DRenderer`, `LegacyAssets` | `PORTED_PARTIAL` | Les BMP de feuilles sequence sont decodes en RGBA8, caches par DataId + identite de payload puis uploades en R8G8B8A8_UNORM; le cache GPU partage et l'alpha source sont testes par readback D3D12. Le fond historique conserve encore sa voie `LegacyAssets`; les substitutions stock `TextureCatalog` vers les slots mesh passent maintenant par `BoardTextureRuntime` et `MeshRuntimeCache`. |
| Manifestes DMake | `LegacyManifest`, `MonopolyManifestTool` | `PORTED_COMPLETE` | Les dix headers reels donnent 46 423 tags/noms/types contigus et peuvent etre exportes en TSV. |
| DMAKE99 et reconstruction bit-a-bit | aucun outil historique | `MISSING_TOOLING` | Sources/executable DMAKE99, fichiers `.df`, chemins source, payloads, tailles et offsets manquent. |
| Banques DAT retail exactes | absentes | `BLOCKED_MISSING_DATA` | `dat_main`, `dat_pat`, `dat_bord[e]`, `dat_brd2`, `dat_3d`, `dat_ln/lm/lkNN` introuvables dans l'arbre, `Source.zip` et l'ISO inspectes. |
| `2DVIEW01..39` externes | noms portes, fichiers absents | `BLOCKED_MISSING_DATA` | Ne pas confondre ces vues custom avec les vues 2D standard stockees en DAT/TAB. |
| HMD retail / objets MESHX | headers/tags seulement, fixtures HMD synthetiques | `BLOCKED_MISSING_DATA` | Le decodeur HMD partiel est teste depuis le contrat source, mais aucun mesh retail ne permet sa validation; aucun faux payload MESHX disque n'est cree. |

### Dettes de fidelite connues, non bloquantes pour le sequenceur

- Le decodeur LANG moderne valide les paires de substituts et le contenu apres
  NUL plus strictement que les manipulations historiques de code units UTF-16
  Windows. Cette difference reste a arbitrer avant de declarer LANG complet.
- `lookupIndexedDataId` represente l'absence par une erreur typee; le helper
  generique ne reproduit donc pas encore exactement le retour sentinelle
  historique `LED_EI == 0` de tous les callers. Ne pas masquer cette difference
  en `PORTED_COMPLETE` lorsqu'un caller dependra de la sentinelle.

`MonopolyManifestTool <output.tsv> <header-DMake>...` exporte uniquement les
informations effectivement reconstructibles : banque, tag decimal, type et
symbole. Il ne genere jamais silencieusement un DAT ou un payload fictif.

Une absence de DAT ne justifie jamais l'invention d'un chunk, d'une chaine,
d'un offset ou d'une texture. Les fixtures DAT/CNK/LANG presentes sous
`modern/tests` valident le format source et sont explicitement synthetiques.

### Configuration et duree de vie des ressources

Le jeu cherche desormais les banques sous `Dat_Mon/` dans le repertoire de
l'executable. `MONOPOLY_DATA_ROOT` peut designer explicitement une autre racine
absolue d'installation (celle qui contient `Dat_Mon/`). Une valeur relative ou
vide est une erreur de configuration; elle ne reutilise pas une ancienne
configuration et ne retombe pas sur une autre installation. Ce choix portable
remplace la recherche legacy INI/CD/cwd sans chemin propre a une machine.
L'API accepte un contexte edition/langue; le jeu conserve par defaut le contexte
historique USA/English US. Les choix Europe/French sont testes sur fixtures et
ne constituent pas encore un menu de selection utilisateur.

`ResourceRuntime` prepare cinq banques core puis les trois banques LANG dans
un nouveau registre. `LanguageService` utilise les memes instances. Le nouvel
ensemble n'est publie qu'apres validation des huit archives et de l'index LANG.
Une erreur preserve l'ancien snapshot du service. `Game` libere son snapshot
actif apres DISPLAY, souris et slots; un consommateur qui conserve un snapshot
peut encore charger des donnees non cachees. Cela evite le probleme signale par
le commentaire de `Source/monopoly/Main.cpp:450` sur un nettoyage LANG premature.
L'arret retire les expirations des timers de jeu deja en attente, en conservant
les autres evenements et leur ordre FIFO. Les tests de reprise utilisent les
vrais lecteurs DATA/LANG, timers et file UI; les consommateurs graphiques sont
des doubles de test et ne constituent pas une preuve de rendu GPU.

Le startup refuse maintenant aussi une erreur de banque core, dont le retour
etait ignore dans `Userifce.cpp`. C'est un renforcement explicite du diagnostic.
Les banques retail restant absentes, l'application ne peut pas demarrer une
partie complete; aucune fixture n'est installee ou chargee a leur place.
Le fond BMP de `LegacyAssets` conserve pour l'instant son chemin separe
`assets/legacy/BackGround.bmp`; son integration contextuelle reste a faire.

Pour une qualification formelle, la preuve de reference reste : configure CMake,
build puis CTest sur le meme etat de code. Un ancien executable de test ne
constitue pas une validation, et les lots codes sous consigne `sans tests` ne
sont jamais marques comme valides par heritage d'un run precedent.

### HMD et sequences : limites des contrats raccordes

`LegacyMeshData` conserve les octets HMD immuables. Il borne la table des
blocs, les headers, les chaines de primitives et les sections, detecte les
cycles et impose des budgets avant allocation. Les offsets sont des DWORDs de
quatre octets, jamais des pointeurs natifs. Pour Monopoly, les defines
`USE_OLD_FRAME` dans les callers jeu/ArtLib rendent `hmdload.cpp`/`meshx`
autoritatif; `NewMesh.cpp` est un chemin alternatif non compile par ces callers.
Le port suit donc le parcours tail-first de `HMD_MapUnit`, l'ordre append des
diff blocks et les contrats de `HMD_interpolate`, plutot que les typos visibles
dans `NewMesh.cpp`.

`triangle()` valide le sous-ensemble categorie 0 actuellement consomme. Les
images `GsUIMG1` 8-bit + CLUT sont converties en RGBA8 sans GDI. `mimePoses()`
decode `GsVtxMIMe` et `GsNrmMIMe` avec offsets relatifs bornes, budgets de
blocs/SVECTOR et appariement ordinal. La pose 0 reste le mesh de base; les poses
1..N appliquent les deltas. Reset/joint MIMe et autres categories restent
explicites/non portees. Une simple reussite de `parse()` ne pretend toujours pas
valider tous les payloads HMD retail.

`LegacySequence` decode maintenant les huit parties fixes effectivement utiles
a cette etape, dont le record tweeker de 13 octets. Il decode aussi les attributs
prives dimensionality, offset, matrix et OSRT 2D/3D sans copier dans une
structure native. Les octets/records et attributs restent immutables; ils ne
sont jamais utilises comme etat runtime mutable.

`SequenceClock` possede l'etat temporel. Le contrat est extrait de
`L_Seqncr.cpp:3756-3835,6443-6703,7326-7337` : premiere mise a jour immediate,
rattrapage initial des enfants seulement si dropFrames, cadence, pause/reprise,
stop/suicide, maintien a la fin avec cadence 255 activee dans `C_ArtLib.h`,
boucle naturelle a zero (pas modulo du depassement), et seek explicite. EndTime
zero conserve la sentinelle historique 1234567890. Les commandes de remplacement
non nulles de cadence/action sont appliquees avant validation. Une cadence finale
nulle, une action inconnue, une duree negative ou un depassement arithmetique
produit une erreur explicite. Le decoder conserve les champs disque bruts.
Les horloges son/video et la visibilite des mondes defilants ne sont pas simulees.

`SequenceChildSchedule` porte la selection de `AddNewlyBornChildren`
(`L_Seqncr.cpp:5230-5330`) : intervalle ouvert a gauche, ferme a droite,
premier enfant futur bloquant le scan, enfants Stop deja termines ignores.
L'ordre disque n'est jamais trie dans le calendrier. Le loader DATA respecte les
bornes du parent direct et le remplacement par une liste CNK indirecte; le tag
relatif zero est remappe, tandis que l'ID absolu zero signifie aucun enfant. La
description partagee conserve sa lease et chaque runtime possede son propre
curseur. Un type non decode est refuse, pas saute silencieusement.
Une reference indirecte vers le meme item est refusee : le source conserve
dans ce cas les bornes du parent puis echoue en tentant de revenir a zero
(`L_Seqncr.cpp:5269-5279`, `L_Chunk.cpp:2497-2511`). Le port preserve ce refus;
il n'invente pas une lecture recursive pour cette donnee invalide.

`SequenceProgram` construit ensuite un DAG descriptif immutable, avec budgets
de references/descriptions et profondeur configurable plafonnee a 128. Les
cycles indirects sont refuses par couple `(DataId, offset)`; les sous-descriptions
partagees ne sont pas amplifiees exponentiellement. Une construction depuis
`ResourceSnapshot` garde toutes les archives en vie apres remplacement du
snapshot publie. Ce chargement eager et borne remplace volontairement le parcours
paresseux par pointeurs du code Win32 afin que toute dependance invalide echoue
avant publication.

`SequenceRuntime` instancie une foret mutable a ownership unique sous la racine
historique implicite. Les enfants naissent a leur tick, sont mis a jour
recursivement et detruits enfant-avant-parent. Les loops detruisent les anciennes
instances, rembobinent le calendrier et creent de nouveaux IDs; stop, fin
naturelle, hold, pause/reprise et seek reconstruisent ou conservent les enfants
selon le contrat de l'horloge. Les siblings sont ordonnes par priorite croissante,
avec insertion avant l'ancien en cas d'egalite comme `InsertRuntimeChild`.
Aucun handle n'est reutilise et une erreur de construction/mise a jour vide
explicitement la foret plutot que publier un etat partiel trompeur.

`SequenceCommandQueue` reproduit la FIFO owner-thread de 500 entrees et le niveau
Collect/Execute, y compris un Execute surnumeraire negatif. Start cree toujours
une nouvelle instance; Stop et SetEndingAction ciblent le couple DataId/priorite
16 bits, top-level ou arbre entier, avec offset de donnees nul. L'audit des
callers Monopoly ne montre aucun chain ID actif; les listes waiting/dechained ne
sont donc pas inventees. Les commandes MoveTheWorks/MoveXY/MoveRySTxz et SetCamera sont portees.
L ownership des labels camera suit `LE_SEQNCR_LabelArray`: le dernier demarre prend
le label et sa suppression ne restaure pas un overlap plus ancien. Les labels
generiques, callbacks et chains restent partiels. `GetInfo` et
`GetChildMeshWorldMatrix` couvrent maintenant les champs/parcours effectivement lus par Monopoly.
`ForceRedraw` est porte dans la FIFO et le runtime avec un redraw transitoire
source-compatible et propagation vers les ancetres pendant l update.

`SequenceTransforms` porte les conventions row-vector 2D/3D de `L_Matrix.cpp`,
les six attributs offset/matrix/OSRT et la composition local puis parent. Les
tweekers identity, constant et linear appliquent leur transformation avant le
calcul de position du parent (`L_Seqncr.cpp:5484-5672,5789-6275`). Le chunk
prive 139 `3D_MESH_CHOICE` est maintenant un etat non matriciel distinct :
constant/linear le mettent a jour, A/B restent ceux de la premiere cle et la
proportion seule interpole sans clamp; Identity n'efface pas cet etat.

Les feuilles 3D publient la pose MIMe evaluee jusqu'au slot World3D 1. La scene
SDL_GPU conserve indices/textures statiques et alloue un vertex buffer anime par
node, mis a jour par cycling puis prune avec le lifecycle. Un readback D3D12
prouve que le changement de pose deplace reellement les pixels rasterizes. Le record camera 7, le FOV 144 et SetCamera atteignent maintenant la camera
de projection utilisee par la frame; un label absent conserve la camera precedente.
Les effets son/callbacks et les objets sequence non encore consommes restent
explicitement partiels. La visibility 3D a ete auditee : le renderer original
renvoie toujours TRUE a `SequenceMoved`, donc aucun feedback de culling n est ajoute.

### Validation historique au 16 septembre 2026

Apres le raccord final du lot `UDOpts` Load/Save, la cible **Debug `MonopolyModern` compile PASS** avec MSVC/Visual Studio 18 2026 et SDL 3.4.14. La nouvelle cible `MonopolyOptionsSaveRuntimeTests` compile egalement PASS.

Le CTest cible `Monopoly.OptionsSaveRuntime` est **1/1 PASS**. La campagne globale de ce snapshot est **106/106 PASS, 0 echec**, executee le 16 septembre 2026 apres le raccord des cinq slots, de la persistance `.msv/.sgd`, du playback du dialogue et des corrections de pending-save. Elle remplace, pour ce snapshot historique, l ancien resultat 103/103.

Les lots `VoiceChatAudioRuntime`, `AuctionTextPlayback`/`MoneyFormat`, les surfaces texte Trade/IBar et `FontRuntime::wrap`, ainsi que le correctif du generateur de progression ont
ete codes **apres** cette campagne. Conformement a la consigne alors en vigueur, aucune
compilation ni aucun test n'etait attribue a ces changements; 106/106 reste donc
la preuve propre au snapshot du 16 septembre, et non une validation du HEAD actuel.

La premiere tentative de build parallele de cette campagne a rencontre une contention MSVC `C1041` sur le PDB parce qu une execution precedente et une relance se chevauchaient. Apres arret de ce doublon, le build mono-job a termine normalement; ce point est un artefact d orchestration locale et non un echec du code du port.

### Validation consolidee au 13 septembre 2026

Le dernier resultat **CTest global 92/92 PASS** retrouve correspond au lot
`18eecae`, avant les trois derniers commits. Le journal local
`modern/build/Testing/Temporary/LastTest.log` se termine le 13 septembre a 15:08
et contient 92 succes, zero echec. Le run associe rapporte une reconstruction
Debug complete; le run du matin a aussi reconstruit entierement le build Ninja
`modern/build-phase-c` avant ses 92/92.

Ce resultat ne qualifie donc pas globalement `0779b79`, `c20ee4b` et `62d4a84`.
Les preuves plus recentes sont les tests cibles et compilations distingues dans
le bilan de la journee. Compiler une cible de tests ne signifie pas l'executer;
en particulier, les parcours enchere/tour/penurie/dette demandent encore des
regressions dediees et une execution globale apres reconstruction du dernier code.

Les preuves GPU sur fixtures montrent un vrai rendu D3D12, sans skip GPU.
Deux branches `ResourcePaths` restent sautees pour des contraintes de l'hote
(casse et droits de symlink). Aucun run ne demontre une partie retail complete,
ni Linux/macOS ou Vulkan/Metal. Cette consolidation documentaire ne relance
pas le build ni CTest et n'attribue aucun resultat ancien au dernier checkpoint.

### Validation de reference du 10 septembre 2026 (historique)

Le 2026-09-10, avec MSVC 19.51 / toolset 14.51, Visual Studio 18 2026,
SDL 3.4.14 et zlib 1.3.2 :

- regeneration CMake du build Visual Studio puis reconstruction **clean-first
  complete** de `modern/build`, code de sortie zero, incluant
  `MonopolyModern.exe`, `MonopolyDataCore` et `MonopolyGPU3DCore` ;
- suite complete depuis ce build neuf : **87/87 suites passees**, zero echec ;
- le noyau AI economique valide `AI_Should_Unmortgage_Property`, `AI_Should_Buy_House` et `AI_Get_Cash_Multiplier`, avec budget exact, penurie/proximite, simulation prealable et interpolation retail volontairement atypique ;
- les suites `MeshGPUResources`, `World3DGPUScene`, `World3DRenderer` et
  `World2DRenderer` utilisent un vrai device SDL_GPU **Direct3D 12** sans
  passing skip ;
- le readback 2D alloue une cible R8G8B8A8, attend une fence GPU puis mappe les
  pixels reels. Une fixture BMP synthetique 2x2 conserve exactement bleu/blanc/
  rouge/vert, puis `DiceDisplay::plan2D` deplace les deux instances de `-35` et
  `-11`; le pixel d'origine est efface et les pixels attendus sont presents aux
  nouvelles positions ;
- le meme test verrouille cache texture partage, priorites, ordre des siblings,
  coordonnees logiques 800x600, scale 1600x1200, letterbox 1000x600 et purge du
  cache apres Stop ;
- `Dice2DPlayback` verrouille `CurrentDiceID`, `CurrentBobDice`, ordre
  Stop/Start/Move, consommation historique de `DiceRollNotification`, bobbing
  gauche/droit, DropDropFrames uniquement a droite et LoopToBeginning ;
- le sequenceur accepte maintenant un `DataUAP` brut comme racine bitmap 2D infinie a 60 Hz / StayAtEnd, exactement comme `LI_SEQNCR_StartUpSequence`; le decodeur `NEWBITMAPHEADER` conserve raster top-down, stride DWORD, palette/alpha et origine signee, puis publie du straight RGBA8 dans Overlay2D ;
- les surfaces `DataNative` runtime disposent maintenant d un registre RGBA8 versionne distinct des banques DAT, avec blits Replace/SourceOver, clipping, publication live dans Overlay2D et `transitionXY` atomique Stop/Start/Move ;
- `BoardBackdropPlayback` reproduit le cache 2D UDBoard USA : 4 buffers Main 800x450, 1 buffer Portfolio/Trade 400x225, `BMP_mybs*`/`BMP_mybss*`, priorite 10, offsets viewport et eviction historique; l index DataBMP est `camera + 39*city` pour les villes USA 0..10, le cache Main est `(city,camera)` et les villes invalides sont rejetees transactionnellement ;
- le board 3D USA selectionne maintenant `HMD_boardmed` pour city 0 et `HMD_board_citymed` pour toute valeur non nulle, contrat source 992-995 verrouille dans `TextureCatalogTests` ;
- `BoardLightingController` et `World3DRenderer` portent le bloc LIGHTING UDBoard : ambient 0.53/0.84, deux directionnelles, spotlight Y+200 avec range/attenuation/falloff/theta/phi, focus 210 ticks, suivi anime 0.3, idle strict >25 s, couleur joueur 0.003/tick; HLSL/MSL sont alignes, DXIL regenere avec le SDK 10.0.26100 et le readback D3D12 prouve eclairage directionnel, spotlight dans le cone et absence hors cone ;
- `viewportBackgroundFillOn` suit maintenant `game3DOn` comme `UDBoard.cpp:913-966`; `GPUFrame` ne reblitte le `BackGround.bmp` 3D que lorsque ce clear/fill est actif, tandis que le mode plateau 2D laisse ce remplissage desactive ;
- `UDBOARD_SelectAppropriateView` est verrouille pour les 40 cases, le fallback prison/off-board `>39 -> 10` et la vue prison fixe; les deux notifications UDIBar actives publient maintenant ces cameras, tandis que le preload 2D reste volontairement absent car son caller retail est sous `#if 0` ;
- `UDPSEL` couvre maintenant RulesChoice Standard/Custom de bout en bout : hotspots exacts, preset Standard retail complet, ACTION_ACCEPT_CONFIGURATION v1 avec blob GameOptions, decode/validation transactionnels cote RULE, reset d acceptation + proposer sur changement, NotifyProposedConfiguration avec blob et mise a jour sure de la projection UI; le helper PlayerSet choisit le premier humain local comme le Userifce.cpp original ;
- `UDPSEL CustomizeRules` verrouille maintenant les 21 controles de regle, Restore Standard, Short Game, le rectangle retail de Okay, les messages interim `numberD=1` et final `numberD=0`, ainsi que l exactitude du blob GameOptions et le rejet transactionnel des choix hors plage; les hotspots calcules depuis les metriques LANG/font restent differes volontairement ;
- `UDPSEL SelectPlayer` verrouille la liste historique pure : 100 entrees maximum, filtrage des noms deja en partie, troncature 10 caracteres, page de 8, wrap More, huit rectangles de cartes plus New/More exacts et transitions New -> EnterName / carte -> SelectToken; la persistance INI et HiScore reste hors de ce checkpoint ;
- `UDPieces PieceShadowDisplay` verrouille les 11 sequences `CNK_shadowa..k`, la priorite 100+player, le suivi X/Z/yaw de la pose token runtime avec Y=1, les gates board/lighting/game3DOn, la coupure paddywagon state 8, l exception off-board pendant animation et le rejet transactionnel des tokens invalides ;
- `UDAuct` dispose maintenant d un chemin UI/Overlay2D reel : notifications `NotifyNewHighBid`/`NotifyAuctionGoing`, roll-call, retour de vue et third-going, geometrie des panneaux 4/5+ joueurs, hit-testing des sept billets et `ACTION_BID` sont verrouilles; `AuctionPlayback` rend bottom bar, deed ville/maison/hotel, panneaux, trays, bid plates et tokens avec les priorites 314/315+i/316+i/330; `AuctionPennyBagsPlayback` couvre les CNK `an01..17`, l enchainement first-auction/later-auction, la detection `endTime <= sequenceClock`, les interruptions Highest/Going/Sold, Congrats, EndAuction et le retour exact a `formerView`; la voix d hote est maintenant emise en synchro animation avec ClipOldSoundIfPlaying, y compris les offsets specifiques token/pb277/pb279 et le garde anti-relecture retail; `Begin` envoie `I_AM_HERE` a chaque joueur local attendu, IA locale comprise, avec serial conserve et preflight FIFO; les echecs ressource/FIFO/sender ne publient aucun etat partiel ;
- `UDTrade/TradeUI` couvre maintenant selection partenaire, cash, jail cards, deeds/proprietes, Propose/Cancel, `Trade_SendItems` et futures/immunites; `TradePropertyPlayback` publie les quatre boites de deeds statiques utilisees par le retail (before A/B + offered A/B), avec assets PAT normaux/hypotheques, priorites 324/374/424/474, positions/ordre sources, MoveXY lors des recompositions et rollback complet si ressource/FIFO insuffisante. Le `movingcard` porte aussi les quatre pas de 25 ms, les priorites 626/676/726/776 et le verrou input local. `TradeOfferIconPlayback` rend les cartes prison DAT_MAIN `TAB_ibjlcdf0/1` et les icones DAT_LANG2 `TAB_syfut`/`TAB_syimm` aux priorites 275..278 et positions retail, avec preflight FIFO et retrait exact hors Trade. `TradeCashDialogPlayback` rend le fond `TAB_tnmtry00`, les CNK idle/pressed Clear/Okay/Cancel aux priorites 1975/1976/1977, les positions A/B exactes et le `CashTradeDying` historique avec `EndingActionStop`; les clics restent modaux pendant la fermeture et le popup n est retire qu apres la fin du feedback. `TradeContractDialogPlayback` rend aussi les deux fleches DAT_MAIN `CNK_byahaupi`/`CNK_byahadni` partagees par Future/Immunity a la priorite 525 et positions sources, avec preflight FIFO et retrait exact hors dialogue/Trade. `TradeBackdropPlayback` ajoute `CNK_tradebg`, `CNK_tndispbd`, `CNK_tyttft00`, `CNK_tnarrow0`, `CNK_tydispll/r`, les rails `tnclrl/tnclrr` selon la couleur joueur et les boutons `tycrtnew`/`tyfutres`/`tyimmty` conditionnels. `TradeTokenPlayback` ajoute les jetons joueurs DAT_MAIN `TAB_inpsa+token`, priorites 274/275, A `(10,235)` et B aligne a droite par largeur bitmap reelle (`790-width`). `TradeActionButtonPlayback` ajoute les idle/out Propose/Cancel aux priorites 202/201 et `EndingActionStop`; a cette date de validation historique, `leftpanel/rightpanel/NameA/NameB` restaient encore des surfaces GRAFIX/FONTS non rendues. Les ensembles after A/B restent logiques car leur repositionnement graphique 4/5 est commente dans `UDTrade.cpp`;
- le routage input respecte maintenant `UDAUCT -> UDBOARD -> UDIBAR -> UDPSEL -> UDTRADE`; les mouvements SDL transportent X/Y et DeltaX/DeltaY dans le repere logique 800x600, et le controle manuel souris de la camera 3D est teste de bout en bout avec release strictement apres 20 secondes ;
- `BoardOwnershipHighlight` verrouille l ordre des 28 proprietes, les formules `DAT_BOARD/DAT_BOARD2`, les HMD ownership/mortgage, les quatre orientations, les priorites `12+square`, le remplacement `2D -> 3D -> Stop`, l origine UAP `StartCXYSlot` et l atomicite en cas de ressource/FIFO insuffisante ;
- `IBarRuleState` verrouille les valeurs 0..26 de `IBAR_STATES`, les quatre
  variantes de sortie de prison, les transitions notification/action acceptee et
  le reset; `UserInterface` expose cette projection au moteur ;
- le playback des boutons UDIBar conserve les priorites 999/1000/1001/1002,
  les atlas couleur/gris, `IBarIsStable` et le cycle outgoing-only
  `IBAR_JustChanged`; Bankrupt, Build/Sell/Mortgage/Unmort, AucHouse/AucHotel,
  PlaceHouse/PlaceHotel et TradeAcc/TradeCnt/TradeRej rejoignent les feuilles
  Overlay2D attendues; les disponibilites BSSM sont calculees depuis le
  `GameState` et `RuleBuildings::testBuildingPlacement` ;
- `IBarLayout` verrouille les neuf rectangles d'action originaux et les variantes
  BuyAuction/TaxDecision/Trading, y compris le chevauchement d'un pixel du source
  filtre par le masque des slots visibles et les bornes right/bottom exclusives ;
- `IBarActionInput` prouve le filtrage souris par masque actif et les actions directes
  Buy/Auction, taxe, prison, Trade, Bankrupt et shortage; les sous-etats BSSM
  restent locaux a l'IBar et reviennent a la projection RULE lorsqu'elle change ;
- `IBarPropertyPlayback` reproduit `propconv`, `IBARPropertyBarOrder`, les 28 titres
  DAT_MAIN, leur ordre de recouvrement 41->0, `DeedActive` et les actions rapides ;
- le feedback `Pressed` des boutons est alimente par `NotifyActionCompleted`, conserve
  l'index legacy du bouton et est consomme sans redemarrage parasite ;
- le mouseover deed UDIBar respecte le delai strict `>36`, la priorite 1003,
  `StartXY(540,130)`, les variantes normale/hypothequee et l'absence de blow-up
  pour un titre low-colour ;
- `IBarCardPlayback` couvre les 32 cartes USA et les 39 cameras : deck-out DAT_MAIN,
  card-in/face/idle/out DAT_LANG2, priorite 1005, DropDropFrames, y=0 sur Main,
  y=136 hors Main, `StayAtEnd` puis `Stop`; Chance et Community Chest parcourent
  le lifecycle complet et `CardSeen`/`NotifyPutAwayCard` conduisent a `Out` ;
- `IBarScoreStripPlayback` porte les feuilles statiques des score boxes : atlas token/couleur
  large/petite, barreaux de prison, positions 1..6 joueurs, priorites 257+t/305+t/306+t
  et hover +1; a cette date de validation historique, le snapshot texte conservait cash/nom, gate exact de 20 ticks et direction
  CashUp/CashDown sans encore creer de surface ou glyphes ;
- le hit-test joueur/banque partage l etat `playerCurrentMouseOver` du source; le bank hover
  atteint reellement Overlay2D par `MoveXY(755,561)` et revient a y=560 ;
- deux branches ResourcePaths dependent de l'hote restent `[SKIP]` sur ce
  Windows (collision de casse et creation de symlink sans droit). Elles ne sont
  pas presentees comme validees.

Les regressions reproduites puis corrigees restent verrouillees par tests :
chemins `fichier/.`, expirations de timers 0/1 restant dans la file UI, reference
sequence indirecte vers son propre item et fixture tweeker dont le flag
drop-frames etait initialement incoherent avec son attente. Les tests DATA/LANG
couvrent aussi banques manquantes/tronquees, index invalide, rollback et duree
de vie des snapshots.

La validation du dernier checkpoint et les trois indicateurs de progression
sont distingues dans le bilan du 13 septembre et la validation consolidee
ci-dessus; les resultats historiques ne valent pas validation des lots suivants.

Preuve historique distincte (2026-08-24, MSVC 19.50) : build autonome sans
arbre `Source`, 16/16 suites, dix manifestes (46 423 entrees) et 1 001 BMP.
Ni Linux ni macOS n'ont ete compiles pour le jalon actuel. Vulkan/Metal ne sont
pas valides ici; le SPIR-V perime n'est plus distribue comme s'il correspondait
au shader texture actuel. Les DAT/HMD retail exacts restent absents, donc les
fixtures synthetiques prouvent les contrats source mais pas un contenu retail
indisponible.

## Mise a jour et validation du checkpoint du 23 septembre 2026

Depuis le checkpoint du 20 septembre, le travail a porte principalement sur la
fermeture des chemins UI visibles et sur l engagement des dernieres familles
legacy encore non demarrees. `833f414` valide le runtime texte partage;
`4ab3ecd` ferme le chemin **UDOpts Load Board / custom board** de bout en bout :
inventaire `custbrds`, pagination, verification du DWORD de securite de
l editeur, `city=-1`, `customRoot` 2D/3D et invalidation transactionnelle des
caches. Les 39 BMP `2DBoards` sont prevalides avant publication; Trade et
Portfolio conservent le petit board stock comme le retail.

Le texte runtime a ensuite ete consolide sur plusieurs ecrans : `357a9d4`
valide le panneau Trade Future/Immunity; `f485145` ajoute le panneau texte
Future/Immunity de `UDStats`; `fbf3098` porte le texte du floater Deed et
`24fa571` les valeurs numeriques des barres Deed. `09658b8` raccorde aussi le
pointeur `TAB_pointer` a Overlay2D et le curseur I-beam du champ Chat, avec
fallback natif et transitions transactionnelles.

Enfin, `a797312` engage les trois dernieres familles de la matrice :
`L_voice.cpp` via `VoiceChatLegacyContract`, `Debugart.cpp` via
`DebugDialogs` et `L_Video.*` via `VideoRuntime`. Le dernier lot ne pretend
pas fermer ces familles : la voix conserve des qualifications device/reseau,
`ErrorExit` reste sans caller actif, et la video n a encore ni decodeur
pixels/audio ni raccordement aux opcodes video du sequenceur.

La preuve executable courante est maintenant **126/126 suites CTest passees,
zero echec** sur Windows/MSVC Debug apres le lot `a797312`. Elle remplace le
checkpoint intermediaire 119/119 comme preuve du HEAD actuel. Les controles
Git de fin de lot sont propres et `Source/` n a pas ete modifie.

Etat structurel courant : **41/41 familles engagees = 100 %**, **63 % d indice
automatique**, soit 18 entrees completes/remplacees, 50 partielles et
0 non demarree. Ces nombres ne signifient pas que le portage global est termine :
le dernier audit fonctionnel global reste le snapshot historique **75 % du
14 septembre** tant qu un nouvel audit fonctionnel du HEAD n a pas ete mene.

Les DAT/HMD retail exacts restent absents de cette preuve. Elle ne remplace donc
ni une partie complete avec contenu retail, ni la qualification visuelle des
plateaux/menus, ni un essai voice-chat entre machines avec peripheriques audio
physiques, ni un essai custom-board avec un vrai Board Editor et ses assets.

## Prochaines priorites requalifiees

1. **Recalculer l audit fonctionnel global du HEAD.** L engagement des familles
   est termine; le prochain indicateur utile est maintenant l inventaire des
   50 entrees `PORTED_PARTIAL` classees par impact sur un parcours de partie
   reel. Ne pas extrapoler le 75 % historique ni le 63 % structurel.
2. **Fermer d abord les manques visibles sur le parcours jouable**, en suivant
   cet audit : surfaces 2D/GRAFIX encore partielles, popup calculateur
   `UDStats`, datasets Bank externes/Account History lorsqu ils ont un owner
   moderne, puis les phases `UDPsel` encore non auditees. Les chemins
   `UDOpts` Credits, Quick Help, Full Help et custom-board sont maintenant
   implementes; ils passent de priorites de portage a priorites de qualification.
3. **Completer le chemin video ArtLib** si la parite retail des opening movies
   reste dans la cible : raccorder `L_Seqncr` a `VideoRuntime`, decoder et
   presenter les frames/audio, puis valider timing, loop, cut et alternatives.
   Le parseur AVI/status seul ne constitue pas une lecture video effective.
4. **Conserver GIS-8 et GIS-9 comme chantiers de qualification integree** :
   GIS-8 avec deux processus puis deux machines et audio physique; GIS-9 avec
   assets retail pour les changements USA/Europe, devise/langue et maintien des
   UV. Le custom-board rejoint ce lot de qualification avec Board Editor reel.
   **GIS-10 reste clos** tant qu aucun nouveau caller ou contenu HMD ne prouve
   qu un type supplementaire doit etre decode.

Les travaux `LEGACY_UNUSED` et les primitives HMD sans caller restent hors
priorite. Les differences fines GDI/FreeType ne doivent etre traitees qu apres
les ecarts fonctionnels et visuels reproductibles.
