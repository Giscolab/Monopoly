cmake_minimum_required(VERSION 3.25)

project(
    MonopolyModern
    VERSION 0.1.0
    LANGUAGES C CXX
)

set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

if(MSVC)
    # CMake's MSVC probe can decode the French UTF-8 nonbreaking spaces as
    # OEM text. Ninja then records zero header dependencies and retains stale
    # ABI objects. Repair only this detected corruption; other locales retain
    # their compiler-probed prefix (an English language pack is not required).
    if(CMAKE_GENERATOR MATCHES "Ninja")
        string(REPLACE "┬á" " " CMAKE_CL_SHOWINCLUDES_PREFIX
            "${CMAKE_CL_SHOWINCLUDES_PREFIX}")
    endif()
    # MSVC does not enable standard C++ stack unwinding for these Ninja
    # targets automatically in the current VS 18 toolchain.
    add_compile_options(
        $<$<COMPILE_LANGUAGE:CXX>:/EHsc>
    )
endif()

include(CTest)

# Keep generated shader assets beside the application, independent of cwd.
# configure_file also registers shader changes as build dependencies.
# The checked-in SPIR-V fragment still describes the pre-texture contract;
# do not package it until it can be regenerated by a SPIR-V-capable compiler.
foreach(WORLD_SHADER IN ITEMS World3D.vert.dxil World3D.frag.dxil
    World3D.vert.msl World3D.frag.msl)
    configure_file("shaders/generated/${WORLD_SHADER}"
        "shaders/${WORLD_SHADER}" COPYONLY)
endforeach()
file(REMOVE
    "${CMAKE_CURRENT_BINARY_DIR}/shaders/World3D.vert.spv"
    "${CMAKE_CURRENT_BINARY_DIR}/shaders/World3D.frag.spv")

include(FetchContent)

set(
    MONOPOLY_LEGACY_SOURCE_ROOT
    "${CMAKE_CURRENT_SOURCE_DIR}/../Source"
)

set(MONOPOLY_LEGACY_MANIFESTS_AVAILABLE TRUE)

foreach(LEGACY_MANIFEST_HEADER IN ITEMS
    dat_3d.h
    dat_bord.h
    DAT_BORDE.h
    dat_brd2.h
    dat_lk01.h
    dat_lk02.h
    dat_lm01.h
    dat_lm02.h
    dat_main.h
    dat_pat.h
)
    if(NOT EXISTS
        "${MONOPOLY_LEGACY_SOURCE_ROOT}/monopoly/Dat_Mon/${LEGACY_MANIFEST_HEADER}"
    )
        set(MONOPOLY_LEGACY_MANIFESTS_AVAILABLE FALSE)
    endif()
endforeach()

set(MONOPOLY_LEGACY_TEXTURE_CORPUS_AVAILABLE TRUE)

foreach(LEGACY_TEXTURE_DIRECTORY IN ITEMS
    Boards
    Cities
    Currency
    Languages
)
    if(NOT IS_DIRECTORY
        "${MONOPOLY_LEGACY_SOURCE_ROOT}/monopoly/${LEGACY_TEXTURE_DIRECTORY}"
    )
        set(MONOPOLY_LEGACY_TEXTURE_CORPUS_AVAILABLE FALSE)
    endif()
endforeach()

if(
    EXISTS "${MONOPOLY_LEGACY_SOURCE_ROOT}/monopoly/BackGround.bmp"
    AND EXISTS
        "${MONOPOLY_LEGACY_SOURCE_ROOT}/monopoly/BackGroundEuro.bmp"
)
    set(MONOPOLY_LEGACY_BACKGROUNDS_AVAILABLE TRUE)
else()
    set(MONOPOLY_LEGACY_BACKGROUNDS_AVAILABLE FALSE)
endif()

# Source/ZLib/ZImplode.c proves that the historical implode/explode names
# wrap ordinary zlib streams (windowBits = 15). Prefer an installed package;
# otherwise build the pinned upstream release, never the private SDL copy.
find_package(ZLIB 1.2.9 QUIET)

if(ZLIB_FOUND)
    set(MONOPOLY_ZLIB_TARGET ZLIB::ZLIB)
else()
    set(ZLIB_BUILD_TESTING OFF CACHE BOOL "" FORCE)
    set(ZLIB_BUILD_SHARED OFF CACHE BOOL "" FORCE)
    set(ZLIB_BUILD_STATIC ON CACHE BOOL "" FORCE)
    set(ZLIB_INSTALL OFF CACHE BOOL "" FORCE)

    FetchContent_Declare(
        zlib
        GIT_REPOSITORY https://github.com/madler/zlib.git
        GIT_TAG v1.3.2
        GIT_SHALLOW TRUE
    )

    FetchContent_MakeAvailable(zlib)
    set(MONOPOLY_ZLIB_TARGET ZLIB::ZLIBSTATIC)
endif()

FetchContent_Declare(
    SDL3
    GIT_REPOSITORY https://github.com/libsdl-org/SDL.git
    GIT_TAG release-3.4.14
    GIT_SHALLOW TRUE
)

FetchContent_MakeAvailable(SDL3)

add_library(
    MonopolyDataCore
    STATIC
    src/DataBanks.cpp
    src/LegacyDataArchive.cpp
    src/LegacyDataArchiveBuilder.cpp
    src/LegacyChunk.cpp
    src/LegacySequence.cpp
    src/SequenceClock.cpp
    src/SequenceChildSchedule.cpp
    src/SequenceRuntime.cpp
    src/SequenceCommands.cpp
    src/SequencePlayback.cpp
    src/SequenceTransforms.cpp
    src/LegacyMeshData.cpp
    src/MeshRuntime.cpp
    src/SequenceRenderData.cpp
    src/SequenceBitmapRenderData.cpp
    src/BitmapRuntime.cpp
    src/RuntimeBitmapSurface.cpp
    src/SequenceWorld2DSlot.cpp
    src/World3DProjection.cpp
    src/SequenceWorld3DSlot.cpp
    src/LanguageResources.cpp
    src/ResourcePaths.cpp
    src/ResourceRuntime.cpp
    src/LegacyManifest.cpp
    src/LegacyBitmap.cpp
    src/TextureCatalog.cpp
)

target_include_directories(
    MonopolyDataCore
    PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}/src
)

target_link_libraries(
    MonopolyDataCore
    PUBLIC
    ${MONOPOLY_ZLIB_TARGET}
)

if(MSVC)
    target_compile_options(
        MonopolyDataCore
        PRIVATE
        /W4
        /permissive-
    )
endif()

add_library(
    MonopolyGPU3DCore
    STATIC
    src/MeshGPUResources.cpp
    src/World3DGPUScene.cpp
    src/World3DShaderAssets.cpp
    src/World3DPipeline.cpp
    src/World3DRenderer.cpp
    src/BoardLightingController.cpp
    src/World2DRenderer.cpp
    src/LogicalViewport.cpp
)

target_include_directories(
    MonopolyGPU3DCore
    PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}/src
)

target_link_libraries(
    MonopolyGPU3DCore
    PUBLIC
    MonopolyDataCore
    SDL3::SDL3
)

if(MSVC)
    target_compile_options(
        MonopolyGPU3DCore PRIVATE /W4 /permissive-
    )
endif()

add_executable(
    MonopolyManifestTool
    src/ManifestTool.cpp
)

target_link_libraries(
    MonopolyManifestTool
    PRIVATE
    MonopolyDataCore
)

if(MSVC)
    target_compile_options(
        MonopolyManifestTool
        PRIVATE
        /W4
        /permissive-
    )
endif()

if(BUILD_TESTING)

    add_executable(
        MonopolyLegacyManifestTests
        tests/LegacyManifestTests.cpp
    )

    target_link_libraries(
        MonopolyLegacyManifestTests
        PRIVATE
        MonopolyDataCore
    )

    if(MONOPOLY_LEGACY_MANIFESTS_AVAILABLE)
        target_compile_definitions(
            MonopolyLegacyManifestTests
            PRIVATE
            MONOPOLY_LEGACY_SOURCE_DIR="${MONOPOLY_LEGACY_SOURCE_ROOT}"
        )
    endif()

    if(MSVC)
        target_compile_options(
            MonopolyLegacyManifestTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.LegacyManifest
        COMMAND MonopolyLegacyManifestTests
    )

    add_executable(
        MonopolyLegacyBitmapTests
        tests/LegacyBitmapTests.cpp
    )

    target_link_libraries(
        MonopolyLegacyBitmapTests
        PRIVATE
        MonopolyDataCore
    )

    if(MONOPOLY_LEGACY_TEXTURE_CORPUS_AVAILABLE)
        target_compile_definitions(
            MonopolyLegacyBitmapTests
            PRIVATE
            MONOPOLY_LEGACY_SOURCE_DIR="${MONOPOLY_LEGACY_SOURCE_ROOT}"
        )
    endif()

    if(MSVC)
        target_compile_options(
            MonopolyLegacyBitmapTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.LegacyBitmap
        COMMAND MonopolyLegacyBitmapTests
    )

    add_executable(
        MonopolyTextureCatalogTests
        tests/TextureCatalogTests.cpp
    )

    target_link_libraries(
        MonopolyTextureCatalogTests
        PRIVATE
        MonopolyDataCore
    )

    if(MSVC)
        target_compile_options(
            MonopolyTextureCatalogTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    if(MONOPOLY_LEGACY_TEXTURE_CORPUS_AVAILABLE)
        add_test(
            NAME Monopoly.TextureCatalog
            COMMAND MonopolyTextureCatalogTests
                "${MONOPOLY_LEGACY_SOURCE_ROOT}/monopoly"
        )
    else()
        add_test(
            NAME Monopoly.TextureCatalog
            COMMAND MonopolyTextureCatalogTests
        )
    endif()

endif()

add_executable(
    MonopolyModern
    src/main.cpp
)

target_link_libraries(
    MonopolyModern
    PRIVATE
    SDL3::SDL3
    MonopolyDataCore
    MonopolyGPU3DCore
)

if(MSVC)
    target_compile_options(
        MonopolyModern
        PRIVATE
        /W4
        /permissive-
    )
endif()

# ============================================================
# ArtLib 60 Hz timer -> UIMessages contract
# ============================================================

if(BUILD_TESTING)

    add_executable(
        MonopolyUIMessagesTests
        tests/UIMessagesTests.cpp
        src/UIMessages.cpp
    )

    target_include_directories(
        MonopolyUIMessagesTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )

    if(MSVC)
        target_compile_options(
            MonopolyUIMessagesTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.UIMessages
        COMMAND MonopolyUIMessagesTests
    )

    add_executable(
        MonopolyTimerTests
        tests/TimerTests.cpp
        src/Timers.cpp
        src/UIMessages.cpp
    )

    target_include_directories(
        MonopolyTimerTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )

    if(MSVC)
        target_compile_options(
            MonopolyTimerTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.Timers
        COMMAND MonopolyTimerTests
    )

endif()

# TARGET_RUNTIME_DLLS is empty on non-DLL platforms and with static SDL.
# Do not invoke copy_if_different with only a destination in those builds.
if(WIN32 AND SDL_SHARED)
    add_custom_command(
        TARGET MonopolyModern
        POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_if_different
            $<TARGET_RUNTIME_DLLS:MonopolyModern>
            $<TARGET_FILE_DIR:MonopolyModern>
        COMMAND_EXPAND_LISTS
    )
endif()


target_sources(
    MonopolyModern
    PRIVATE
    src/Application.cpp
    src/Game.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/Engine.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/RenderSlots.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/UDUtils.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/MousePointer.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/Timers.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/RuntimeState.cpp
    src/ExtendedInitialization.cpp
)

# ============================================================
# Legacy L_Data identifiers and bank catalog contracts
# ============================================================

if(BUILD_TESTING)

    add_executable(
        MonopolyLegacyDataContractTests
        tests/LegacyDataContractTests.cpp
    )

    target_include_directories(
        MonopolyLegacyDataContractTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )

    target_link_libraries(
        MonopolyLegacyDataContractTests
        PRIVATE
        MonopolyDataCore
    )

    if(MSVC)
        target_compile_options(
            MonopolyLegacyDataContractTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.LegacyDataContract
        COMMAND MonopolyLegacyDataContractTests
    )

    add_executable(
        MonopolyLegacyDataArchiveTests
        tests/LegacyDataArchiveTests.cpp
    )

    target_link_libraries(
        MonopolyLegacyDataArchiveTests
        PRIVATE
        MonopolyDataCore
    )

    if(MSVC)
        target_compile_options(
            MonopolyLegacyDataArchiveTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.LegacyDataArchive
        COMMAND MonopolyLegacyDataArchiveTests
    )

    add_executable(
        MonopolyLegacyChunkTests
        tests/LegacyChunkTests.cpp
    )

    target_link_libraries(
        MonopolyLegacyChunkTests
        PRIVATE
        MonopolyDataCore
    )

    if(MSVC)
        target_compile_options(
            MonopolyLegacyChunkTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.LegacyChunk
        COMMAND MonopolyLegacyChunkTests
    )

    add_executable(
        MonopolyLanguageResourcesTests
        tests/LanguageResourcesTests.cpp
    )

    target_link_libraries(
        MonopolyLanguageResourcesTests
        PRIVATE
        MonopolyDataCore
    )

    if(MSVC)
        target_compile_options(
            MonopolyLanguageResourcesTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.LanguageResources
        COMMAND MonopolyLanguageResourcesTests
    )

    foreach(RESOURCE_SUITE IN ITEMS ResourcePaths ResourceRuntime LegacySequence LegacyMeshData MeshRuntime SequenceClock SequenceChildSchedule SequenceRuntime SequenceCommands SequenceTransforms SequenceRenderData World3DProjection SequenceWorld3DSlot)
        add_executable(
            Monopoly${RESOURCE_SUITE}Tests
            tests/${RESOURCE_SUITE}Tests.cpp
        )
        target_link_libraries(
            Monopoly${RESOURCE_SUITE}Tests PRIVATE MonopolyDataCore
        )
        if(MSVC)
            target_compile_options(
                Monopoly${RESOURCE_SUITE}Tests PRIVATE /W4 /permissive-
            )
        endif()
        add_test(
            NAME Monopoly.${RESOURCE_SUITE}
            COMMAND Monopoly${RESOURCE_SUITE}Tests
        )
    endforeach()

    add_executable(
        MonopolyResourceLifecycleTests
        tests/ResourceLifecycleTests.cpp
        src/Game.cpp
        src/ExtendedInitialization.cpp
        src/Timers.cpp
        src/UIMessages.cpp
    )
    target_link_libraries(
        MonopolyResourceLifecycleTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyResourceLifecycleTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.ResourceLifecycle
        COMMAND MonopolyResourceLifecycleTests
    )

    add_executable(
        MonopolyResourceConfigurationTests
        tests/ResourceConfigurationTests.cpp
        src/UDUtils.cpp
    )
    target_link_libraries(
        MonopolyResourceConfigurationTests PRIVATE MonopolyDataCore SDL3::SDL3
    )
    if(MSVC)
        target_compile_options(
            MonopolyResourceConfigurationTests PRIVATE /W4 /permissive-
        )
    endif()
    if(WIN32 AND SDL_SHARED)
        add_custom_command(
            TARGET MonopolyResourceConfigurationTests POST_BUILD
            COMMAND ${CMAKE_COMMAND} -E copy_if_different
                $<TARGET_RUNTIME_DLLS:MonopolyResourceConfigurationTests>
                $<TARGET_FILE_DIR:MonopolyResourceConfigurationTests>
            COMMAND_EXPAND_LISTS
        )
    endif()
    add_test(
        NAME Monopoly.ResourceConfiguration
        COMMAND MonopolyResourceConfigurationTests
    )

endif()

target_sources(
    MonopolyModern
    PRIVATE
    src/UIMessages.cpp
    src/UserInterface.cpp
    src/AuctionUI.cpp
    src/AuctionPlayback.cpp
    src/AuctionPennyBagsPlayback.cpp
    src/TradeUI.cpp
    src/TradePropertyPlayback.cpp
    src/TradeOfferIconPlayback.cpp
    src/TradeCashDialogPlayback.cpp
    src/TradeContractDialogPlayback.cpp
    src/TradeBackdropPlayback.cpp
    src/TradeTokenPlayback.cpp
    src/TradeActionButtonPlayback.cpp
    src/OptionsUI.cpp
    src/OptionsFilePlayback.cpp
    src/OptionsNavigationPlayback.cpp
    src/OptionsOptionPlayback.cpp
    src/OptionsTogglePlayback.cpp
)

if(BUILD_TESTING)

    add_executable(
        MonopolyUserInterfaceRoutingTests
        tests/UserInterfaceRoutingTests.cpp
        src/UserInterface.cpp
        src/IBarRuleState.cpp
        src/RuntimeState.cpp
        src/PieceMoveIngress.cpp
        src/PieceMovePlan.cpp
        src/PieceCamera.cpp
        src/PieceIdleTransition.cpp
        src/DiceIngress.cpp
        src/PiecePlacement.cpp
        src/BoardGeometry.cpp
        src/RuleArchive.cpp
        src/RuleOptions.cpp
        src/TradeUI.cpp
        src/OptionsUI.cpp
        src/IBarLayout.cpp
    )

    target_include_directories(
        MonopolyUserInterfaceRoutingTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )

    if(MSVC)
        target_compile_options(
            MonopolyUserInterfaceRoutingTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.UserInterfaceRouting
        COMMAND MonopolyUserInterfaceRoutingTests
    )

endif()

# UDTrade.cpp / trade.cpp pure editor projection and partner selection
if(BUILD_TESTING)
    add_executable(
        MonopolyTradeUITests
        tests/TradeUITests.cpp
        src/TradeUI.cpp
        src/IBarLayout.cpp
    )
    target_include_directories(
        MonopolyTradeUITests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )
    if(MSVC)
        target_compile_options(
            MonopolyTradeUITests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.TradeUI
        COMMAND MonopolyTradeUITests
    )
endif()

# UDTrade.cpp static PAT deed playback
if(BUILD_TESTING)
    add_executable(
        MonopolyTradePropertyPlaybackTests
        tests/TradePropertyPlaybackTests.cpp
        src/TradePropertyPlayback.cpp
        src/TradeUI.cpp
        src/IBarLayout.cpp
    )
    target_include_directories(
        MonopolyTradePropertyPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyTradePropertyPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyTradePropertyPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.TradePropertyPlayback
        COMMAND MonopolyTradePropertyPlaybackTests
    )
endif()

# UDTrade.cpp jail/future/immunity offer-icon playback
if(BUILD_TESTING)
    add_executable(
        MonopolyTradeOfferIconPlaybackTests
        tests/TradeOfferIconPlaybackTests.cpp
        src/TradeOfferIconPlayback.cpp
    )
    target_include_directories(
        MonopolyTradeOfferIconPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyTradeOfferIconPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyTradeOfferIconPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.TradeOfferIconPlayback
        COMMAND MonopolyTradeOfferIconPlaybackTests
    )
endif()

# UDTrade.cpp persistent cash-dialog sequence playback
if(BUILD_TESTING)
    add_executable(
        MonopolyTradeCashDialogPlaybackTests
        tests/TradeCashDialogPlaybackTests.cpp
        src/TradeCashDialogPlayback.cpp
    )
    target_include_directories(
        MonopolyTradeCashDialogPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyTradeCashDialogPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyTradeCashDialogPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.TradeCashDialogPlayback
        COMMAND MonopolyTradeCashDialogPlaybackTests
    )
endif()

# UDTrade.cpp shared Future/Immunity dialog arrow playback
if(BUILD_TESTING)
    add_executable(
        MonopolyTradeContractDialogPlaybackTests
        tests/TradeContractDialogPlaybackTests.cpp
        src/TradeContractDialogPlayback.cpp
    )
    target_include_directories(
        MonopolyTradeContractDialogPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyTradeContractDialogPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyTradeContractDialogPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.TradeContractDialogPlayback
        COMMAND MonopolyTradeContractDialogPlaybackTests
    )
endif()

# UDTrade.cpp autonomous backdrop, colour rails and contract buttons
if(BUILD_TESTING)
    add_executable(
        MonopolyTradeBackdropPlaybackTests
        tests/TradeBackdropPlaybackTests.cpp
        src/TradeBackdropPlayback.cpp
    )
    target_include_directories(
        MonopolyTradeBackdropPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyTradeBackdropPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyTradeBackdropPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.TradeBackdropPlayback
        COMMAND MonopolyTradeBackdropPlaybackTests
    )
endif()

# UDTrade.cpp player token playback with measured bitmap width
if(BUILD_TESTING)
    add_executable(
        MonopolyTradeTokenPlaybackTests
        tests/TradeTokenPlaybackTests.cpp
        src/TradeTokenPlayback.cpp
    )
    target_include_directories(
        MonopolyTradeTokenPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyTradeTokenPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyTradeTokenPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.TradeTokenPlayback
        COMMAND MonopolyTradeTokenPlaybackTests
    )
endif()

# UDTrade.cpp Propose/Cancel idle and finite-out playback
if(BUILD_TESTING)
    add_executable(
        MonopolyTradeActionButtonPlaybackTests
        tests/TradeActionButtonPlaybackTests.cpp
        src/TradeActionButtonPlayback.cpp
    )
    target_include_directories(
        MonopolyTradeActionButtonPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyTradeActionButtonPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyTradeActionButtonPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.TradeActionButtonPlayback
        COMMAND MonopolyTradeActionButtonPlaybackTests
    )
endif()

# UDOpts.cpp File-screen projection and autonomous sequence playback
if(BUILD_TESTING)
    add_executable(
        MonopolyOptionsUITests
        tests/OptionsUITests.cpp
        src/OptionsUI.cpp
        src/OptionsFilePlayback.cpp
        src/OptionsNavigationPlayback.cpp
        src/OptionsOptionPlayback.cpp
        src/OptionsTogglePlayback.cpp
    )
    target_include_directories(
        MonopolyOptionsUITests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyOptionsUITests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyOptionsUITests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.OptionsUI
        COMMAND MonopolyOptionsUITests
    )
endif()

# UDAuct.cpp notification projection and bill hit testing
if(BUILD_TESTING)
    add_executable(
        MonopolyAuctionUITests
        tests/AuctionUITests.cpp
        src/AuctionUI.cpp
        src/IBarLayout.cpp
    )
    target_include_directories(
        MonopolyAuctionUITests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )
    if(MSVC)
        target_compile_options(
            MonopolyAuctionUITests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.AuctionUI
        COMMAND MonopolyAuctionUITests
    )
endif()

# UDAuct.cpp static PAT/LANG2 sequence playback
if(BUILD_TESTING)
    add_executable(
        MonopolyAuctionPlaybackTests
        tests/AuctionPlaybackTests.cpp
        src/AuctionPlayback.cpp
        src/AuctionUI.cpp
        src/IBarLayout.cpp
    )
    target_include_directories(
        MonopolyAuctionPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyAuctionPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyAuctionPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.AuctionPlayback
        COMMAND MonopolyAuctionPlaybackTests
    )
endif()

# UDAuct.cpp Pennybags graphic state machine / deferred roll-call
if(BUILD_TESTING)
    add_executable(
        MonopolyAuctionPennyBagsPlaybackTests
        tests/AuctionPennyBagsPlaybackTests.cpp
        src/AuctionPennyBagsPlayback.cpp
    )
    target_include_directories(
        MonopolyAuctionPennyBagsPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyAuctionPennyBagsPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyAuctionPennyBagsPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.AuctionPennyBagsPlayback
        COMMAND MonopolyAuctionPennyBagsPlaybackTests
    )
endif()

target_sources(
    MonopolyModern
    PRIVATE
    src/Messaging.cpp
    src/RulesEngine.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/TimeStep.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/PhaseStack.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/RuleStartupActions.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/Display.cpp
    src/PlayerSelection.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/GPUFrame.cpp
    src/LogicalViewport.cpp
)

if(BUILD_TESTING)
    add_executable(
        MonopolyMeshGPUResourcesTests
        tests/MeshGPUResourcesTests.cpp
    )
    target_link_libraries(
        MonopolyMeshGPUResourcesTests
        PRIVATE MonopolyGPU3DCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyMeshGPUResourcesTests PRIVATE /W4 /permissive-
        )
    endif()
    if(WIN32 AND SDL_SHARED)
        add_custom_command(
            TARGET MonopolyMeshGPUResourcesTests POST_BUILD
            COMMAND ${CMAKE_COMMAND} -E copy_if_different
                $<TARGET_RUNTIME_DLLS:MonopolyMeshGPUResourcesTests>
                $<TARGET_FILE_DIR:MonopolyMeshGPUResourcesTests>
            COMMAND_EXPAND_LISTS
        )
    endif()
    add_test(
        NAME Monopoly.MeshGPUResources
        COMMAND MonopolyMeshGPUResourcesTests
    )

    add_executable(
        MonopolyWorld3DGPUSceneTests
        tests/World3DGPUSceneTests.cpp
    )
    target_link_libraries(
        MonopolyWorld3DGPUSceneTests
        PRIVATE MonopolyGPU3DCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyWorld3DGPUSceneTests PRIVATE /W4 /permissive-
        )
    endif()
    if(WIN32 AND SDL_SHARED)
        add_custom_command(
            TARGET MonopolyWorld3DGPUSceneTests POST_BUILD
            COMMAND ${CMAKE_COMMAND} -E copy_if_different
                $<TARGET_RUNTIME_DLLS:MonopolyWorld3DGPUSceneTests>
                $<TARGET_FILE_DIR:MonopolyWorld3DGPUSceneTests>
            COMMAND_EXPAND_LISTS
        )
    endif()
    add_test(
        NAME Monopoly.World3DGPUScene
        COMMAND MonopolyWorld3DGPUSceneTests
    )

    add_executable(
        MonopolyWorld3DRendererTests
        tests/World3DRendererTests.cpp
        src/GPUFrame.cpp
        src/LogicalViewport.cpp
    )
    target_link_libraries(
        MonopolyWorld3DRendererTests
        PRIVATE MonopolyGPU3DCore
    )
    target_compile_definitions(
        MonopolyWorld3DRendererTests PRIVATE
        MONOPOLY_SHADER_DIR="${CMAKE_CURRENT_BINARY_DIR}/shaders"
    )
    if(MSVC)
        target_compile_options(
            MonopolyWorld3DRendererTests PRIVATE /W4 /permissive-
        )
    endif()
    if(WIN32 AND SDL_SHARED)
        add_custom_command(
            TARGET MonopolyWorld3DRendererTests POST_BUILD
            COMMAND ${CMAKE_COMMAND} -E copy_if_different
                $<TARGET_RUNTIME_DLLS:MonopolyWorld3DRendererTests>
                $<TARGET_FILE_DIR:MonopolyWorld3DRendererTests>
            COMMAND_EXPAND_LISTS
        )
    endif()
    add_test(
        NAME Monopoly.World3DRenderer
        COMMAND MonopolyWorld3DRendererTests
    )

    add_executable(
        MonopolyBoardLightingControllerTests
        tests/BoardLightingControllerTests.cpp
    )
    target_link_libraries(
        MonopolyBoardLightingControllerTests
        PRIVATE MonopolyGPU3DCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyBoardLightingControllerTests PRIVATE /W4 /permissive-
        )
    endif()
    if(WIN32 AND SDL_SHARED)
        add_custom_command(
            TARGET MonopolyBoardLightingControllerTests POST_BUILD
            COMMAND ${CMAKE_COMMAND} -E copy_if_different
                $<TARGET_RUNTIME_DLLS:MonopolyBoardLightingControllerTests>
                $<TARGET_FILE_DIR:MonopolyBoardLightingControllerTests>
            COMMAND_EXPAND_LISTS
        )
    endif()
    add_test(
        NAME Monopoly.BoardLightingController
        COMMAND MonopolyBoardLightingControllerTests
    )
endif()

# ============================================================
# Logical 800x600 viewport, resizing and letterbox contract
# ============================================================

if(BUILD_TESTING)

    add_executable(
        MonopolyLogicalViewportTests
        tests/LogicalViewportTests.cpp
        src/LogicalViewport.cpp
    )

    target_include_directories(
        MonopolyLogicalViewportTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )

    if(MSVC)
        target_compile_options(
            MonopolyLogicalViewportTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.LogicalViewport
        COMMAND MonopolyLogicalViewportTests
    )

endif()

target_sources(
    MonopolyModern
    PRIVATE
    src/LegacyAssets.cpp
)

if(MONOPOLY_LEGACY_BACKGROUNDS_AVAILABLE)
    add_custom_command(
        TARGET MonopolyModern
        POST_BUILD

        COMMAND ${CMAKE_COMMAND} -E make_directory
            "$<TARGET_FILE_DIR:MonopolyModern>/assets/legacy"

        COMMAND ${CMAKE_COMMAND} -E copy_if_different
            "${MONOPOLY_LEGACY_SOURCE_ROOT}/monopoly/BackGround.bmp"
            "$<TARGET_FILE_DIR:MonopolyModern>/assets/legacy/BackGround.bmp"

        COMMAND ${CMAKE_COMMAND} -E copy_if_different
            "${MONOPOLY_LEGACY_SOURCE_ROOT}/monopoly/BackGroundEuro.bmp"
            "$<TARGET_FILE_DIR:MonopolyModern>/assets/legacy/BackGroundEuro.bmp"
    )
endif()

target_sources(
    MonopolyModern
    PRIVATE
    src/BoardRules.cpp
    src/BoardGeometry.cpp
    src/BoardOwnershipHighlight.cpp
    src/PiecePlacement.cpp
    src/PieceRuntime.cpp
    src/PieceCamera.cpp
    src/PieceMovePlan.cpp
    src/PieceMovePlayback.cpp
    src/GameQueueGate.cpp
    src/PieceMoveIngress.cpp
    src/PieceInterpolation.cpp
    src/PieceJailPlan.cpp
    src/PieceJailPlayback.cpp
    src/PieceIdleTransition.cpp
    src/PieceIdlePlayback.cpp
    src/PieceIdleDisplay.cpp
    src/PieceBuildingDisplay.cpp
    src/PieceShadowDisplay.cpp
    src/DiceIngress.cpp
    src/DiceDisplay.cpp
    src/BoardCameraController.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/RuleRandom.cpp
    src/CardDecks.cpp
    src/RuleConfiguration.cpp
    src/RuleGameStart.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/RuleTurnActions.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/RuleEconomy.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/RuleBuildings.cpp
    src/RuleAuction.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/CardDeckRuntime.cpp
    src/RuleCards.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/RuleJail.cpp
    src/RuleStartingOrder.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/RuleTrade.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/RuleLifecycle.cpp
    src/RulePlayers.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/RuleArchive.cpp
    src/RuleResync.cpp
    src/RuleSave.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/RuleSynchronization.cpp
    src/RuleCoreActions.cpp
)

target_sources(
    MonopolyModern
    PRIVATE
    src/RuleOptions.cpp
    src/RuleInitialDeal.cpp
    src/RulePrediction.cpp
)

# ============================================================
# RULE scenario tests
# ============================================================

if(BUILD_TESTING)

    add_executable(
        MonopolyRuleTests
        tests/RuleScenarioTests.cpp
        src/BoardRules.cpp
        src/PhaseStack.cpp
        src/RuleArchive.cpp
        src/RuleOptions.cpp
        src/RuleConfiguration.cpp
    )

    target_include_directories(
        MonopolyRuleTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )

    if(MSVC)
        target_compile_options(
            MonopolyRuleTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.RuleScenarios
        COMMAND MonopolyRuleTests
    )

endif()

# ============================================================
# Original Userifce local-player ownership model
# ============================================================

target_sources(
    MonopolyModern
    PRIVATE
    src/LocalPlayers.cpp
)

# ============================================================
# UDPSEL player setup state machine
# ============================================================

target_sources(
    MonopolyModern
    PRIVATE
    src/PlayerSetupFlow.cpp
)

if(BUILD_TESTING)

    add_executable(
        MonopolyPlayerSetupTests
        tests/PlayerSetupFlowTests.cpp
        src/PlayerSetupFlow.cpp
        src/RuleOptions.cpp
    )

    target_include_directories(
        MonopolyPlayerSetupTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )

    if(MSVC)
        target_compile_options(
            MonopolyPlayerSetupTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.PlayerSetup
        COMMAND MonopolyPlayerSetupTests
    )

endif()

if(BUILD_TESTING)

    add_executable(
        MonopolyLocalPlayersTests
        tests/LocalPlayersTests.cpp
        src/LocalPlayers.cpp
        src/Messaging.cpp
    )

    target_include_directories(
        MonopolyLocalPlayersTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )

    if(MSVC)
        target_compile_options(
            MonopolyLocalPlayersTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.LocalPlayers
        COMMAND MonopolyLocalPlayersTests
    )

endif()

# ============================================================
# DISPLAY_UDIBAR
# Player-select status/player bar
# ============================================================

target_sources(
    MonopolyModern
    PRIVATE
    src/IBar.cpp
    src/IBarLayout.cpp
    src/IBarBackdropPlayback.cpp
    src/IBarCardPlayback.cpp
    src/IBarJailCardPlayback.cpp
    src/IBarScoreStripPlayback.cpp
    src/IBarPropertyPlayback.cpp
    src/IBarCurrentPlayerPlayback.cpp
    src/IBarBankPlayback.cpp
    src/IBarCameraButtonPlayback.cpp
    src/IBarRuleState.cpp
)

if(BUILD_TESTING)

    add_executable(
        MonopolyIBarTests
        tests/IBarLayoutTests.cpp
        src/IBarLayout.cpp
    )

    target_include_directories(
        MonopolyIBarTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )

    if(MSVC)
        target_compile_options(
            MonopolyIBarTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.IBarLayout
        COMMAND MonopolyIBarTests
    )

endif()

if(BUILD_TESTING)
    add_executable(
        MonopolyIBarActionInputTests
        tests/IBarActionInputTests.cpp
        src/IBar.cpp
        src/IBarLayout.cpp
    )
    target_include_directories(
        MonopolyIBarActionInputTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )
    if(MSVC)
        target_compile_options(
            MonopolyIBarActionInputTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.IBarActionInput
        COMMAND MonopolyIBarActionInputTests
    )
endif()

if(BUILD_TESTING)

    add_executable(
        MonopolyIBarBackdropPlaybackTests
        tests/IBarBackdropPlaybackTests.cpp
        src/IBarBackdropPlayback.cpp
        src/IBarCardPlayback.cpp
        src/IBarJailCardPlayback.cpp
        src/IBarScoreStripPlayback.cpp
        src/IBarPropertyPlayback.cpp
        src/IBarLayout.cpp
        src/IBarCurrentPlayerPlayback.cpp
        src/IBarBankPlayback.cpp
        src/IBarCameraButtonPlayback.cpp
        src/RuntimeState.cpp
    )
    target_include_directories(
        MonopolyIBarBackdropPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyIBarBackdropPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyIBarBackdropPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.IBarBackdropPlayback
        COMMAND MonopolyIBarBackdropPlaybackTests
    )

endif()

if(BUILD_TESTING)
    add_executable(
        MonopolyIBarJailCardPlaybackTests
        tests/IBarJailCardPlaybackTests.cpp
        src/IBarJailCardPlayback.cpp
    )
    target_include_directories(
        MonopolyIBarJailCardPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyIBarJailCardPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyIBarJailCardPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.IBarJailCardPlayback
        COMMAND MonopolyIBarJailCardPlaybackTests
    )
endif()

if(BUILD_TESTING)
    add_executable(
        MonopolyIBarScoreStripPlaybackTests
        tests/IBarScoreStripPlaybackTests.cpp
        src/IBarScoreStripPlayback.cpp
        src/IBarLayout.cpp
    )
    target_include_directories(
        MonopolyIBarScoreStripPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyIBarScoreStripPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyIBarScoreStripPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.IBarScoreStripPlayback
        COMMAND MonopolyIBarScoreStripPlaybackTests
    )
endif()

if(BUILD_TESTING)
    add_executable(
        MonopolyIBarPropertyPlaybackTests
        tests/IBarPropertyPlaybackTests.cpp
        src/IBarPropertyPlayback.cpp
        src/IBarLayout.cpp
    )
    target_include_directories(
        MonopolyIBarPropertyPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyIBarPropertyPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyIBarPropertyPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.IBarPropertyPlayback
        COMMAND MonopolyIBarPropertyPlaybackTests
    )
endif()

if(BUILD_TESTING)
    add_executable(
        MonopolyIBarCardPlaybackTests
        tests/IBarCardPlaybackTests.cpp
        src/IBarCardPlayback.cpp
    )
    target_include_directories(
        MonopolyIBarCardPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyIBarCardPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyIBarCardPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.IBarCardPlayback
        COMMAND MonopolyIBarCardPlaybackTests
    )
endif()

if(BUILD_TESTING)
    add_executable(
        MonopolyIBarCurrentPlayerPlaybackTests
        tests/IBarCurrentPlayerPlaybackTests.cpp
        src/IBarCurrentPlayerPlayback.cpp
    )
    target_include_directories(
        MonopolyIBarCurrentPlayerPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyIBarCurrentPlayerPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyIBarCurrentPlayerPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.IBarCurrentPlayerPlayback
        COMMAND MonopolyIBarCurrentPlayerPlaybackTests
    )
endif()

if(BUILD_TESTING)
    add_executable(
        MonopolyIBarBankPlaybackTests
        tests/IBarBankPlaybackTests.cpp
        src/IBarBankPlayback.cpp
    )
    target_include_directories(
        MonopolyIBarBankPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyIBarBankPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyIBarBankPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(NAME Monopoly.IBarBankPlayback COMMAND MonopolyIBarBankPlaybackTests)
endif()

if(BUILD_TESTING)
    add_executable(
        MonopolyIBarCameraButtonPlaybackTests
        tests/IBarCameraButtonPlaybackTests.cpp
        src/IBarCameraButtonPlayback.cpp
    )
    target_include_directories(
        MonopolyIBarCameraButtonPlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyIBarCameraButtonPlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyIBarCameraButtonPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.IBarCameraButtonPlayback
        COMMAND MonopolyIBarCameraButtonPlaybackTests
    )
endif()

if(BUILD_TESTING)
    add_executable(
        MonopolyIBarRuleStateTests
        tests/IBarRuleStateTests.cpp
        src/IBarRuleState.cpp
    )
    target_include_directories(
        MonopolyIBarRuleStateTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )
    if(MSVC)
        target_compile_options(
            MonopolyIBarRuleStateTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.IBarRuleState
        COMMAND MonopolyIBarRuleStateTests
    )
endif()

if(BUILD_TESTING)

    add_executable(
        MonopolyDisplayContractTests
        tests/DisplayContractTests.cpp
        src/Display.cpp
        src/BoardCameraController.cpp
        src/PieceCamera.cpp
    )

    target_include_directories(
        MonopolyDisplayContractTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )

    if(MSVC)
        target_compile_options(
            MonopolyDisplayContractTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.DisplayContract
        COMMAND MonopolyDisplayContractTests
    )

endif()
# ============================================================
# DISPLAY_UDPIECES geometry subset
# ============================================================

if(BUILD_TESTING)

    add_executable(
        MonopolyPiecePlacementTests
        tests/PiecePlacementTests.cpp
        src/PiecePlacement.cpp
        src/BoardGeometry.cpp
    )

    target_include_directories(
        MonopolyPiecePlacementTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )

    if(MSVC)
        target_compile_options(
            MonopolyPiecePlacementTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.PiecePlacement
        COMMAND MonopolyPiecePlacementTests
    )

    add_executable(
        MonopolyPieceRuntimeTests
        tests/PieceRuntimeTests.cpp
        src/PieceRuntime.cpp
        src/PiecePlacement.cpp
        src/BoardGeometry.cpp
    )

    target_include_directories(
        MonopolyPieceRuntimeTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )

    target_link_libraries(
        MonopolyPieceRuntimeTests
        PRIVATE
        MonopolyDataCore
    )

    if(MSVC)
        target_compile_options(
            MonopolyPieceRuntimeTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.PieceRuntime
        COMMAND MonopolyPieceRuntimeTests
    )

endif()

# DISPLAY_UDPIECES camera-selection subset
if(BUILD_TESTING)
    add_executable(
        MonopolyPieceCameraTests
        tests/PieceCameraTests.cpp
        src/PieceCamera.cpp
    )

    target_include_directories(
        MonopolyPieceCameraTests PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src
    )

    target_link_libraries(
        MonopolyPieceCameraTests PRIVATE MonopolyDataCore
    )

    if(MSVC)
        target_compile_options(
            MonopolyPieceCameraTests PRIVATE /W4 /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.PieceCamera
        COMMAND MonopolyPieceCameraTests
    )
endif()
# DISPLAY_UDPIECES move-plan subset
if(BUILD_TESTING)
    add_executable(
        MonopolyPieceMovePlanTests
        tests/PieceMovePlanTests.cpp
        src/PieceMovePlan.cpp
        src/PieceCamera.cpp
    )

    target_include_directories(
        MonopolyPieceMovePlanTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )
    if(MSVC)
        target_compile_options(
            MonopolyPieceMovePlanTests
            PRIVATE
            /W4
            /permissive-
        )
    endif()

    add_test(
        NAME Monopoly.PieceMovePlan
        COMMAND MonopolyPieceMovePlanTests
    )
endif()
# DISPLAY_UDPIECES move-playback subset
if(BUILD_TESTING)
    add_executable(
        MonopolyPieceMovePlaybackTests
        tests/PieceMovePlaybackTests.cpp
        src/PieceMovePlayback.cpp
        src/PiecePlacement.cpp
        src/BoardGeometry.cpp
    )

    target_include_directories(
        MonopolyPieceMovePlaybackTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyPieceMovePlaybackTests
        PRIVATE
        MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyPieceMovePlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.PieceMovePlayback
        COMMAND MonopolyPieceMovePlaybackTests
    )
endif()


# Userifce.cpp game-queue animation gate
if(BUILD_TESTING)
    add_executable(
        MonopolyGameQueueGateTests
        tests/GameQueueGateTests.cpp
        src/GameQueueGate.cpp
    )
    target_include_directories(
        MonopolyGameQueueGateTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )
    if(MSVC)
        target_compile_options(
            MonopolyGameQueueGateTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.GameQueueGate
        COMMAND MonopolyGameQueueGateTests
    )
endif()
# DISPLAY_UDPIECES movement-message ingress
if(BUILD_TESTING)
    add_executable(
        MonopolyPieceMoveIngressTests
        tests/PieceMoveIngressTests.cpp
        src/PieceMoveIngress.cpp
        src/PieceMovePlan.cpp
        src/PieceCamera.cpp
    )
    target_include_directories(
        MonopolyPieceMoveIngressTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )
    if(MSVC)
        target_compile_options(
            MonopolyPieceMoveIngressTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.PieceMoveIngress
        COMMAND MonopolyPieceMoveIngressTests
    )
endif()
# UDUtils.cpp 3D interpolation subset
if(BUILD_TESTING)
    add_executable(
        MonopolyPieceInterpolationTests
        tests/PieceInterpolationTests.cpp
        src/PieceInterpolation.cpp
    )
    target_include_directories(
        MonopolyPieceInterpolationTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )
    if(MSVC)
        target_compile_options(
            MonopolyPieceInterpolationTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.PieceInterpolation
        COMMAND MonopolyPieceInterpolationTests
    )
endif()
# UDPieces.cpp GoToJail route-planning subset
if(BUILD_TESTING)
    add_executable(
        MonopolyPieceJailPlanTests
        tests/PieceJailPlanTests.cpp
        src/PieceJailPlan.cpp
        src/PieceInterpolation.cpp
        src/PiecePlacement.cpp
        src/BoardGeometry.cpp
        src/PieceCamera.cpp
    )
    target_include_directories(
        MonopolyPieceJailPlanTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )
    if(MSVC)
        target_compile_options(
            MonopolyPieceJailPlanTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(NAME Monopoly.PieceJailPlan COMMAND MonopolyPieceJailPlanTests)
endif()
# UDPieces.cpp GoToJail playback state machine
if(BUILD_TESTING)
    add_executable(
        MonopolyPieceJailPlaybackTests
        tests/PieceJailPlaybackTests.cpp
        src/PieceJailPlayback.cpp
        src/PieceJailPlan.cpp
        src/PieceInterpolation.cpp
        src/PiecePlacement.cpp
        src/BoardGeometry.cpp
        src/PieceCamera.cpp
        src/PieceMoveIngress.cpp
        src/PieceMovePlan.cpp
    )
    target_include_directories(
        MonopolyPieceJailPlaybackTests
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyPieceJailPlaybackTests
        PRIVATE
        MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyPieceJailPlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.PieceJailPlayback
        COMMAND MonopolyPieceJailPlaybackTests
    )
endif()

# UDPieces.cpp center/resting idle transition planner
if(BUILD_TESTING)
    add_executable(
        MonopolyPieceIdleTransitionTests
        tests/PieceIdleTransitionTests.cpp
        src/PieceIdleTransition.cpp
        src/PiecePlacement.cpp
        src/BoardGeometry.cpp
    )
    target_include_directories(
        MonopolyPieceIdleTransitionTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )
    target_link_libraries(
        MonopolyPieceIdleTransitionTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyPieceIdleTransitionTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.PieceIdleTransition
        COMMAND MonopolyPieceIdleTransitionTests
    )
endif()

# UDPieces.cpp center/resting idle transition playback
if(BUILD_TESTING)
    add_executable(
        MonopolyPieceIdlePlaybackTests
        tests/PieceIdlePlaybackTests.cpp
        src/PieceIdlePlayback.cpp
    src/PieceIdleDisplay.cpp
        src/PieceIdleTransition.cpp
        src/PiecePlacement.cpp
        src/BoardGeometry.cpp
    )
    target_include_directories(
        MonopolyPieceIdlePlaybackTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyPieceIdlePlaybackTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyPieceIdlePlaybackTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.PieceIdlePlayback
        COMMAND MonopolyPieceIdlePlaybackTests
    )
endif()

# UDPieces.cpp persistent center/resting token idles
if(BUILD_TESTING)
    add_executable(
        MonopolyPieceIdleDisplayTests
        tests/PieceIdleDisplayTests.cpp
        src/PieceIdleDisplay.cpp
        src/PieceIdleTransition.cpp
        src/PiecePlacement.cpp
        src/BoardGeometry.cpp
    )
    target_include_directories(
        MonopolyPieceIdleDisplayTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyPieceIdleDisplayTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyPieceIdleDisplayTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.PieceIdleDisplay
        COMMAND MonopolyPieceIdleDisplayTests
    )
endif()

# UDPieces.cpp persistent house/hotel display
if(BUILD_TESTING)
    add_executable(
        MonopolyPieceBuildingDisplayTests
        tests/PieceBuildingDisplayTests.cpp
        src/PieceBuildingDisplay.cpp
        src/PiecePlacement.cpp
        src/BoardGeometry.cpp
    )
    target_include_directories(
        MonopolyPieceBuildingDisplayTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyPieceBuildingDisplayTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyPieceBuildingDisplayTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.PieceBuildingDisplay
        COMMAND MonopolyPieceBuildingDisplayTests
    )
endif()

# UDPieces.cpp persistent token shadows
if(BUILD_TESTING)
    add_executable(
        MonopolyPieceShadowDisplayTests
        tests/PieceShadowDisplayTests.cpp
        src/PieceShadowDisplay.cpp
        src/PieceRuntime.cpp
    )
    target_include_directories(
        MonopolyPieceShadowDisplayTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyPieceShadowDisplayTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyPieceShadowDisplayTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.PieceShadowDisplay
        COMMAND MonopolyPieceShadowDisplayTests
    )
endif()

# UDPieces.cpp dice notification ingress
if(BUILD_TESTING)
    add_executable(
        MonopolyDiceIngressTests
        tests/DiceIngressTests.cpp
        src/DiceIngress.cpp
    )
    target_include_directories(MonopolyDiceIngressTests PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)
    if(MSVC)
        target_compile_options(MonopolyDiceIngressTests PRIVATE /W4 /permissive-)
    endif()
    add_test(NAME Monopoly.DiceIngress COMMAND MonopolyDiceIngressTests)
endif()

# UDPieces.cpp 2D/3D dice display and roll timing
if(BUILD_TESTING)
    add_executable(
        MonopolyDiceDisplayTests
        tests/DiceDisplayTests.cpp
        src/DiceDisplay.cpp
    )
    target_include_directories(
        MonopolyDiceDisplayTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(MonopolyDiceDisplayTests PRIVATE MonopolyDataCore)
    if(MSVC)
        target_compile_options(MonopolyDiceDisplayTests PRIVATE /W4 /permissive-)
    endif()
    add_test(NAME Monopoly.DiceDisplay COMMAND MonopolyDiceDisplayTests)
endif()

# TimeStep queue-lock integration
if(BUILD_TESTING)
    add_executable(
        MonopolyTimeStepLockTests
        tests/TimeStepLockTests.cpp
        src/TimeStep.cpp
        src/GameQueueGate.cpp
    )
    target_include_directories(
        MonopolyTimeStepLockTests PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src
    )
    if(MSVC)
        target_compile_options(
            MonopolyTimeStepLockTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.TimeStepLock
        COMMAND MonopolyTimeStepLockTests
    )
endif()

# UDBoard.cpp ownership/mortgage highlights, including raw DataUAP Main fallback
if(BUILD_TESTING)
    add_executable(
        MonopolyBoardOwnershipHighlightTests
        tests/BoardOwnershipHighlightTests.cpp
        src/BoardOwnershipHighlight.cpp
        src/BoardGeometry.cpp
        src/BoardRules.cpp
    )
    target_include_directories(
        MonopolyBoardOwnershipHighlightTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(
        MonopolyBoardOwnershipHighlightTests PRIVATE MonopolyDataCore
    )
    if(MSVC)
        target_compile_options(
            MonopolyBoardOwnershipHighlightTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.BoardOwnershipHighlight
        COMMAND MonopolyBoardOwnershipHighlightTests
    )
endif()

# UDBoard.cpp historical 3D board camera interpolation
if(BUILD_TESTING)
    add_executable(
        MonopolyBoardCameraControllerTests
        tests/BoardCameraControllerTests.cpp
        src/BoardCameraController.cpp
    )
    target_include_directories(
        MonopolyBoardCameraControllerTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
    )
    if(MSVC)
        target_compile_options(
            MonopolyBoardCameraControllerTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.BoardCameraController
        COMMAND MonopolyBoardCameraControllerTests
    )
endif()
# Generic 2D SequencePlayback path used by UDPieces dice/IBar overlays
if(BUILD_TESTING)
    add_executable(
        MonopolySequencePlayback2DTests
        tests/SequencePlayback2DTests.cpp
    )
    target_include_directories(
        MonopolySequencePlayback2DTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(MonopolySequencePlayback2DTests PRIVATE MonopolyDataCore)
    if(MSVC)
        target_compile_options(
            MonopolySequencePlayback2DTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.SequencePlayback2D
        COMMAND MonopolySequencePlayback2DTests
    )
endif()
# CPU render intent for legacy 2D bitmap sequence leaves
if(BUILD_TESTING)
    add_executable(
        MonopolySequenceBitmapRenderDataTests
        tests/SequenceBitmapRenderDataTests.cpp
    )
    target_include_directories(
        MonopolySequenceBitmapRenderDataTests PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/src
        ${CMAKE_CURRENT_SOURCE_DIR}/tests
    )
    target_link_libraries(MonopolySequenceBitmapRenderDataTests PRIVATE MonopolyDataCore)
    if(MSVC)
        target_compile_options(
            MonopolySequenceBitmapRenderDataTests PRIVATE /W4 /permissive-
        )
    endif()
    add_test(
        NAME Monopoly.SequenceBitmapRenderData
        COMMAND MonopolySequenceBitmapRenderDataTests
    )
endif()

if(BUILD_TESTING)
    add_executable(MonopolyBitmapRuntimeTests tests/BitmapRuntimeTests.cpp)
    target_include_directories(MonopolyBitmapRuntimeTests PRIVATE src tests)
    target_link_libraries(MonopolyBitmapRuntimeTests PRIVATE MonopolyDataCore)
    add_test(NAME Monopoly.BitmapRuntime COMMAND MonopolyBitmapRuntimeTests)
endif()

if(BUILD_TESTING)
    add_executable(MonopolyWorld2DRendererTests tests/World2DRendererTests.cpp src/DiceDisplay.cpp)
    target_include_directories(MonopolyWorld2DRendererTests PRIVATE src tests)
    target_link_libraries(MonopolyWorld2DRendererTests PRIVATE MonopolyGPU3DCore)
    target_compile_definitions(MonopolyWorld2DRendererTests PRIVATE
        MONOPOLY_SHADER_DIR="${CMAKE_CURRENT_BINARY_DIR}/shaders")
    add_test(NAME Monopoly.World2DRenderer COMMAND MonopolyWorld2DRendererTests)
    set_tests_properties(Monopoly.World2DRenderer PROPERTIES TIMEOUT 60)
endif()

if(BUILD_TESTING)
    add_executable(MonopolyDice2DPlaybackTests tests/Dice2DPlaybackTests.cpp
        src/DiceDisplay.cpp src/DiceIngress.cpp)
    target_include_directories(MonopolyDice2DPlaybackTests PRIVATE src tests)
    target_link_libraries(MonopolyDice2DPlaybackTests PRIVATE MonopolyDataCore)
    add_test(NAME Monopoly.Dice2DPlayback COMMAND MonopolyDice2DPlaybackTests)
endif()

if(BUILD_TESTING)
    add_executable(MonopolyRuntimeBitmapSurfaceTests tests/RuntimeBitmapSurfaceTests.cpp)
    target_include_directories(MonopolyRuntimeBitmapSurfaceTests PRIVATE src tests)
    target_link_libraries(MonopolyRuntimeBitmapSurfaceTests PRIVATE MonopolyDataCore)
    if(MSVC)
        target_compile_options(MonopolyRuntimeBitmapSurfaceTests PRIVATE /W4 /permissive-)
    endif()
    add_test(NAME Monopoly.RuntimeBitmapSurface COMMAND MonopolyRuntimeBitmapSurfaceTests)
endif()

target_sources(MonopolyModern PRIVATE src/BoardBackdropPlayback.cpp)

if(BUILD_TESTING)
    add_executable(MonopolyBoardBackdropPlaybackTests
        tests/BoardBackdropPlaybackTests.cpp
        src/BoardBackdropPlayback.cpp)
    target_include_directories(MonopolyBoardBackdropPlaybackTests PRIVATE src tests)
    target_link_libraries(MonopolyBoardBackdropPlaybackTests PRIVATE MonopolyDataCore)
    if(MSVC)
        target_compile_options(MonopolyBoardBackdropPlaybackTests PRIVATE /W4 /permissive-)
    endif()
    add_test(NAME Monopoly.BoardBackdropPlayback COMMAND MonopolyBoardBackdropPlaybackTests)
endif()
